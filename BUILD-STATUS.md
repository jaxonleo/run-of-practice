# Run of Practice — Build Status

**Last updated:** 2026-07-21 (tenth session, continued — Coach/Org mode)
**Status:** Live in production at [runofpractice.com](https://www.runofpractice.com), single real user (solo coach), actively iterating ahead of August testing. Same session as the org-model build below, continued after Jax hit two real bugs testing it live (team_staff never granted to the org-team creator; a broken/no-op "Invite Coach") and then asked for a proper Coach-vs-Organization mode split, including a real mine-vs-org content split for Locations/Equipment/Skill Tags. See the "Coach/Org mode" entry at the top of the session log below for the full list of what changed. Frontend + one new migration (`20260721050000_org_create_team_head_coach.sql`, already applied to production) are committed and pushed to `main`.

This file is maintained at the end of each working session so a fresh Claude conversation (or a human) can pick up context quickly. If you're an AI assistant reading this cold: read this whole file before touching code, then check `git log --oneline -20` for anything more recent than "Last updated" above.

---

## What this is

A live-practice-orchestration app for youth sports coaches: build a practice plan (drills, stations, timing), run it live with a timer/rotation system, share a read-only view with parent helpers, track attendance, and plan on a recurring schedule. React/Vite frontend, Supabase (Postgres + Auth + Realtime) backend, deployed on Vercel.

## Stack & repo layout

- **Frontend:** React 18, Vite, no CSS framework (hand-rolled CSS-in-JS injected via a `<style>` tag, Barlow Condensed/DM Mono fonts). No component library.
- **Backend:** Supabase — Postgres with row-level security on every table, Auth (email OTP), Realtime for live-session sync.
- **Deploy:** Vercel, auto-deploys `main` to production.
- `src/App.jsx` — still the largest file; holds `TeamsScreen`, `BuilderScreen`, `PlayerProfile`, `RostersTab`, `NotesTab`, `AuthScreen`, `NameScreen`, and the top-level `App()` router. Other major screens have been split into their own files under `src/components/`.
- `src/supabase.js` — the entire data-access layer. Every DB read/write goes through a named export here; nothing calls `supabase.from(...)` directly from a component.
- `src/constants.js` — pure helpers + shared constants (`uid()`, `TEAM_COLORS`, grouping math, `INIT` shape).
- `supabase/migrations/*.sql` — 93 migrations, applied directly via the Supabase Management API SQL endpoint (no local Supabase CLI in this environment). Each new feature gets one dated migration file with inline comments explaining *why*, not just *what*.
- `supabase/functions/*` — Edge Functions (Deno), deployed via the Management API's `/functions/deploy` multipart endpoint (also no local CLI). First one added 2026-07-10 (`notify-team-staff-added`). Secrets set via `POST /v1/projects/{ref}/secrets`, never committed to migrations.

## Feature inventory (what actually works today)

**Auth & account**
- Email OTP sign-in (code, not magic link — magic links broke for the installed-homescreen-PWA case). Branded email via Resend SMTP.
- One-time name capture on first sign-in (covers pre-existing accounts too).
- Soft account deactivation (reversible, auto-reactivates on next sign-in) and team deletion.

**Teams & roster**
- Teams with sport, timezone, season dates, and a color (auto-assigned from a curated palette on creation, editable, includes black/dark gray) — now shown as a left-edge stripe on the Teams list and team detail header.
- Players, staff (head coach/assistant/helper roles), structured focus areas (skill-tag taxonomy + coach freeform sub-tags).
- **Team creator is automatically head coach** (trigger on team insert, personal teams only) with a one-time backfill for pre-existing teams.
- **Staff invite linking**: adding a coach by email (`add_team_staff` RPC) silently links to an existing account if one exists, and a `claim_pending_team_staff` trigger auto-links any pending invite the moment the invited person signs up — covers both orderings. Re-adding someone who left revives their archived row instead of duplicating.
- **Invite notification email**: a `pg_net`-triggered Edge Function sends an informational (non-auth) email via Resend when staff are added — never Supabase's `admin.inviteUserByEmail` (that's a magic link, the exact mechanism already removed for PWA sign-in).
- **One-time welcome card + self-serve leave**: a newly-linked staff member sees "You've been added to X by Y" once, with a "Leave" link (`leave_team` RPC, narrow — archives only the caller's own row, refuses for team owners).
- **Staff-add suggestions**: adding a coach shows tap-to-fill chips for staff you've already added on your other teams (deduped by email; no account-existence search beyond that).
- **Role-aware UI**: assistants/helpers view everything and can run live sessions, but Builder/schedule-wizard/team-settings/staff-management entry points are hidden for teams they don't head-coach — checked per-team (`isHeadCoach` in `constants.js`), not globally, since a user can be head coach on one team and assistant on another.

**Library**
- Drills, equipment/assets, templates — coach-owned with an org-sharing model (share to org, browse "From Our Coaches").
- Manual drill reordering.
- **Skill tags** (2026-07-11, fifth session): each coach gets a personal starter set of skill tags seeded automatically (56 tags across Baseball's and Basketball's 7 categories each — the only two sports with `skill_categories` today), via `seed_default_skill_tags_for_coach(coach_id)` (migration `20260711000000_seed_default_skill_tags.sql`). Deliberately **not** true `scope='global'` rows — those can only be managed by service_role/`is_org_admin` per the existing RLS (`skill_tags_update_manage`), so one coach archiving a shared global tag would silently remove it for every other coach. Seeding a personal `scope='coach'` copy per coach sidesteps that with zero RLS changes, reusing the exact machinery that already existed for a coach's own custom tags. The RPC is also called client-side on every sign-in (`ensureDefaultSkillTags`, idempotent) so a coach picks up tags for any sport/category added in a later migration, not just what existed at signup. New Library tab ("Skills") lets a coach add/remove tags per sport/category (`SkillsTab` in `NewLibraryScreen.jsx`) — categories are still curated/read-only, only the tags underneath are coach-editable. The drill editor's tag picker (`ModalLayer.jsx`) no longer shows a "(mine)"/"(org)" suffix on tag pills, since virtually every tag a solo coach sees is now their own. **Picker redesign (2026-07-11, sixth session)**: with 56+ tags across many categories, the always-expanded flat pill grid stopped scaling. `SkillTagPicker` (new component in `ModalLayer.jsx`) now shows only the selected tags as removable chips plus an "Add/Edit Skill Tags" button; clicking it opens the full category-grouped, searchable picker in a bottom-sheet overlay (reusing the existing `.movly`/`.modal` classes at a higher z-index so it layers over the drill editor's own sheet). Only the drill editor's picker was touched — `PlayerProfile`'s focus-area add flow (`App.jsx`) was already a one-at-a-time category drill-down, not a flat pill grid, so it wasn't in scope.

**Planning & scheduling**
- **Builder**: practice/template editor — drills, checklist activities (intro/closer), station blocks with rotation, grouping (whole/partners/N groups), equipment, coaching points, per-activity coach/sublocation assignment.
- **Recurring schedules**: a wizard (days of week → time/duration → date range defaulted from team season → location → preview with per-date deselect) backed by a `create_practice_series` RPC — atomic, server-capped (≤150 occurrences, ≤400 day range), timezone-correct (DST-safe via `AT TIME ZONE`).
- **Home screen**: adaptive hero card for the next practice (Plan/Review/Run depending on state), "N practices need a plan" nudge, 14-day agenda, quick actions.
- **Schedule tab**: Agenda (day-grouped, team-color-coded, team filter chips) and Month (calendar grid with per-day dots) views. **Dot semantics fixed (2026-07-11, sixth session)**: Agenda's per-practice team-color dots (`ScheduleScreen.jsx`, `HomeScreen.jsx`) were always solid regardless of plan status — only Month's dots were correctly hollow-when-unplanned/filled-when-planned. Agenda dots now follow the same rule (border always the team color, fill only when `planned`).
- **Planned absences**: capture from Practice Detail ("Who's out?"), Player Profile ("Mark out for..."), or Home quick action. Feeds Builder's default group assignment (absent players excluded from auto-splits) and the live session's attendance defaults.
- Cancel (this-occurrence or this-and-future for a series) / restore; a "missed" state for practices whose time passed with no run.
- All practice times display in the **team's** timezone, not the viewing device's.
- **"Today" is now the viewer's local calendar day, not UTC (2026-07-11, sixth session)**: every "today"/"tomorrow"/"in 14 days" computation across `App.jsx`, `HomeScreen.jsx`, `ScheduleScreen.jsx`, `PracticeDetail.jsx`, `AbsencePicker.jsx`, `NewLibraryScreen.jsx`, and `constants.js`'s demo seed used `new Date().toISOString().slice(0,10)`, which converts to UTC before truncating. West of Greenwich this rolls "today" over to the next calendar day hours before local midnight — reported symptom: a Phoenix coach (UTC-7) checking at 8:46pm Saturday saw Sunday highlighted as "today" in Month view and Monday's practices labeled "Tomorrow" in the Home agenda. Fixed with a new `localDateStr()` helper in `constants.js` (same local-getter construction Month view's grid already used correctly) and swapped in everywhere the UTC-based pattern appeared.
- **Planning-depth indicators**: when a practice has both activities and a `scheduled_duration_minutes`, agenda rows/hero card/day sheet/Practice Detail show "35/60 min" — half-filled/amber for partial (short by >max(10min,15%)), warning-tinted for overplanned (over by >5min), filled+check when within tolerance. Purely derived, never stored. Month-view dots stay binary on purpose (a third fill state doesn't read at dot size). `planningState()`/`sumMins()` in `constants.js`.
- **Getting-started checklist**: a single "?" icon on Home (with a dot while incomplete), opening a 5-step checklist (team → roster → schedule → first plan → first live run) fully derived from existing data, no stored progress flags.
- Series wizard no longer exposes sublocation at the practice level (station/activity-level sublocation is unaffected) — that's drill/station detail, not "where do we meet."

**Live sessions**
- Real-time state machine (`practice_live_sessions`) with take-control/hand-off between coaches, pause/resume, ±1 min nudge, station rotation, append-only attendance (`session_attendance`) and per-activity dwell-time logging.
- Offline resilience: single-pending-write retry with backoff, distinguishes "network down" from "someone else moved the session" so a signal drop doesn't blank the screen.
- Mid-run plan editing (`LiveEditBuilder`).

**Helper/parent-facing (anonymous, token-based, no login)**
- `/preview/:token` — pre-practice setup view (equipment, roster, countdown to start).
- `/live/:token` — live view during a run (current drill/station, roster, optional attendance-marking scope).
- Player data is minimized (first name + last-initial + jersey), everything else (equipment, coaching points, location) is shown — a deliberate choice, not an oversight.

**Public landing page, feedback, and legal pages** (2026-07-10, second + third sessions)
- `/` is now adaptive on session state (`src/App.jsx`): signed-out renders `LandingPage`, signed-in renders the app exactly as before. Deliberately not a hard route split — manifest `start_url` stays `/`, so an already-installed PWA icon keeps launching straight into the app.
- Landing page CTAs ("Try it free" / "Already have an account? Sign in") both lead to the same `AuthScreen` (now with a back button to return to the landing page), just visually weighted differently.
- New `feedback` table + `submit_public_feedback` RPC (anon, narrow) — see migration `20260710050000_public_feedback.sql`. This is now only used by the in-app Home "?" → "Send Feedback" flow (`submitFeedback`, a separate authenticated function/modal in `HomeScreen.jsx`) — the landing page's own anonymous form was removed (see below), so `submitPublicFeedback` in `supabase.js` is currently unused dead code, left in place since the RPC/table might still be wanted for a future non-authenticated contact path.
- Sign-in screen links Terms/Privacy under the Send Code button.
- **Third session — Landing Page V2 (brand voice) rewrite**, per `ROP-Landing-V2-Addendum.md`: replaced the original hero/"Founding Coach"/TeamSnap-comparison copy with the brand-foundation-derived structure — hero ("Make every minute of practice count."), problem section, 4-item "what it does" (same capability set, new copy), a "what it is" card (the term "coaching operations platform" appears exactly once, only here), an "Early access" section ("Built by a coach who still coaches"), a closing line, and a footer with Terms/Privacy/contact email/Sign in. **No real product screenshots yet** — the "what it does" grid still uses the pre-existing icon cards as a placeholder (Jax has none ready; explicitly deferred, not forgotten — swap in real screenshots when available, addendum §1 forbids stylized mockups). Copy is deliberately sport/level-agnostic ("coaches"/"players"/"helpers", no "youth"/"kids"/"parents") per the addendum's positioning decision.
- **Third session, later same day — inline feedback form removed + copy pass**: Jax didn't want an always-visible email/message/"Send Feedback" form on the landing page, and flagged the copy as reading AI-generated (em dashes, listy cadence). `FeedbackBlock` and its `#feedback` anchor are gone from `LandingPage.jsx`; the "tell us" link in the Early Access section now just opens `mailto:contact@runofpractice.com`. Also rewrote the problem section (dropped the "You put in the hours to prepare" opener, smoothed the choppy multi-sentence list into flowing prose) and removed every em dash on the page.
- **Third session — legal pages finalized**: `/terms` and `/privacy` (`src/components/LegalPages.jsx`) now render Jax's attorney-cleared final copy verbatim (level-agnostic wording included) — the draft-notice banner and the italic "placeholder pending legal review" footers from the first landing-page session are gone; that draft language is superseded, not lost (still in git history / the superseded addendum). Routed via explicit `vercel.json` rewrites added in the second session (`/live` and `/preview` were the only prior entries — anything not covered there 404s on Vercel's static host).
- **Sixth session (2026-07-11) — full rewrite to a product-walkthrough marketing site**: `LandingPage.jsx` replaced entirely per Jax's new spec (a coach should understand the product in seconds, each section answers the next logical question). Now a proper desktop marketing page, not just the mobile app's old single-column scroll card: sticky header (logo+name, How It Works/Features/Early Access anchors, Sign In, Try It Free — nav anchors hide under 860px, mobile keeps just the two CTAs), a hero with a live-practice-view mockup, then 14 alternating copy/visual sections (Schedule, Drill Library, Practice Builder, Stations/Groupings, Locations/Equipment, Templates, a plan→live transition line, Live Practice View, Assistant/Helper Views, Consistent Coaching, Live Adjustments, Timers/Warnings, Transition Support, an outcomes summary, Practice History), an Early Access section, closing CTA, a 7-question FAQ accordion, and a footer. New `.lp-*` CSS scoped inside the component (doesn't touch `App.jsx`'s global CSS) adds the one real breakpoint (860px) this app has ever had — alternating rows go `row`/`row-reverse` above it, single-column copy-first below it. **Still no real product screenshots** (per addendum §1, no stylized icon mockups either) — instead every section's visual is built from the actual app's own CSS classes (`.li`, `.pill`, `.bdg`, `.cc-timer`, `.cc-focus`, `.cc-trans-card`, etc.) populated with realistic sample data, so they're pixel-identical to the real components, just not live screenshots. Swap in real screenshots whenever Jax has them — the slots are visual props on each `Section` call, one-line swaps. Verified via build + DOM/computed-style inspection in the dev-server preview (alternating flexDirection, mobile column-stacking, nav-link hiding, FAQ accordion all confirmed programmatically); the preview tool's own screenshot capture hit the same known scroll-position blank-frame glitch noted in the third session's log, so mid-page visuals were confirmed via `getBoundingClientRect`/`getComputedStyle` rather than trusted screenshots — treated as a tooling artifact, not a page bug, consistent with that prior precedent.

**Library — drill skill tags** (2026-07-11, fourth session)
- Drills already supported multiple skill tags per drill at the data-model and UI level — `drill_tags` is a proper many-to-many join table (`activity_library_id`, `skill_tag_id`), mirroring `player_focus_areas`, and `ModalLayer.jsx`'s "Skill Tags" section was already a multi-select toggle grid, not a single-select. What Jax hit was a **silent persistence bug**: `syncDrillTags`/`syncDrillEquipment` in `supabase.js` never checked `.error` on their insert/delete calls, so any rejected write (e.g. an RLS check failing) looked identical to a successful save — the modal closed, `refreshLibrary()` ran, and the drill's tags silently reverted to whatever they'd been before. Direct SQL against production confirmed `drill_tags` had **zero rows across the entire database** despite drills existing, and that a matching insert (same owner on both the drill and the tag, which is the only case that exists in this single-coach production data) succeeds cleanly under RLS when run directly — so the failure was purely the app never surfacing whatever error actually occurred, not a fundamentally broken permission model. Fixed by having both sync functions return `{error}` on failure, propagating that through `createDrill`/`updateDrill`, and having `ModalLayer.save()` show "Something went wrong saving. Try again." and keep the modal open instead of closing it, for the `addActivity`/`editActivity` paths specifically (other entity types in the same `save()` function still don't check errors — out of scope for this fix, same latent gap, not touched). **Not verified end-to-end against a real signed-in session** — creating a disposable test login via the Admin API to click through the actual save flow was blocked by the environment's permission system (using the service-role key to fabricate a session was flagged as credential misuse, correctly). Verified instead via: full code trace of the UI/data-layer/RLS chain, and direct SQL (as `authenticated` with the real coach's own `auth.uid()` claim, inside a rolled-back transaction) proving the insert both the RLS policy and the exact row shape are correct. **Ask Jax to try adding/removing skill tags on a drill after this deploys** — if it still fails, the new error message plus the browser console's logged Postgres error will pinpoint the actual cause immediately, instead of failing silently like before.

## Known gaps / deferred (do not build without checking with the user first)

- **Hard account/data deletion** (GDPR-style erase) — not built. Soft deactivation only. There's an unresolved multi-path FK cascade-ordering issue blocking this; see memory `rop_actor_deletion_fk_gotcha` (now also covering `practice_live_sessions.practice_id` and `preview_sessions.practice_id`, found 2026-07-10).
- **Week view, drag-to-reschedule, games/events, series-level default templates, moving a plan between practices, availability polling, parent/player-facing anything** — explicitly out of scope per the scheduling addendum's positioning boundary (this is a coach tool, not TeamSnap).
- **Notifications/reminders** — needs PWA push, not started (the 2026-07-10 invite-notification email is a one-off transactional send via a new Edge Function, not a general notifications system).
- **Org-tier staff provisioning / accept-decline flows** — the welcome-card+leave pattern (2026-07-10) is deliberately informal for personal teams; a real accept/decline flow is deferred to when orgs let strangers add strangers (Google Calendar's auto-add spam history is the cautionary tale for that scale, not this one).
- No automated test suite — every feature has been verified empirically in a live browser against the real Supabase project with disposable throwaway accounts, not via unit/integration tests.
- 11 disposable test accounts from **prior** sessions (2026-07-06/07, `rop-stage4/5/sharing-*@example.com`) are still sitting in `auth.users` with no associated data — found while cleaning up this session's own test accounts, not cleaned up yet (didn't want to bulk-delete auth users without checking first). Harmless but worth a cleanup pass.
- **Not yet click-tested**: the signed-in root path (`/` → straight into the app for an authenticated session) and the actual installed-PWA-icon launch, for the 2026-07-10 landing-page routing change. Verified thoroughly by code inspection (the `!session` branch is the only place `wantsAuth` is read; a truthy session falls through to the existing app render unconditionally, so nothing about the addendum's change can affect that path) and by exercising the signed-out landing/auth/terms/privacy flows live in a browser, but this session had no access to a real inbox or the project's service_role key to complete an actual OTP sign-in headlessly. Do a real sign-in (and, if possible, an installed-icon launch) before calling this addendum fully verified.

## Conventions worth knowing before you touch this codebase

- **No client-side inserts loop for bulk operations** — anything that creates many rows (e.g. a recurring series) goes through a single atomic RPC with server-side caps, never a client `for` loop of inserts.
- **Archive, don't delete** — almost every table uses `archived_at`, not hard deletes. Matches the whole app's "your data is safe" posture.
- **RLS on everything** — new tables get policies following the existing `can_access_team`/`can_manage_team`/`can_access_practice` helper-function pattern (`supabase/migrations/20260704000800_rls_functions.sql` and friends), not ad hoc per-table logic.
- **Actor-identity columns** (`created_by`, `marked_by`, `noted_by`) are stamped via `WITH CHECK (col = auth.uid())` on insert, following the pattern in `20260704000900_rls_policies.sql`.
- **`.upsert()` needs UPDATE grant** even for `ON CONFLICT DO UPDATE` — if a table's RLS is deliberately insert/delete-only (no update policy), use `{onConflict, ignoreDuplicates: true}` instead, which only needs INSERT.
- **Migrations are dry-run before applying** — wrap in `BEGIN; ...; ROLLBACK;` via the Management API first, confirm no errors, then apply for real. See any recent migration-adding session in git history for the exact curl pattern.
- **`npx supabase db push` also works and is simpler than raw Management API curl calls** — the CLI is already logged in and linked to this project (`npx supabase projects list` proves it, no separate access token needed). **Gotcha (2026-07-10, second session):** migrations applied via the raw Management API (or any path other than `db push`) don't get recorded in the CLI's own remote migration-history table, so a later `db push --dry-run` will think *all* of them are still pending and try to re-run already-applied SQL. Fix: `npx supabase migration repair --status applied <version> <version> ...` for each already-applied-but-unrecorded version first (confirm with `npx supabase migration list`, which shows `remote:""` for anything unrecorded), *then* `db push` for the actually-new one. To read query results back out (e.g. to confirm grants/policies after applying), `npx supabase db query --linked -f file.sql` works without any extra credentials.
- **Testing pattern**: create a disposable Supabase user via the Admin API (`generate_link` returns an `email_otp`/`hashed_token` without sending real email), sign in via `verifyEmailOtp` in the browser console, exercise the feature, verify via direct SQL against the project, then delete the disposable user/data. Never test destructive flows against the real coach's account. **Gotcha (2026-07-10):** calling `generate_link` and the app's own "Send Code" button share the same per-email Supabase Auth rate-limit bucket — doing both back-to-back trips it. Do one rate-limited action at a time: click Send Code first with no prior `generate_link` call, then call `generate_link` once afterward just to read the resulting code.
- **Timezone**: practice times must always be computed/displayed in the *team's* `timezone` column, not the browser's. Use `scheduledAtToTeamLocal`/`teamLocalToScheduledAt` in `supabase.js`, not raw `Date` methods.
- **`service_role` needs explicit table GRANTs too** — it bypasses RLS but NOT the base table grants; nothing before 2026-07-10 had ever queried app tables with the service_role key (everything else goes through `authenticated`/`anon` RLS, or the Postgres superuser via the Management API for migrations). Any new Edge Function that queries app tables needs `grant select/insert/... on public.<table> to service_role` — won't be caught by RLS testing, only by actually running the function.
- **Secrets never go in migration files** — migrations are committed to git. Vault (`vault.create_secret`, already installed on this project) or Edge Function secrets (`POST /v1/projects/{ref}/secrets`) hold the actual values; migrations only reference them by name. `pg_net`-triggered Edge Functions need `verify_jwt: false` at deploy time (no user JWT on a DB-trigger-originated call) and must gate themselves with a shared-secret header instead.

## Recent session log (most recent first)

- **2026-07-21 (tenth session, continued — org bugs + Coach/Org mode)**: Jax tested the org-model build (below) live and hit two real bugs, then asked for a proper Coach-vs-Organization split. **Bug 1**: creating a team from Org Home landed on a Schedule screen with no team-workspace tab bar, and the team was then invisible from Teams and Org Home too. Root cause: `org_create_team` gave the new team `owner_user_id = null` (correct, it's org-owned) but the existing `handle_new_team_head_coach` trigger only auto-adds a `team_staff` row `when new.owner_user_id is not null` -- never extended to the org-owned case. Every client-side "can manage this team" check (`isHeadCoach`/`myTeamRole` in `constants.js`) reads `team_staff` rows only, so the director who'd just created the team had zero recognized role on it and every management control vanished, including the tab bar itself (`Layout.jsx` only renders it when the team resolves via `data.teams`, which was fine -- the real gap was upstream). Fixed in `org_create_team` (migration `20260721050000_org_create_team_head_coach.sql`, applied to production) to insert the director's own head-coach row in the same statement as team creation, plus a backfill for the one team this had already happened to (confirmed via `org_staff` that exactly one director existed for that team's org, so the backfill target was unambiguous). Separately, Org Home's "+Team" form only asked for a name (hardcoded `sport: 'General'`, no color) -- consolidated it to open the same full `addTeam` modal personal teams use (name/sport/color), branching `ModalLayer.save()` on `payload.organizationId` to call `orgCreateTeam` instead of `createTeam`. **Bug 2**: "Invite Coach" on Org Home didn't send anything -- confirmed correct, it only ever inserted a database row (unlike `add_team_staff`, which triggers a real Resend email via `notify-team-staff-added`). Jax questioned whether it was needed at all, since coaches can already be added per-team; clarified the two aren't equivalent (team `+Coach` = scoped to one team's roster; org invite = makes someone a co-**director** of the whole org, v1's only org role) and Jax decided to keep it, relabeled honestly as "Add Org Member" with copy that says what it actually grants, still no email (unchanged; flagged, not fixed, same gap as before).
- **Coach/Org mode** (Jax's design, refined via clarifying questions on multi-org handling, where invites/rollup should live, and whether team-workspace screens change per mode -- confirmed no): a persisted per-device toggle (`localStorage`, `mode = {type:'coach'}` or `{type:'org',orgId}`, state lifted to `App.jsx`, exposed via `AppCtx`) that changes what the three global tabs (Home/Teams/Library) show, without touching any team-workspace screen. Coach mode = teams/practices this person personally coaches, across any org (`teamsForMode()`, new in `constants.js`, filters by `myTeamRole`). Org mode = every team in the org being viewed, regardless of personal involvement -- the whole point of the split -- plus a new `canManageTeamInMode()` helper so a director can plan/manage any team in their org from Home even without a personal `team_staff` row there (matches what RLS already allowed server-side via `is_org_admin`; this just taught the client-side gate the same thing). Switching to Org mode with more than one org (`data.myOrgs`) shows a picker; with exactly one, jumps straight in. **Standalone Org Home page retired**: its content (weekly-practices rollup, Add Org Member + pending-sent-invites) folded directly into `HomeScreen.jsx` under `isOrgMode`, per Jax's call that these belong on Home, not a separate screen. `/org/:orgId` route, `OrgHomeRoute`, and `OrgHomeScreen.jsx` all deleted; Settings' org rows and "Create Organization" now switch mode + navigate home instead of routing to the deleted page. `Layout.jsx`'s tab bar gets a solid-green `.tabbar.org` style in Org mode (new CSS rule, same three tabs) plus the org's name shown where the coach's name normally sits, since color alone isn't a reliable enough cue on its own. **Library 5-tab redesign** (Jax's call, consciously reversing a prior session's decision -- see below): `LocationsSection`, `EquipmentTab`, `visibleEquipment`, and `GearEditRow` moved out of `SettingsScreen.jsx` into `NewLibraryScreen.jsx` (alongside `SkillsTab`, which was already there) to avoid a circular import (`SettingsScreen` already imports `SkillsTab` from `NewLibraryScreen`; the reverse direction would have cycled). Library's "My/Org Library" section now has a 5-tab sub-nav -- Drills (default), Templates, Locations, Equipment, Skill Tags -- replacing the old 2-tab Drills/Templates toggle; Settings lost those three NAV_ITEMS entirely (Account is what's left, plus founder-admin and org-creation). Drills and Templates now actually filter by mode (`organizationId` in Org mode, `ownerUserId` in Coach mode -- Templates previously showed every RLS-visible template unfiltered, which happened to work only because org templates didn't really exist yet). **Locations/Equipment/Skill Tags now also split mine-vs-org**, added right before commit per Jax's ask: three new supabase.js functions (`createOrgLocation`, `createOrgAsset`, `createOrgSkillTag`) -- no new RPCs needed, confirmed via `pg_policies`/`pg_constraint` that `locations_insert_manage`/`assets_insert_manage`/`skill_tags_insert_scoped` already permit a director's direct insert with `organization_id` set (same coach-or-org pattern as drills/templates), and `skill_tag_scope_matches_owner` requires `owner_user_id` null when `scope='org'`. `LocationsSection`/`EquipmentTab`/`visibleEquipment`/`SkillsTab` all now filter by mode the same way Drills/Templates already did, and each "+Add" branches to the org-owned create function in Org mode. One naming fix along the way: `EquipmentTab`'s existing `mode` prop (an unused, never-actually-passed "team"/"player" toggle override) was renamed to `forceType` to free up `mode` for the real Coach/Org value. Skill tags additionally always show `scope='global'` (curated) tags regardless of mode, on top of the coach-or-org set. Team-owned equipment (via `assets.team_id`) stays excluded from this screen either way -- it already has its own per-team Equipment page. Builder's team picker (`goToBuilder(null)` from Home) still isn't mode-scoped -- shows every team the user can access regardless of Coach/Org mode, same as it always has; not broken, just not perfectly scoped, flagged rather than expanded into given the size of this pass already. **Verification**: build compiles cleanly and the signed-out landing page renders with no console errors after every change (checked repeatedly through the restructuring, given its size); the `org_create_team` migration was dry-run (`BEGIN;...;ROLLBACK;`) before applying for real, and the backfill's target row was confirmed correct via direct SQL before and after. **Not verified**: any of this against a real signed-in session (same credentials limitation as the rest of this project) -- ask Jax to sign in and click through Home's Coach/Organization toggle (including the org picker if a second org ever exists), creating a team from Org mode and confirming the tab bar/Roster/+Coach all work immediately with no reload needed, Add Org Member, and each of the Library screen's 5 tabs in both modes -- specifically, add a location/equipment item/skill tag while in Org mode and confirm it shows up under the org (not personally), and confirm it disappears from the list when switching back to Coach mode.
- **2026-07-21 (tenth session)**: Jax handed over `ROP-Org-Experience-Handoff.md` (design-finalized org model spec) after a prior same-day session produced `DATABASE-ARCHITECTURE.md`, a live-DB-introspected snapshot for exactly this handoff. Two architecture questions were raised before starting (via clarifying questions, since both were irreversible-ish/security-relevant): (1) the handoff's new `org_staff` (director-only) table didn't address the existing `organization_members` (owner/admin/coach/viewer), which already backed `is_org_member`/`is_org_admin` across teams/library/assets/locations/skill_tags/templates and had zero real rows — Jax chose to replace it outright; (2) the handoff's `activity_library.share_scope` column would have duplicated the existing `shared_with_organization_id`'s job — Jax flagged that a coach can need to share into *more than one* org (director of Org A, coaching a team in Org B), which neither a single column nor a boolean flag can hold, so the design became a proper many-to-many join table instead. **Implemented, in order**: (1) `org_staff` replaces `organization_members` wholesale — `is_org_member`/`is_org_admin` redefined in place (`CREATE OR REPLACE`, same signatures, every downstream call site picked it up free), no direct write policy at all (RPC-only), the 4 stale test orgs (leftover from the 2026-07-07 sharing-model session, all `created_by is null`) archived rather than deleted. (2) Org-scoped `org_create_team`/`org_assign_team_staff`/`org_assign_player` RPCs — explicit auditable entry points; the underlying RLS was already permissive enough for an org admin to write directly (not narrowed, since that wasn't asked for). (3) Multi-org drill sharing: new `activity_library_org_shares` join table + `set_drill_org_shares` batch RPC (all-or-nothing on both drill-ownership and target-org-relationship) replaces `activity_library.shared_with_organization_id` (column dropped, confirmed zero rows used it first) — `templates.shared_with_organization_id` deliberately left alone, same limitation, out of scope. Also added `promote_drill_to_org_library` (director-only fork, full-copy not reference, matching the existing `practice_activities`-copies-`activity_library` convention; equipment/tags only carry over where still link-compatible with an org-owned drill). (4) Equipment picker needed no schema change — `assets.team_id` already existed from an earlier, undocumented fix (see `DATABASE-ARCHITECTURE.md` finding 5 correction); only added "own equipment sorts first" to the drill editor's picker. (5) `org_invites` + `accept_org_invite`/`decline_org_invite`, deliberately not the `team_staff` auto-link pattern — joining an org needs real consent, so nothing is granted until accept runs, and email match is server-side against `auth.jwt()`, never client-asserted. (6) `get_org_weekly_practice_rollup` RPC (much smaller sibling of `get_founder_metrics_summary`, gated by `is_org_member` not `is_admin`) + a new `OrgHomeScreen.jsx` routed at `/org/:orgId` (reached from Settings, mirrors the Founder Metrics entry point), plus a pending-org-invite accept/decline card on Home next to the existing team-welcome card. **Verification**: every migration was dry-run (`BEGIN;...;ROLLBACK;`) before applying for real via `supabase db query` (`migration repair` to reconcile the CLI's history table was blocked by this session's permission classifier — same drift as 2026-07-18/19, needs reconciling before the next `db push`). Security boundaries were then verified with a 9-case test suite, all passing, run inside a single rolled-back transaction against production using real existing profile ids with `SET LOCAL ROLE authenticated; SET LOCAL request.jwt.claims = ...` to simulate each caller — no fabricated auth session, no service-role key, nothing persisted (confirmed via a follow-up query after rollback): stranger blocked from org RPCs / director allowed / a private drill invisible to a non-org stranger / an org-shared drill visible to org members / batch share all-or-nothing and atomic (no partial apply) / org-library fork independently editable in both directions / invite-accept email mismatch rejected, matching email succeeds and grants `org_staff` / per-org scoping doesn't bleed across two orgs for the same director. **Not verified**: an actual signed-in click-through of the invite/create-team/Org-Home UI — same credentials limitation as every prior session (no real coach login, no service-role key to fabricate a disposable test account). Confirmed only that the build compiles cleanly and the signed-out landing page renders with no console errors after every client-side change. Ask Jax to sign in and click through: Settings → an org row → Org Home (invite a coach, create a team, check the weekly-practices chart), the Home pending-invite accept/decline card from a second account, and sharing a drill to more than one org from the Library screen. Full as-built detail in `DATABASE-ARCHITECTURE.md` §7.
- **2026-07-15 (ninth session, nav feedback)**: Jax tested the full handoff live and said the navigation felt weird overall; asked me to ask clarifying questions rather than guess. Two concrete answers came back: (1) teams should be reachable via a persistent bottom-bar option, not only from Home; (2) the pill/chip row on Home for picking a team read as a filter control (tap to toggle something in place) rather than navigation (tap to leave the page), which felt unnatural given it actually navigated away. Confirmed the Schedule-vs-Team's-Practices-sub-tab overlap was NOT the issue (ruled out via direct question). Fixed both: added a third global tab, **Teams** (`Layout.jsx`'s `GLOBAL_TABS`, new `Ic.Teams` icon, new route `/teams` → new `src/components/TeamsListScreen.jsx`, a plain `.li`-row list matching the house style for navigable lists elsewhere, not chips), reachable from anywhere including Library without detouring through Home. Removed Home's pill row entirely rather than reskin it in place -- with a dedicated Teams tab now covering "pick a team," a second team-picker on Home would have been redundant with what the hero/recap cards already surface contextually (team name/color per practice). Also removed the now-dead `goToTeam` prop thread through `HomeScreen`/`HomeRoute` (only `goToTeamGoals`, used by the recap card, is still needed there). **Verified**: clean `npx vite build`, and confirmed via `document.getElementById('root').innerHTML.length` directly (not just page text) that the app renders after a full dev-server restart, ruling out a stale-HMR explanation for two transient `ReferenceError: <Route> is not defined` console entries that turned out to be console-log-history artifacts from the tool used to read them (a fresh server process cannot carry over HMR state, and production is a single pre-bundled file with no incremental HMR at all, so this class of transient error is dev-only regardless). **Not verified**: the actual Teams tab, its list, and whether the nav now reads more naturally to Jax -- same credentials limitation as everything else this session. Ask Jax to confirm the Teams tab shows up, lists real teams, navigates correctly, and that Home no longer feels like it has a stray filter control.
- **2026-07-15 (ninth session, nav restructure round 2: 3-tab workspace + back-button audit)**: Jax clicked through the whole app and flagged the Team tab specifically: its Practices sub-tab is pure duplication now that Schedule covers forward/backward-looking practices, Roster felt nested too deep, and Team Equipment being in global Settings would show baseball gear while coaching a basketball team -- "weird." Asked clarifying questions before touching anything (per Jax's explicit request) rather than guessing; answers locked in a bigger reshape than the label suggests. **Team workspace is now 3 tabs, not 4**: Schedule / Plan / Team. Goals + Insights folded into Plan as a sub-toggle (Build / Goals & Insights) -- Jax's own reasoning: both are "how this team spends its practice time," and Plan's original scope (team-scoped drill/template access, from the handoff spec) had never actually been built past its placeholder, so nothing real was lost merging it. **New `Build` sub-tab** (inside `PlanScreen.jsx`, replacing the placeholder for real): the team's default template (`templates.default_team_id`) promoted to a callout with "Start from Template," other templates matching the team's sport, and a same-sport drill list -- the team-scoped build access the handoff always intended here. **`GoalsScreen.jsx`** stripped of its own page header/title (no longer a standalone tab) and is now embedded content inside Plan's "Goals & Insights" sub-tab; Home's recap card links via `?tab=goals` (`useSearchParams` in `PlanScreen`) so it still lands directly on Goals instead of Build. **Skill Tags stay in Settings** (Jax agreed) -- they're coach-wide vocabulary with no team ownership in the schema, so editing from inside one team's context could confusingly ripple to every other team silently. **Locations stay in Settings** too, for the same reasoning (a location isn't owned by one team) -- Jax's "people, places, and things" phrase turned out to only cover people+things in the actual ask. **Team Equipment + Player Gear move into the Team tab** (its own sub-toggle alongside Roster), and get sport-aware for the first time: `EquipmentTab` (`SettingsScreen.jsx`, now exported for reuse) takes an optional `sportFilter` -- team-equipment creation auto-stamps the current team's sport instead of the old hardcoded `'General'` (no picker needed, context already answers it), and both team equipment and player-gear lists filter to `sport === team.sport || sport === 'General'` when shown inside a team. Jax explicitly scoped this to sport-filtering only for now, not full team-owned equipment in the data model -- confirmed as its own deferred project once org equipment is real, not silently expanded. **Back-button audit** (Jax asked for this explicitly), three real fixes: (1) Layout.jsx's persistent team-context header was "‹ My Week" → Home; changed to "‹ Teams" → `/teams`, matching the canonical "leave a team" destination used everywhere else. (2) `ManageScreen`'s own internal "Teams" button was exact duplicate chrome once Layout's header already provides that link on every team-workspace screen -- deleted. (3) Builder's "Back" button was a hardcoded `navigate("/")`, always dropping you on Home regardless of whether you'd come from a team's Plan tab, Schedule, or Library -- changed to `navigate(-1)` so it returns to wherever you actually came from; the `useBlocker` unsaved-changes guard still intercepts this exact navigation correctly. (4) Found one back button that never existed: Settings' top-level list had no way out at all beyond noticing the tab bar still worked underneath it (it's reached via a gear icon, not a tab, so it needs its own explicit control) -- added. Also removed `ManageScreen`'s now-fully-unused practices/history state and the `fetchPracticeRunStatus`/`fetchNoteCountsForPractices`/`archivePractice`/`PracticeDetail` imports that only that deleted code used. **Verified**: clean `npx vite build` (95 modules), root renders non-empty after a fresh reload, and `/team/fake-id/plan` and the gear-icon Settings route both still correctly fall through to the landing page when signed out. **Not verified, same limitation as this entire session**: the actual Plan tab's Build/Goals toggle, the new Team tab's Roster/Equipment toggle and its sport-filtering, `presetTeamId` correctly seeding Builder's team picker when starting from a team's Plan tab, and whether any of the three back-button fixes feel right in practice. Ask Jax to: build a practice from inside a team's Plan → Build tab and confirm it defaults to that team (not team #1); add a piece of team equipment from two teams of different sports and confirm each only sees its own sport's gear; and click Back from Builder after entering it from three different starting points (Home, a team's Plan, Schedule) to confirm it returns to each correctly.
- **2026-07-15 (ninth session, IA restructure: Settings + Library split)**: Jax asked for a real product-thinking pass on the orphaned Manage items (My Locations / Team Equipment / Player Gear / Account Settings) and on Library's future shape given the browse-others'-drills vision. Framing that drove the design: the app had never separated *coaching content* (stuff you browse/build with) from *setup* (stuff you configure once) -- the old Manage screen was a junk drawer holding both. Jax picked, via explicit options: gear-icon-on-Home entry (not a 4th tab), "Explore" as the shelf name, and Skill Tags moving to Settings. **Library** (`NewLibraryScreen.jsx`): top-level toggle is now My Library / Explore. My Library holds Drills + Templates (sub-toggle); Explore holds the org shelves that already existed as chips inside the drills tab ("<org> Library" / "From <org>") -- a promotion of existing structure, not new code -- with an empty state that names the coming curated catalogs, so chunk-6 content lands in an established mental slot. Skills tab removed (SkillsTab now exported for Settings; the drill editor's inline Add/Edit Skill Tags flow still covers the frequent case). Also deleted `window.__ropLibTab`, a global that was set on every mount but never read anywhere (dead since some earlier refactor). **New `src/components/SettingsScreen.jsx`** at `/settings` (gear icon beside Home's "?" button): Account (moved verbatim from App.jsx's AccountSettingsScreen), My Locations (extracted from ManageScreen's list-mode JSX), Equipment & Gear (EquipmentTab moved from App.jsx, used in its combined team/player-toggle mode -- one entry instead of the old two), and Skill Tags (imported from NewLibraryScreen, one-way import, no cycle -- learned that lesson from the Ic crash earlier this session). **ManageScreen** (App.jsx) is now purely the team workspace's Team tab: its entire list mode (NAV_ITEMS, five manageTab branches, BackRow) is deleted, its internal "Teams" back button navigates to `/teams` instead of dropping into the now-nonexistent list, and the deleted-team fallback navigates there too instead of rendering blank. **Critical catch during planning, not after**: the old Manage "My Teams" list carried the app's only reachable "+ Team" button (RostersTab's copy is gated behind `!fixedTeamId`, unreachable since the route always passes it) -- deleting the list without rehoming the button would have silently removed team creation from the app entirely. It now lives on the Teams tab's header (`TeamsListScreen.jsx`). **Verified**: clean `npx vite build` (94 modules), root renders non-empty after a fresh dev-server start, `/settings` correctly falls through to the landing page when signed out, and every modal type SettingsScreen opens (addLocation/addSublocation/editLocation/editAsset/addTeam) confirmed present in ModalLayer. **Not verified, same credentials limitation as the whole session**: the signed-in Settings drill-downs, the Library My Library/Explore toggle against real drills/orgs, and the + Team flow from its new home. Ask Jax to click through: gear icon → all four Settings sections (especially Equipment & Gear's internal toggle and Skill Tags editing), Library's two shelves + Drills/Templates sub-toggle, Explore's empty state (he has no orgs, so he should see the coming-soon message), and creating a team from the Teams tab.
- **2026-07-15 (ninth session, steps 4-6, finishing the handoff)**: Jax confirmed the team-chip/stripe/My-Week fix worked live, then said to carry the `ROP-Goals-TeamNav-Handoff.md` implementation through to the finish. Completed the remaining steps in the handoff's own order. **Step 4 (§4.3-4.4, MyWeek + Schedule scoping)**: `HomeScreen.jsx`'s agenda window shrank from 14 to 7 days ("My week"); completed practices now leave the list entirely instead of just getting a "· Completed" badge (reused the same `!ran(p)` pattern `ScheduleScreen` already used, just applied to Home for the first time); added a "Last Practice" recap card per team (date, planned-duration proxy via `sumMins`, attendance via the existing planned-absences proxy, and a stubbed "See goals insights →" link into `/team/:id/goals` -- the handoff explicitly allows stubbing the real delta until the Goals tab exists, which it now does). `ScheduleScreen` gained a `fixedTeamId` prop that hides the now-redundant team-filter chip row. `fetchPracticesFull(teamId)` in `supabase.js` now accepts an optional team scope (filters the `practices` query itself, not just a client-side re-filter of the unbounded global fetch -- the join tables underneath are still fetched unscoped, flagged as an intentionally deferred gap per the handoff's own "not required for this milestone" language). `TeamScheduleRoute` does its own local scoped fetch, separate from `App`'s global `planning.practices` (Home still needs the cross-team unscoped list), with a combined refresh that keeps both in sync after a mutation. **Step 2 addendum**: added one more RPC, `get_team_session_history(team_id)`, not in the handoff's original §3 list (that only covered the aggregate report + the three editing RPCs) but reusing the same §3.1 attribution logic per-session instead of summed over the window, for the History list's date/wall-duration/attendance/top-3-skills/adjusted/excluded per session -- validated against real data before applying (reverse-chron order, top_skills correctly capped at 3, attendance matches a direct latest-status-per-player count). **Steps 5 + 6 (§5, Goals tab + History rebuild/editing) turned out to be one deliverable**: new `src/components/GoalsScreen.jsx` mounted at `/team/:teamId/goals` (replacing the placeholder) with: a goals editor (target % per skill tag, category-grouped add-tag picker filtered to the team's sport, sum-over-100 validation, the 1-12-week window setting writing `teams.goals_window_weeks`) gated on `isHeadCoach`; a bullet-chart-style glance view (target tick, planned/actual bars, delta chips only at >=3pts, untagged row, "other/transitions" per-practice average, a hint when untagged exceeds 25%); and a promoted, actuals-first History section (reverse-chron list from the new RPC, tapping into a detail view that is the *first frontend read path for `session_activity_log`'s real timing* -- planned-vs-actual per activity/station, per-skill rollup, notes, and manager-only editing: exclude-from-goals toggle, adjust an existing log row's start/end via `datetime-local` inputs, or log a row for an activity that was never captured live). **Deliberately did not touch the existing `HistoryViewer`** (in `CommandScreen.jsx`, used by `HomeScreen`/`ScheduleScreen`/`ManageScreen`) to add this -- those three call sites only have a `practice` object, no `session_id`, so retrofitting session-specific actual-timing display onto an already-widely-used component risked destabilizing three working screens for one new one; built `SessionHistoryDetail` as new, separate code instead, matching the same "isolate regressions" judgment call made throughout this whole implementation. **Found and fixed two real bugs during self-review** (no live-auth verification was possible for any of this, same limitation as before): (1) a genuinely invalid CSS `calc()` in the glance view's target-tick positioning -- multiplied a percentage by another percentage-derived value, which `calc()` doesn't allow (only percentage × unitless number), so the tick would have silently failed to position at all; fixed by using `target_pct/100` as a plain number multiplier. (2) A leftover call to the removed `markDirty` prop in `BuilderScreen`'s cleanup effect (dead code from the step-3 `useBlocker` rewrite that never got deleted) -- `markDirty` no longer exists anywhere in scope, so this would have thrown `ReferenceError: markDirty is not defined` the moment a coach navigated away from Builder, a real crash on an extremely common action. Deleted the whole now-unnecessary effect. **Verified**: clean `npx vite build` after every change, no-console-errors reload of the signed-out landing page as a smoke test, and every new RPC validated against real production data in rolled-back transactions before being applied for real (same discipline as steps 1-2). **Not verified, same limitation throughout this entire implementation**: nothing requiring a real sign-in -- the Goals editor, the glance view's numbers against a real report, the History list/detail, or any of the editing flows (exclude/adjust/add-row) have never been exercised in a live browser session. Ask Jax to: set a goal or two and confirm the sum-over-100 guard and the glance view's bars/tick line up sensibly; open a completed session from History and confirm planned-vs-actual shows real numbers (not just planned duplicated); try excluding a session and confirm the report's numbers actually shift; try adjusting a logged activity's time and confirm the "Adjusted" badge appears; and generally click through Home's new recap card → "See goals insights" link. **Known, deliberately deferred, not bugs**: the client-side "warn if attributed time exceeds session wall time" guardrail from §5.4 (the DB-side sane-bounds check built in step 2 is the real safety net; this would be additional UI polish); a bounded/paginated practices query (flagged since the original Snapshot); and the still-orphaned My Locations/Team Equipment/Player Gear/Account Settings routes flagged in the step-3 entry above, which nothing in steps 4-6 touched.
- **2026-07-15 (ninth session, live-testing fix)**: Jax signed in to test step 3 and reported the 4-tab team workspace bar showed, but the team-color stripe and "‹ My Week" link didn't. Root cause, found by grepping the whole codebase for anything that constructs a `/team/...` path: **nothing did** -- the routes and `Layout.jsx` worked correctly once *on* a `/team/:teamId/*` URL, but no button/link anywhere actually took a coach there. Jax must have reached one with an invalid/placeholder team id (typed or guessed), which explains the symptom exactly -- the tab bar only checks that a `teamId` param is present in the URL, while the stripe/link additionally require that id to resolve to a real team in `data.teams`, so a bad id shows the tabs but not the stripe/link. Fixed by adding the missing entry point: a new `goToTeam(teamId)` nav helper (`App.jsx`, alongside `goToBuilder`/`goToRun`/`goHome`) and a persistent "My Teams" chip row on `HomeScreen.jsx` (always visible whenever `data.teams.length > 0`, not just when there's an upcoming practice -- the old plan of relying solely on the hero card's team name would have left a coach with no upcoming practices with zero way in, same bug in a different guise). Verified clean build and a no-console-errors reload of the signed-out landing page; **could not verify the actual chip tap → tab bar → stripe/link chain live**, same credentials limitation as the rest of step 3 -- ask Jax to retry after this deploys and confirm the stripe and "My Week" link both appear now.
- **2026-07-15 (ninth session, continued)**: Step 3 (handoff §4.1-4.2, router shell) — replaced `App.jsx`'s `view` string state machine with real `react-router-dom` (v6.30, data router via `createBrowserRouter`/`RouterProvider`, needed for `useBlocker`). Routes now match the handoff's §4.1 list: `/`, `/library`, `/builder/:practiceId`, `/run/:practiceId` (handoff calls this `:sessionId`; kept it a practice id for now since that's still what `liveId` actually holds -- CommandScreen's internals weren't touched this step, so renaming the concept without renaming the state would just be misleading), `/team/:teamId` (redirects to `schedule`), `/team/:teamId/schedule|plan|goals|team`, plus `/live/:token`/`/preview/:token`/`/terms`/`/privacy` converted from manual `window.location.pathname` regex checks into real sibling routes (same before-auth-check ordering preserved). New `Layout.jsx` renders a context-sensitive tab bar (Home/Library outside a team; Schedule/Plan/Goals/Team inside `/team/:teamId/*`, active-state and team-color-stripe derived from `useParams()`/`useLocation()`) and the live-resume bar, both now rendering across every route instead of only four `view===` branches. Builder's unsaved-changes guard was rebuilt on `useBlocker` (catches in-app nav and browser back/forward) plus a `beforeunload` listener (catches hard refresh/tab-close, which the old `guardedSetView` mechanism never actually protected against either) -- replaces the old `guardedSetView`/`builderDirtyRef`/`priorView` mechanism entirely, per the handoff's explicit §4.2 instruction. Shared app state now flows through a React context (`AppCtx` in `App.jsx`) so the router can be created once (`useMemo`, empty deps -- recreating it every render would reset navigation state) while route-wrapper components still read live data. Mechanically swapped every `setView`/`setLiveId`/`setEditPracticeId` navigation call site (~18 across `HomeScreen.jsx`, `ScheduleScreen.jsx`, `PracticeDetail.jsx`, `NewLibraryScreen.jsx`, `CommandScreen.jsx`, `App.jsx`'s `ManageScreen`/`BuilderScreen`) for `goToBuilder`/`goToRun`/`goHome` -- confirmed via grep beforehand that every `setLiveId`/`setEditPracticeId` call was already immediately followed by a `setView` call in the same handler, so this was a safe 1:1 collapse, not a behavior change. `ManageScreen` gained an `initialTeamId` prop (same precedent as `RostersTab`'s existing `fixedTeamId`) so `/team/:teamId/team` drops straight into that team's workspace instead of the flat team-picker list. `vercel.json`'s rewrites collapsed to one catch-all (`/(.*) → /index.html`) so a refresh on any new route doesn't 404 (the existing `sw.js` needed no changes -- its navigation handler is already path-agnostic, network-first with a cached-shell fallback). **Found and fixed one real bug during verification**: a circular import (`Layout.jsx` importing `Ic` from `App.jsx`, which itself imports `Layout.jsx`) threw `ReferenceError: Cannot access 'Ic' before initialization` on every load, a total blank-page crash caught by nothing (not even `ErrorBoundary`, since it happened during module evaluation, before React ever mounted) -- fixed by extracting `Ic` into a standalone `src/icons.jsx` with no back-reference to `App.jsx`. **Verified**: clean `npx vite build`; live in the dev-server browser preview -- signed-out landing page renders with no console errors, `/terms`/`/privacy` render their full copy, `/live/:faketoken` correctly reaches `HelperView` and shows its real "session not found" state (proving the token param wiring works, not just the route match), and a deep authenticated path (`/team/some-fake-id/goals`) correctly falls through to the landing page when signed out, same as `/`. **Not verified**: any authenticated route (tab bar, team workspace, Builder's guard, Command's hidden-tab-bar behavior) -- this environment has no real coach credentials, and creating a disposable test login requires Jax's explicit go-ahead per this project's established testing convention (see prior sessions' notes on the harness blocking that without authorization). Ask Jax to sign in and click through: Home → Library tab bar, tapping into a team (`/team/:id/schedule` through `/team/:id/team`), the team-color header stripe, Builder's unsaved-changes guard on tab-tap/back-button/browser-back, and that Command's run screen still hides the tab bar. **Real gap found, not fixed (needs a decision, not a code fix)**: the handoff's own §4.1 route list has no home for My Locations / Team Equipment / Player Gear / Account Settings -- these currently live only in `ManageScreen`'s top-level list view, reachable today solely via its own internal "Teams" back-button from inside a team (which no longer matches the URL, since `/team/:teamId/team` now always seeds `initialTeamId` and skips straight past that list). Nothing was invented to paper over this; flagging for Jax to decide where these land (a `/settings` route? a global fifth tab? folded into "Team"?) before step 4. **Also flagged for later**: `/schedule` is a deliberate, temporary step-3-only bridge route (not in the handoff's list) so `goToSchedule()` call sites and the existing cross-team `ScheduleScreen` have somewhere to go until step 4 folds Schedule into `/team/:teamId/schedule` + Home's own agenda -- remove it then. No commits made yet this step; holding for Jax's review before committing/pushing, since this is a full nav rewrite that hasn't been exercised with a real signed-in session.
- **2026-07-15 (ninth session, in progress)**: Started implementing `ROP-Goals-TeamNav-Handoff.md` (team goals + team-first nav restructure), grounded in `ROP-Goals-Nav-Snapshot.md`. Following the handoff's own 6-step implementation order; steps 1-2 are applied to production, steps 3-6 (router shell, MyWeek extraction, Goals tab, History rebuild) are not started. **Step 1 (§2.1-2.7, schema)**: 7 migrations (`20260715000000` through `20260715060000`) — `teams.goals_window_weeks` (default 4), `team_goals` table (target % per skill tag per team, archive-only, RLS via existing `can_access_team`/`can_manage_team`), a `tag_visible_via_team_goal` SECURITY DEFINER function + RLS policy widening `skill_tags` SELECT so assistants can resolve a coach-scoped tag's name once it's referenced by their team's goals (D1), `practice_live_sessions.excluded_at`/`excluded_by` (soft-exclude test runs from actuals, D2), `session_activity_log.adjusted_by`/`adjusted_at` (post-hoc edit audit), a trigger (`close_open_session_activity_rows`) that closes any still-open `session_activity_log` row to the session's `ended_at` the moment `practice_live_sessions.status` flips to `'completed'` (D3, plus a one-time backfill for pre-existing dangles — zero rows needed backfilling in production, confirmed via query before and after), and a `'break'` activity type on both `practice_activities`/`template_activities` (D4). **Step 2 (§3.2-3.5, RPCs)**: `get_team_goal_report(team_id)` — the server-side planned-vs-actual-vs-target aggregation (D9: deliberately not an extension of the unbounded `fetchPracticesFull()`), plus `set_session_exclusion`, `adjust_session_activity`, `add_session_activity_row` for history editing. All four are `security definer` with the permission check as the first statement, following the existing `create_practice_series` pattern. **Verification method for this session**: no local/staging environment exists for this project (no Docker, no `psql`, only the CLI's stored login to the linked production project), so every migration and RPC was dry-run inside a `BEGIN;...ROLLBACK;` transaction against production first (confirming exact constraint names, existing RLS policy coverage, and query correctness against real rows) before actually applying via `supabase db push` — and the RPCs were further validated post-dry-run by impersonating a real coach's `auth.uid()` via `set_config('request.jwt.claim.sub', ...)` inside a second rolled-back transaction: confirmed the report's planned/actual percentages reconcile to ~100% against real data, confirmed a same-day practice with a completed session is correctly excluded from "planned", confirmed session exclusion/restore correctly shifts `completed_session_count`/`excluded_session_count` and reverses cleanly, confirmed `adjust_session_activity` stamps the audit columns, confirmed `add_session_activity_row` both succeeds for a valid target and rejects one belonging to a different practice, and confirmed all four RPCs reject an unauthenticated/unrelated caller with `not authorized`. **Known limitation not yet exercised**: station-block actual attribution (D6, wall-elapsed ÷ concurrent-station-count) has zero real rows to validate against — production has 0 `station_blocks`/`stations` rows today (no coach has run a station-block practice yet) — implemented per spec and covered by the dry-run's syntax/type-checking only, not by real data. Ask before relying on it until a real station-block session exists to check against. **Not yet done**: steps 3-6 (router migration, MyWeek/Schedule extraction, Goals tab UI, History rebuild+editing) — no frontend code has been touched this session.
- **2026-07-13 (eighth session)**: Jax's phone locks/backgrounds while coaching (understandably doesn't want to leave it unlocked in a pocket) and the live session's 2-minute-warning/time's-up audio cues went silent as a result — browsers suspend JS almost entirely for backgrounded/locked tabs, and the app's only audio mechanism (`window.speechSynthesis`) has no exemption from that. Laid out four options (silent-background-audio-loop + Wake Lock / Web Push / native app wrapper) and built the first two per Jax's go-ahead. **Before touching anything**, checked `git log --all -i --grep="beep|buzz|audio"` since Jax specifically warned a prior attempt at real beep/buzzer sound had failed and asked me to be careful not to lose the current (working) speechSynthesis fallback — found `dcbf238` ("fix: replace Web Audio buzzer with speechSynthesis - Safari blocks Web Audio output"), which had tried a raw `AudioContext` oscillator square-wave tone and abandoned it. This session's approach is a different code path, not a retry of the same one: real short audio *files* played through actual `<audio>` elements, the standard media-playback primitive Safari's background-audio exemption is actually built around (vs. a bare oscillator graph, which has a history of Safari-specific bugs). **New assets**: `public/audio/tennis-beep.wav` (double-beep, 2-min warning, per Jax's request), `public/audio/gym-buzzer.wav` (harsh low buzzer, time's-up cue, per Jax's request), `public/audio/silence-loop.wav` (near-silent, non-zero-amplitude, 3s loop) — all synthesized locally with a one-off Python script (sine/square wave synthesis via `wave`/`struct`), not sourced externally; verified they decode to real non-silent waveforms with `AudioContext.decodeAudioData` before wiring them in. **`CommandScreen.jsx`**: enabling audio now also starts the silent loop (`startBgAudioSession`, on both the header toggle and the ellipsis-menu "Enable Audio" item) — that's what earns the tab permission to keep running in the background so the real cues can still fire once the phone locks or a live session already fully re-derives its timer from `session.current_phase_started_at` on mount, per the seventh session's finding, so playing a background media stream doesn't change or risk that). Paired with the Screen Wake Lock API (`navigator.wakeLock`, feature-detected, re-requested on `visibilitychange` since the browser auto-releases it whenever the tab is hidden). **Deliberately additive, nothing existing removed**: the tennis-beep/gym-buzzer files play in their own try/catch blocks alongside the exact original `speechSynthesis` calls ("Two minutes remaining.", "Next up!", "Audio on") — same triggers, same wording, unchanged — so if a tone ever fails to fire on some device, the existing working speech cues still fire exactly as before. Added a `playWarningTone`/`warnToneRef` pair mirroring the file's existing `beep`/`beepRef` and `speak`/`speakRef` ref-mirroring pattern, specifically to avoid a stale-closure bug (the 2-minute-warning `useEffect` doesn't list `audioOn` in its dependency array). Scoped to the main `CommandScreen` (coach/controller) only — `HelperView` (the anonymous `/live/:token` page parent volunteers use on their own phones) has the same speechSynthesis-only pattern and would benefit from the same treatment, but wasn't touched since Jax's complaint was specifically about their own coaching phone; worth doing later if helpers report the same issue. **Verified**: clean `npx vite build`, dev-server console clean, all three audio files serve with correct content-type/size and decode to valid non-silent waveforms (checked peak amplitude and duration programmatically) via the browser preview. **Not verified**: actual behavior with a real phone's screen genuinely locked — that requires a real device and couldn't be tested from this environment; ask Jax to try a live session with the phone locked/backgrounded before trusting this fully. **Follow-up same session**: Jax confirmed the tone survives a locked screen, but heard both the new tone and the old spoken cue at each trigger (expected, since it was deliberately additive). Asked which to keep going forward; Jax chose tone + spoken-2-minute-warning, drop the spoken "Next up!" (redundant with the buzzer). Removed the `speechSynthesis` call from `beep()` — the buzzer now plays tone-only; the 2-minute warning still plays both the tennis-beep tone and "Two minutes remaining." spoken, unchanged.
- **2026-07-12 (seventh session)**: Jax asked for a navigation/nav-flow audit of the app (efficiency + intuitiveness), not a specific bug report. Traced the actual routing (`App.jsx`'s single `view` state, no router; deeper levels are local component state) and found five real gaps, all fixed: (1) **Builder had no unsaved-changes guard** — switching tabs mid-edit silently discarded local `acts`/`teamId`/`locId` state with zero warning, since Builder unmounts on tab switch and nothing was lifted to `App`. Added a dirty-snapshot ref (`savedSnapshotRef`) compared against current state, a `markDirty` callback threaded down to `App`, and a `window.confirm` gate in a new `guardedSetView` used by the tab bar; cleared on successful Save/Schedule/Run Now via `markSaved()`. (2) **Builder had no exit affordance at all** — added a "Back" button (`onCancel` prop, routes to `priorView`, itself guarded by the same dirty check). (3) **Tab bar went dark while in Builder** — added `priorView` tracking (last real tab before entering Builder) and an `activeTabId` computation so the originating tab stays highlighted. (4) **No way to leave a live session without ending it** — the tab bar was hidden during `command` view with only "End Practice" as an exit. Added "Leave (keeps running)" to `CommandScreen`'s ellipsis menu (just calls `setView("today")`, doesn't touch session state) plus a persistent green "Resume Live Practice" bar in `App.jsx` shown on other screens whenever `liveId` is set — safe because the live-run timer/stage was already fully derived from `session.current_phase_started_at` on mount (built for the existing multi-coach handoff/reconnect case), so unmounting/remounting `CommandScreen` was already designed to resume correctly. (5) **Tab iconography was confusing** — Library used a play-triangle (`Ic.Run`) and Manage used a near-duplicate of the Schedule tab's calendar icon (`Ic.Build`); swapped in `Ic.Lib`/`Ic.Admin`, two icons that already existed in the file but were never wired to any tab (dead code), and deleted the now-unused `Ic.Run`/`Ic.Build`. Also fixed a small wording inconsistency (Player Profile's exit button said "Done" everywhere else says "Back") and removed a dead tab-bar "live" dot that checked `id==="command"` against a `TABS` array that never contained a `"command"` entry. **Verified via full code trace (including reading `CommandScreen`'s mount-time session-resume logic before deciding the live-session fix was safe) and a clean `npx vite build`; not verified against a real signed-in session** — attempted the established disposable-test-account pattern (Admin API `generate_link` + browser-console `verifyOtp`), but the harness's permission layer blocked the browser-side OTP step as an unauthorized use of the service_role key for UI testing. Deleted the disposable test user and wiped the key/OTP from disk immediately, per Jax's direction to leave it at code-trace-level verification for now. Ask Jax to click through: switching tabs mid-Builder-edit (should confirm), Builder's new Back button, the tab bar during/after Builder, and leaving/resuming a live session via the ellipsis menu and the green resume bar. Frontend changes committed as `b1372f4` and pushed to `main`.
- **2026-07-11 (sixth session)**: Four requests from Jax, handled in order. **Timezone bug**: Jax (Phoenix, UTC-7) reported that at 8:46pm Saturday, Home showed 2 practices "tomorrow" that were actually Monday, and Month view highlighted Sunday as today. Root cause: every "today" computation across the app used `new Date().toISOString().slice(0,10)`, which converts to UTC before truncating — west of Greenwich this rolls the date over hours before local midnight. Added `localDateStr()` to `constants.js` (the same local-getter construction Month view's own grid already used correctly, just not for its "today" comparison) and swapped it in across `App.jsx`, `HomeScreen.jsx`, `ScheduleScreen.jsx`, `PracticeDetail.jsx`, `AbsencePicker.jsx`, and `NewLibraryScreen.jsx`. **Dot consistency**: Month view's per-day dots were already correctly hollow-when-unplanned/filled-when-planned; Agenda's per-practice team-color dots were always solid regardless of plan status. Made Agenda's dots (`ScheduleScreen.jsx`, `HomeScreen.jsx`) follow the same rule. **Skill tag picker redesign**: replaced the drill editor's always-expanded flat pill grid (`ModalLayer.jsx`) with a new `SkillTagPicker` component — closed state shows only the selected tags as removable chips, an "Add/Edit Skill Tags" button opens a searchable, category-grouped picker in a bottom-sheet overlay. Addresses the picker no longer scaling now that a coach has 56+ seeded tags. **Landing page rewrite**: replaced `LandingPage.jsx` entirely with a 14-section product-walkthrough marketing site per Jax's new spec — see the feature-inventory entry below for full detail. Verified the bug fixes and picker via full code trace, `npx vite build`, and DOM/computed-style inspection in the dev-server preview (flex-direction, class toggling, etc.) — **not verified against a real signed-in session**: exercising the actual Schedule/Home screens and the drill editor's tag picker live requires a signed-in coach account, and this session had neither a real inbox nor prior authorization to create a disposable test login, so ask Jax to click through Schedule (Agenda + Month), Home, and the drill editor's Skill Tags field after this deploys.
- **2026-07-11 (fifth session)**: Batch of bug reports from Jax, handled one at a time. **OTP login**: Jax reported the sign-in code fails on the first submit and works when resubmitted with the *same* code — that ruled out a wrong/stale-autofill code (same string, different outcome), pointing at a transient failure on the first `verifyOtp` call specifically. Jax explicitly authorized creating a disposable test login to investigate (the same Admin-API pattern blocked without authorization in the fourth session); reproduced sign-up and sign-in flows cleanly on the first try twice, so couldn't pin the exact backend cause, but added an automatic silent retry (one retry, ~800ms later, same code) plus a check against the current session before surfacing an error (in case the first attempt actually succeeded server-side and only the client response was lost) — this directly matches the reported symptom regardless of the underlying transient cause. Also added `autoComplete="one-time-code"`/`"email"` to the relevant fields (standard practice, harmless either way). **Getting-started checklist**: added a "Build out your library" step between "Add players" and "Set your practice schedule" (`HomeScreen.jsx`), derived from `activityLibrary.length > 0` like every other step. **Sort menu**: the player-roster sort dropdown (`App.jsx`) was positioned `right:0` relative to a narrow wrapper near the *left* side of the row, pushing the 160px-wide menu off the left edge of the screen — changed to `left:0` so it opens into the open space to its right instead. **Schedule duration field**: `SeriesWizard.jsx`'s duration input used `onChange={e => setDurationMinutes(+e.target.value || 60)}` — clearing the field to type a new number produced `+"" || 60` → snapped back to 60 on every keystroke, so a coach could never get past the leading digit of the default. Fixed by allowing the input to hold an empty string while typing and only defaulting to 60 on blur (or at submit time as a fallback). **Skill tags**: see the Library feature-inventory entry above — turned out to be a missing-content problem, not a missing-feature problem (multi-select already worked once real tags existed to select). Seeded real starter tags, added a Library "Skills" management tab, dropped the "(mine)" pill suffix. **iOS input zoom**: `.inp`/`.sel`/`.ta`'s shared `font-size` was 15px, one pixel under the threshold (16px) where iOS Safari auto-zooms the whole page on focus and doesn't reliably un-zoom on blur or orientation change — explains "field text gets bigger on some fields but not all and doesn't return to normal," since the one non-`.inp` field in the drill form (`DurStepper`'s duration readout) isn't a real input and never triggered it. Bumped to 16px app-wide. **Drill form field order**: swapped Coaching Points to come right after Description, before Player Grouping, per Jax's ask (`ModalLayer.jsx`). **Bonus bug found while testing all of the above**: team creation was completely broken for any coach with no last name on their profile (last name is optional per the name-collection screen) — the auto-head-coach trigger copied `profiles.last_name` straight into `team_staff.last_name`, which is `NOT NULL`, so the whole `teams` insert silently rolled back with no error surfaced anywhere in the UI. Fixed the trigger to `coalesce(..., '')`, matching the same nullable→`''` pattern already used everywhere else in this codebase (migration `20260711010000_fix_team_staff_last_name_null.sql`). Verified essentially everything above live with a disposable test account (Jax's explicit go-ahead): OTP sign-up and sign-in both succeeded first try, checklist shows the new step, sort menu opens fully on-screen, duration field clears and accepts a fresh "90" cleanly (confirmed via direct DOM/state inspection since the browser-automation tool's synthetic Backspace key events don't reliably reach native `<input type=number>` elements in this environment — not an app bug, a testing-tool quirk), skill tags multi-select across 3 different categories and survives a reopen of the drill editor, `.inp` computed font-size is 16px, and team creation now succeeds. Test account and its data fully cleaned up afterward (deleted via Admin API, cascade confirmed). Frontend changes and both new migrations are **not yet git-committed** (migrations are applied to production regardless).
- **2026-07-11 (fourth session)**: Investigated Jax's report that a skill tag added to a drill "doesn't persist" and a request for multi-tag support. Found multi-tag support already existed end-to-end (join table + multi-select UI), and the real bug was `syncDrillTags`/`syncDrillEquipment` in `supabase.js` never checking `.error` on their insert/delete calls — a rejected write closed the modal exactly like a successful one. Confirmed via direct SQL against production that `drill_tags` had zero rows total, and that the exact insert the app would attempt (matching real owner ids) succeeds cleanly under RLS in a rolled-back transaction, meaning the previous belief that RLS was blocking it doesn't hold for this coach's actual data — the bug is purely the missing error surfacing. Fixed by propagating `{error}` through `syncDrillTags`/`syncDrillEquipment` → `createDrill`/`updateDrill` → `ModalLayer.save()`, which now shows an inline error and keeps the modal open on failure instead of silently closing. Also fixed a stale claim in this file's own feature inventory (still described the landing page as having its embedded feedback form, which was removed earlier the same day) and noted `submitPublicFeedback`/the `feedback` table are now unused by the landing page (only the separate authenticated `submitFeedback` in-app path still calls anything similar). **Not verified against a real signed-in browser session** — attempting to create a disposable test login via the Admin API (the established convention for this codebase) was blocked by the environment's permission system as credential misuse, so this was verified via full code trace plus direct SQL only. Frontend changes landed in `3c29aec` (verified working live in the fifth session below, once the user explicitly authorized the disposable-test-login pattern for that session).
- **2026-07-10 (third session)**: Landing Page V2 rewrite per `ROP-Landing-V2-Addendum.md` (brand-voice copy, new page structure, sport/level-agnostic language) and legal-page finalization (Jax supplied attorney-cleared final `terms-of-use.md`/`privacy-policy.md`, replacing the `-draft` versions — draft banner and placeholder-disclaimer footers removed from `LegalPages.jsx`). Verified live in the dev-server browser preview: landing page renders top-to-bottom with correct copy (confirmed via DOM text/accessibility-tree inspection — the preview tool's screenshot capture had an unrelated glitch producing blank frames partway down the page on this run, but `get_page_text`/`read_page`/`getBoundingClientRect` all confirmed real content was present and positioned correctly, so treated as a tooling artifact, not a app bug), `/terms` and `/privacy` render the final copy with no draft notice, and the "Try it free" → `AuthScreen` → "Send Code" flow works end-to-end. **No real screenshots for the "what it does" section yet** — used the existing icon cards as an explicit placeholder (see feature inventory above). Frontend changes are **not yet git-committed**.
- **2026-07-10 (second session)**: Built the public landing-page & feedback addendum (`ROP-Landing-Page-Feedback-Addendum.md`), sequenced per its own §6: `feedback` table + RLS + `submit_public_feedback` anon RPC first (migration `20260710050000_public_feedback.sql`, applied to production), then adaptive `/` routing in `App.jsx` (signed-out → new `LandingPage`, signed-in → unchanged app, manifest untouched), then `LandingPage.jsx` (hero, founder framing, Founding-Coach early-access framing, 4-capability grid, anonymous feedback form), `LegalPages.jsx` (`/terms`, `/privacy`, hand-transcribed from Jax's drafts, draft-notice and manual-deletion disclosure preserved verbatim, routed via new `vercel.json` rewrites), and the in-app feedback UI (Home's "?" button is now a two-item menu instead of a second icon). Verified live in a browser: landing page renders, the anonymous feedback form round-trips through `submit_public_feedback` into the real `feedback` table (confirmed via direct SQL, then deleted the test row), `/terms` and `/privacy` render with the draft notice intact, and the "Try it free"/back-button flow into `AuthScreen` works. **Not verified**: the signed-in-session and installed-PWA-icon launch paths, since this session had no real inbox or service_role access to complete an actual OTP sign-in headlessly — confirmed correct by code inspection only (see Known Gaps). Also found and fixed a real pre-existing gap while adding the new routes: `vercel.json` only had rewrites for `/live` and `/preview`, so `/terms`/`/privacy` would have 404'd on Vercel without adding explicit rewrites for them. Frontend changes are **not yet git-committed**; the migration is applied to production regardless.
- **2026-07-10**: Built the full testing-round-1 addendum (`ROP-Testing-Round-1-Addendum.md`, 8 sections, sequenced per its own §8): auto-head-coach trigger + backfill, `add_team_staff` RPC + claim-at-signup linking, an invite-notification Edge Function (Resend, `pg_net`-triggered), welcome card + `leave_team`, role-aware UI for assistants/helpers across Home/Schedule/Teams/PracticeDetail, planning-depth indicators (partial/overplanned/complete pills), team color stripes, staff-add suggestions, practice-level sublocation removal, and a getting-started checklist. Verified empirically at every phase with disposable two-coach test accounts (per the established pattern), including several full end-to-end browser walkthroughs of real sign-in/role-gating/welcome-leave flows. Found and fixed three real bugs along the way, none anticipated by the addendum: (1) `service_role` had no table grants at all anywhere in the schema — the invite-notification function was the first thing to ever need it; (2) two more instances of the known missing-`ON DELETE`-cascade bug class, on `practice_live_sessions.practice_id` and `preview_sessions.practice_id` (found while cleaning up stale test data, not by design); (3) six leftover disposable test teams/accounts from prior sessions (2026-07-07) were still in production, never cleaned up as the testing convention requires — deleted with explicit confirmation. Frontend changes were later committed as `26aaaf1`; all migrations are applied and the Edge Function is deployed to production.
- **2026-07-09**: Practice Detail no longer offers "Run Now" on an unplanned practice (was leading to a blank screen after Attendance) — shows "Plan Practice" instead, "Run Now"/"Edit" only once a plan exists. Team color picker added to team *creation* (was edit-only); added black/dark-gray to the palette. Fixed Schedule tab's Agenda/Month toggle padding (was 0 top / 12px bottom, now uniform).
- **2026-07-09**: Built the full scheduling addendum — recurring series wizard + RPC, Home screen (replacing Today), Schedule tab (agenda/month), team colors, planned absences end-to-end into live-session attendance. Found and fixed two real bugs via empirical testing: an `upsert()` 403 (needed UPDATE grant it was never given), and a same-flow write race that silently discarded seeded absence data (attendance-snapshot submission was overwriting it moments later). Also fixed a real pre-existing gap: practice times were computed in the browser's timezone instead of the team's.
- **2026-07-09**: Auth UX fixes — OTP screen wrongly claimed "6-digit code" when the project sends 8; fixed a real duplicate-row bug (Save button had no working reentrancy guard — `useState` isn't synchronous enough for rapid clicks, needed `useRef`); fixed a blank-page bug after deleting a team (stale `selectedTeam` state).
- **2026-07-08**: Switched primary sign-in from magic link to Email OTP (magic links broke for the installed-PWA case), branded the auth email via Resend SMTP, added one-time name collection so the greeting shows a real name instead of the raw email.
- **2026-07-07**: Production cutover — merged `frontend-rewire` into `main`, pointed `runofpractice.com` at the new app, retired the old POC. Caught and fixed a real pre-cutover bug: Supabase `site_url`/redirect allow-list still pointed at localhost.
- **2026-07-07**: Post-launch feature round — helper attendance marking, account/team deactivation, structured focus areas, roster/upcoming-activities on the helper view, manual drill reordering. Found and fixed a real pre-existing RLS bug (`teams_select_access` ambiguous-column reference silently blocked non-owner coaches from ever seeing their team).
- **2026-07-06/07**: The full 7-stage rewire off the old POC data layer onto the current 35-table Supabase schema — auth, teams/roster, library (with org-sharing), templates/builder, live sessions, helper/preview anonymous pages, offline resilience/PWA polish.

---
*Maintained by Claude. Update this file at the end of any session that changes what's built, what's deferred, or how the codebase should be approached — don't let it go stale.*
