# Run of Practice — Build Status

**Last updated:** 2026-08-03 (twenty-ninth session — see Session Log for the full list of what happened that day)

**Status:** Live in production at [runofpractice.com](https://www.runofpractice.com), single real user (solo coach), actively iterating ahead of August testing. Twenty-sixth session shipped all five Goals & Insights enhancements from the coach's own spec doc in one pass: Skill-Development Trends, Next Practice Guidance, combined Builder Goal Guidance, the Practice Execution Scorecard, and Drill Performance Insights — all built on one new shared SQL attribution layer so every report uses identical category-minute math. Twenty-seventh session, same day, was direct feedback from actually testing that work live: 14 items, spanning a real default-permissions bug (library sharing was defaulting to shared, should default to private), a real z-index bug (a drill's ellipsis menu was visually clipped by the row below it), a new coach-accepted-invite notification, a Builder library switcher, drill-recommendation drill-downs in Next Practice Guidance, a drill-library sort picker, and several smaller UX/copy fixes. Twenty-eighth session shipped Development Pulse, a new dynamic single-CTA Home widget from the coach's own spec doc — reusing the twenty-sixth session's shared goal-attribution math rather than a "fifth variation," and finding/fixing two real bugs along the way (a spec-violating over-duration minute recommendation, and a navigation bug that silently loaded the wrong practice in Builder). Twenty-ninth session, same day, was a small 3-item feedback batch: fixed Goals & Insights' side margins, added a "Tag Untagged Drills" CTA linking Overview to a new filtered/tag-as-you-go Library view, and gave Development Pulse a collapsible caret with persisted state and a new-insight dot — plus one real, pre-existing rendering bug found and fixed along the way (the insufficient-history state's goal-mix bar was invisible due to a camelCase/snake_case prop mismatch). Continued same day with one more direct-feedback fix: Permissions' sharing/delegation toggles weren't visually reflecting a tap until the modal was closed and reopened, a stale-object-snapshot bug (see Gotchas). No automated test suite exists — every change is verified empirically in a live browser against production using disposable Supabase test accounts (see Working Conventions below).

## Standing instructions for Claude Code (read this section every session, before touching anything else)

**What this document is.** This is the single, permanent source of truth for the state of the Run of Practice codebase — not a one-off handoff note. It exists so a brand-new Claude Code session (or Jax, months from now) can pick up exactly where the last session left off without re-deriving context from scratch or re-learning a bug that's already been fixed once. Treat it as living documentation you are expected to both read from and write to, every session, not just a status report someone else maintains.

**Before doing anything else this session:**
1. Read this entire file top to bottom. It's long on purpose — skimming risks re-building something that already exists or re-hitting a bug that's already documented below.
2. Run `git log --oneline -20` and check for anything more recent than the "Last updated" line above — the file is only as current as the last session that bothered to update it.
3. Check the Known Gaps and Gotchas sections specifically before starting on anything that sounds like it might already be a known limitation or a previously-hit bug.

**How this file is organized, top to bottom:** what the app is and how it's built → what already works today (Feature Inventory) → the database/permissions architecture → Known Gaps (this is the closest thing to a roadmap — things deliberately not built yet) → Working Conventions (house rules for how code gets written and tested here) → Gotchas (bugs and tooling quirks not to re-learn) → Persistent Design Themes (the "why" behind decisions that evolved over several sessions) → the Session Log (a dated, chronological record of every session's work, newest entry first). Keep new content in the section it actually belongs to — don't let the Session Log become the only place new facts live, since that makes them easy to miss.

**How to log in and test as a real user.** This app has no local Supabase environment and no automated test suite — everything is verified by actually clicking through the live app in a browser against production, using a disposable throwaway account, never the real coach's account. To do this:
- Check for a local `.env` file in the repo root for a `SUPABASE_SERVICE_ROLE_KEY` (it's gitignored — never commit it, never put it in this file, never paste it into chat). If it's missing, ask Jax for it rather than guessing or working around its absence.
- Use the Supabase Admin API to mint a session for a throwaway account (`generate_link` + `verifyOtp`/`verifyEmailOtp` — see Working Conventions below for the exact pattern), then inject the resulting tokens into the browser's `localStorage` under the key `sb-<project-ref>-auth-token` to land signed in as that account.
- Click through the real app end to end (a local dev server against the same production Supabase backend is fine, or the deployed site directly) rather than stopping at "the code looks right" — this file's own Session Log has multiple entries where a change looked correct on review but failed live (a missing DB grant, an RLS gap, a stale cache), so live verification is not optional for anything touching data access or a real user flow.
- Delete the throwaway account and all its data via the Admin API when done. Never leave test data sitting in production, and never test a destructive flow (account deletion, hard-delete cascades, etc.) against the one real coach's account.

**What's expected of you before you end a session:**
1. **Append a new entry to the top of the Session Log** — newest first, following the voice and level of detail already established there: what was asked, what was actually built (call out anything that turned out to already exist, or that you deliberately chose not to build and why), any real bug found along the way as distinct from requested changes, and exactly how it was verified live.
2. **Update the sections above the log directly**, not just in the log entry — if you shipped a feature, add it to the Feature Inventory; if you closed a known gap, strike it through or remove it from Known Gaps; if you hit a new footgun worth remembering, add it to Gotchas; if you established a new house rule, add it to Working Conventions. The log is history — everything above it should reflect the *current* state of the app without anyone having to read the whole log to reconstruct it.
3. **Update the "Last updated" and "Status" lines at the very top of this file** to reflect the latest session.
4. Do not create new companion doc files for handoff notes, specs, or status summaries — this file replaced five of those already (see the footer note at the bottom of this document) specifically because parallel docs drift out of sync with each other and with the code. Extend this file instead.

A `BUILD-STATUS.original.md` sits alongside this file — the pre-reorganization version, kept only so Jax can diff against it if something looks off. No content was removed when this file was reorganized; sections were regrouped and the log was sorted into strict chronological order for scannability.

---

## What this is

A live-practice-orchestration app for youth sports coaches: build a practice plan (drills, stations, timing), run it live with a timer/rotation system, share a read-only view with parent helpers, track attendance, and plan on a recurring schedule. React/Vite frontend, Supabase (Postgres + Auth + Realtime) backend, deployed on Vercel.


## Stack & repo layout

- **Frontend:** React 18, Vite, no CSS framework (hand-rolled CSS-in-JS injected via a `<style>` tag, Barlow Condensed/DM Mono fonts). No component library. `react-router-dom` v6 (data router) for navigation.
- **Backend:** Supabase — Postgres with row-level security on every table, Auth (email OTP), Realtime for live-session sync.
- **Deploy:** Vercel, auto-deploys `main` to production.
- **Bundle splitting** (Phase 2 continued): `FounderMetricsScreen.jsx` (the only consumer of `recharts`, by far the heaviest single dependency at ~9MB unminified with its `d3-*` subpackages) is `React.lazy()`-loaded behind `FounderAdminRoute`'s existing `isAdmin` check, instead of a static top-level import — a non-admin's browser now never fetches that chunk at all (confirmed via network requests: zero requests for it on a non-admin visit to `/admin/metrics`, which still correctly redirects to `/`). `vite.config.js` also splits `react`/`react-dom`/`react-router-dom`/`@supabase/supabase-js` into a separate `vendor` chunk via `manualChunks`, so it can stay cached across deploys that only touch app code. Net effect on a normal user's first-load gzip: one ~377KB chunk before → ~121KB (vendor) + ~140KB (app) ≈ 261KB after, a ~31% cut, with the ~116KB recharts/d3 chunk now deferred entirely for everyone except the founder-admin. Next candidates if further cuts are wanted: lazy-loading Terms/Privacy/FAQ (low-traffic, self-contained), and possibly splitting `@dnd-kit` out of `ActivityConfigs.jsx` (used by Builder/Library, so a smaller win, deferred for now).
- `src/App.jsx` — still the largest file; holds the router, top-level `App()`, and several screens not yet split out (`TeamsScreen`, `BuilderScreen`, `PlayerProfile`, `RostersTab`, `AuthScreen`, `NameScreen`). Other major screens live under `src/components/`.
- `src/supabase.js` — the entire data-access layer. Every DB read/write goes through a named export here; nothing calls `supabase.from(...)` directly from a component.
- `src/constants.js` — pure helpers + shared constants (`uid()`, `TEAM_COLORS`, grouping math, `INIT` shape, `localDateStr()`).
- `src/Layout.jsx` — the authenticated app shell: global bottom tab bar, team-workspace top tab row, live-resume bar.
- `supabase/migrations/*.sql` — 147 migrations as of this session, each dated and mostly with inline comments explaining *why*, not just *what*. Applied via `npx supabase db push` (CLI is linked); some early ones were applied via the raw Management API and later reconciled with `migration repair`.
- `supabase/functions/*` — Edge Functions (Deno). First one added 2026-07-10 (`notify-team-staff-added`); a second (`notify-org-invite`) followed with the org model. A `_shared/emailTemplate.ts` holds the common Resend-email formatting both use. Secrets set via the Management API, never committed to migrations.
- `scripts/import-public-library.mjs` — one-time/rerunnable Node import script for the public drill catalog (see Library section below).

---


## Feature inventory (what actually works today)

### Auth & account
- Email OTP sign-in (code, not magic link — magic links broke for the installed-homescreen-PWA case). Branded email via Resend SMTP for both regular sign-in (`magic_link` template) and first-ever signup (`confirmation` template, added seventeenth session — Supabase routes a brand-new email's first `signInWithOtp()` through this template, not `magic_link`, and it had no custom template before, so new coaches got Supabase's stock unbranded/link-based default instead of the branded code).
- One-time name capture on first sign-in (covers pre-existing accounts too). First name required (asterisk), last name optional (no "(optional)" suffix, a small "* required" note instead) — reworded seventeenth session.
- Soft account deactivation ("Deactivate Account" section in Settings > Account, no longer labeled "Danger Zone") and team deletion. Reactivation is no longer silent: signing in on a deactivated account now shows a blocking prompt (reactivate, or exit back out) before the rest of the app renders, replacing the old auto-clear-on-sign-in behavior that surprised the one real user in testing.

### Navigation
- Global bottom tabs: **Home / Teams / Library**, plus a gear icon (not a tab) to `/settings`. This replaced an earlier chips-on-Home team picker and a Manage-tab junk drawer — see Session Log.
- Inside a team workspace (`/team/:teamId/*`), the tab bar switches to a flattened horizontal top row of 4 direct routes: **Schedule / Roster / Equipment / Goals & Insights**. There is no more nested Plan/Team toggle and no Build tab — a `/team/:teamId/build` route existed as dead code (superseded by Home/Schedule's own "+ Practice" entry points) until this session removed it.
- A persistent live-resume bar renders across every route (not just certain screens) whenever a live session is in progress and the coach has navigated away without ending it.
- **Active-tab underline** (seventeenth session) on the 3 global bottom tabs — color alone wasn't clear enough per direct feedback. While inside `/team/:teamId/*`, the Teams tab now shows as active (previously none of the three did, since that route doesn't literally start with `/teams`) — tapping it already correctly returned to `/teams` since `onClick` always navigates to the fixed path, so only the `active` check needed fixing. The global `/schedule` route ("My Schedule" from Home) had the same gap (left every tab dark) until the twenty-seventh session counted it as part of Home. Underline thickness increased 50% (2px→3px) the same session, both here and on the team-workspace top tab row, for better visibility on a PWA install.
- **Team workspace back button**: the team-color header strip had no persistent way back to the Teams list (a deliberate omission from an earlier nav restructure — "the bottom Teams tab already covers it") — direct feedback said that wasn't enough. Added one in the same left slot `subViewBack` uses for a drilled-in sub-view (Practice Detail, etc.); `subViewBack` still takes priority when set, un-drilling one level first.

### Teams & roster
- Teams have sport, timezone, season dates, and a color (auto-assigned from a curated palette, editable, includes black/dark gray) — shown as a Teams-list stripe and as the team-workspace header-bar background.
- The header bar's ellipsis is the only way to Edit Team; Delete Team (with a confirm step) lives inside the edit modal. Hidden entirely (`canManageTeamInMode`) for a coach viewing a team they can't actually manage (e.g. an assistant), so it never implies edit access they don't have.
- Players, staff (head coach/assistant/helper roles), structured "Player Focus" areas (renamed from "Skill Notes") — skill-tag taxonomy + coach freeform sub-tags.
- Team creator is automatically head coach (trigger on team insert, personal teams only; one-time backfill already run for pre-existing teams).
- **Staff invite linking**: adding a coach by email (`add_team_staff` RPC) silently links to an existing account if one exists; a `claim_pending_team_staff` trigger auto-links any pending invite the moment the invited person signs up. Re-adding someone who left revives their archived row instead of duplicating.
- Invite notification email via a `pg_net`-triggered Edge Function + Resend (never Supabase's `admin.inviteUserByEmail`, which is a magic link).
- One-time welcome card + self-serve `leave_team` RPC (narrow — archives only the caller's own row, refuses for team owners). The reverse direction exists too: `leave_team` also writes a `team_departures` row, surfaced to whoever can manage the team as a Home notification ("{name} ({role}) left {team}", Acknowledge/View Coaches) via `acknowledge_team_departure` — a head-coach-initiated removal (`archiveStaff`) does NOT create one, only a self-initiated leave.
- **A third one, symmetric with the two above, for accepting an invite** (twenty-seventh session, direct feedback: this used to be silent beyond the roster growing): `accept_team_invite` now also writes a `team_join_notices` row (`20260803010000_team_join_notices.sql`, same shape/RLS/ack pattern as `team_departures`), surfaced on the head coach's Home as "{name} accepted your invite to {team} as a{n} {role}. Set up what you share and delegate with them." with **Set Up Permissions** and **Dismiss**. Set Up Permissions deep-links to `/team/:teamId/roster` with `location.state.openPermissionsForUserId` (same `location.state` deep-link convention Settings' Terms/Privacy back button already established) — `RostersTab` reads it once on mount, switches to the Coaches tab, and auto-opens `PermissionsModal` for that exact coach. Verified live end-to-end with two real accounts: invited, accepted, confirmed the notification's copy/team/role, clicked through, and confirmed the Permissions modal opened pre-targeted at the right coach with both toggles correctly defaulting off.
- Staff-add suggestion chips (tap-to-fill staff already added on your other teams, deduped by email).
- Role-aware UI: assistants/helpers view everything and can run live sessions, but Builder/schedule-wizard/team-settings/staff-management entry points are hidden for teams they don't head-coach — checked per-team (`isHeadCoach`), not globally.
- **Home's "Your Teams" card row shows the caller's per-team role** (Head Coach/Assistant Coach/Helper, low-emphasis text, via the existing `myTeamRole()` — no new query) — Coach mode only, hidden in Org mode where the row lists every team in the org regardless of the viewer's own role on each one.
- **Settings → "My Team Assignments"** already listed every team a coach is on (any role, any org) with a `show_on_home` toggle and the same role label — this was never actually Org-mode-gated despite an earlier assumption that it was; nothing needed building here.

### Organizations (Coach/Org mode)
- `org_staff` is the org-membership table. Today it has exactly **one** role: the CHECK constraint only allows `'director'` — `admin` was proposed in the original design as a future extension (adding it later is a one-line constraint change, not a migration) but does not exist as a usable value yet. Don't assume an `admin` role is already live anywhere.
- A persisted (localStorage), per-device **Coach/Org mode toggle** on Home: Coach mode shows your own teams across every org; Org mode shows every team in one selected org plus a weekly-practices rollup. Only Home/Teams/Library change per mode — team-workspace screens never do. Labeled "Coach Mode"/"Organization Mode" (seventeenth session, was bare "Coach"/"Organization").
- **Org mode no longer reverts to Coach mode on reload, fixed** (Phase 2 continued): `App.jsx`'s guard effect that resets a persisted-but-now-invalid org selection back to Coach mode was gated only on `library.myOrgs` being present — but that array's own `useState` default is `[]`, which is already truthy and already fails `.some(...)`, so the guard fired and wrongly reset a perfectly valid Org mode selection on every fresh page load, well before `refreshLibrary()`'s real fetch had a chance to populate it. Fixed by also requiring `loaded` (the existing combined-fetch-gate flag, already true only once teams/library/planning have all resolved at least once) before the guard runs. Verified live: set Org mode via `localStorage`, reloaded twice in a row, confirmed it stayed in Org mode both times (previously reverted to Coach mode every time).
- **Org creation gated** (seventeenth session): Settings' "+ Create Organization" was instant self-serve for anyone with no approval step. Regular coaches now see a "Request a Consultation" card instead, in Settings > Account under "Membership" (moved there in the twentieth session; originally on the main Settings list) -- a real form (name/email/phone/team count, plus encouraged free text), not a mailto link, since the twentieth session. Only founder-admins (existing `is_admin()` gate, same one used for Public Library curation) get the real create-org form, still on the main Settings list, which also takes an optional director email and sends them a real `org_invites` director invite right away (`orgInviteCoach(...,"director")`) so the admin can hand off control immediately rather than needing to touch `org_staff` rows by hand.
- **`org_create_team` no longer auto-inserts a `team_staff` head_coach row** (Phase 2, seventeenth-session follow-up): the insert added by `20260721050000_org_create_team_head_coach.sql` is removed (`20260730060000_org_create_team_no_auto_head_coach.sql`) — a director overseeing an org team manages it purely through org membership now, matching `can_manage_team`'s server-side RLS, which already had this branch. This was previously deferred because `RostersTab`, `GoalsScreen`, `PracticeDetail`, `ScheduleScreen`, `SeriesWizard`, and `SchedulePracticeModal` all gated management on personal `isHeadCoach`/`myTeamRole`, not the mode-aware `canManageTeamInMode` (which already existed but was only actually used by `HomeScreen.jsx`). All six now use `canManageTeamInMode(team, coachId, mode)` instead, so a director sees +Add Player/Coach, can edit Goals, and can schedule for an org team with zero personal `team_staff` rows there. Verified live (fresh director → new org team → zero `team_staff` rows → Roster/Schedule/Goals all correctly open for editing).
- Org Details screen: name, sports (multi-select, see below), color, member list with an ellipsis-menu for add/remove/role-change ("Add Org Member" — an earlier "Invite Coach" label was renamed once it stopped being a no-op).
- **Orgs are multi-sport** (Phase 2, seventeenth-session continued): `organizations.sport` (a single nullable text column, added 2026-07-22) replaced with `organizations.sports text[] not null default '{}'` (`20260730070000_org_multi_sport.sql`, existing single values backfilled into one-element arrays before the old column was dropped — no parallel columns left behind, on purpose, per the stale-column drift this project already hit once with `team_goals`/`get_team_goal_report`). Org Details' edit form is a multi-select chip row (`SPORTS.map` toggle buttons, not a dropdown) instead of a single "-- Select a sport --" picker. Adding a team inside an org that has sports configured now restricts *and* defaults the new team's Sport field to just that org's sports (`ModalLayer.jsx`'s addTeam form); an org with no sports configured yet (empty array) falls back to the old unrestricted full-sport-list behavior. Not server-enforced (`org_create_team`'s `p_sport` still accepts any text) — sport isn't a security dimension here, same reasoning as every other client-gated UX convention in this app.
- **Coach invite flow** uses real consent, not the informal `team_staff` auto-link pattern: `org_invites` (pending/accepted/declined) → `accept_org_invite`/`decline_org_invite`/`cancel_org_invite`, with the caller's email matched server-side against `auth.jwt()`, never client-asserted.
- **Multi-org drill sharing** via `activity_library_org_shares` (a many-to-many join table, not a single-org column) + batch RPC `set_drill_org_shares` (all-or-nothing). `promote_drill_to_org_library` does a full-copy fork (director-only) — not a live reference.
- Library has a 5-tab sub-nav (Drills, Templates, Locations, Equipment, Skill Tags), each mode-filtered.
- Settings has "My Team Assignments": every team across every org, with a per-team `show_on_home` toggle to hide a team from Home without leaving it.
- **Known, unfixed gap**: Builder's own team picker is not mode-scoped (shows all teams regardless of Coach/Org mode).
- **Org equipment auto-merges into a team's equipment picker when the location matches** (Phase 2, seventeenth-session continued): Builder/live-run's per-activity and per-station equipment pickers (`ActConfig`/`StationConfig` in `ActivityConfigs.jsx`) used to filter `data.assets` by sport and location only, with **no owner/org check at all** — under RLS that meant every asset for *any* org the coach belonged to leaked into every practice's equipment picker regardless of team or location, the same class of bug already fixed once this session in `EquipmentTab`'s Team-tab view. A new `ownedOrOrgAtLoc(asset,coachId,team,loc)` helper now scopes both pickers to the coach's own equipment, plus — only if this specific team belongs to an org and the asset is tagged to the activity's own location — that org's shared equipment too, so a coach can use the club's on-site gear without switching into Org mode. An org asset with no location tags at all does not merge in this way (untagged isn't "everywhere" here, unlike a coach's own untagged equipment); an activity with no location set gets none of the org merge either. Verified live: a director's org-owned "Club Cones" (tagged to a "Club Gym" location) appeared in a Station's equipment picker for a practice scheduled there, while a second org asset tagged to a different location did not.
- **Org name shown on team cards** (seventeenth session): Home's "Your Teams" cards and the Teams tab list now show which org a team belongs to (Coach mode only — redundant in Org mode, which already says the org's name once at the top). Needed a small `organizations_select_member` RLS relaxation (`20260730030000_org_name_visible_to_team_members.sql`) — previously only an org_staff member or the org's creator could read the org's own name row, so an assistant/head coach who's plain `team_staff` on an org team (not personally in `org_staff`) got `null` back for it.
- **Leave-team bug, fixed**: the global `/schedule` route (`ScheduleLegacyRoute`) passed raw, unscoped `data.teams`/`data.practices` straight through, unlike Home/Teams which already scope via `teamsForMode`/`homeTeamsForMode`. A team a coach had genuinely left (their own `team_staff` row archived) stayed visible there as a filterable option and kept showing its practices, because `can_access_team`'s RLS also grants access via org membership independent of that row (correct, by design, for org oversight) — the fix was scoping the client, not the RLS.
- **Added-to-team banner redesigned**: Home's "You've been added to X" card no longer has a raw "Leave" link — it's now "Accept" (local dismiss only; `welcomed_at` is already set the moment the banner would show) and "Manage Team Assignments" (deep-links into Settings' assignments section via `location.state.openSection`, same mechanism Terms/Privacy's back button uses to return to Settings > Account instead of Home).
- There is no standalone Org Home page — it existed briefly (`/org/:orgId`, `OrgHomeScreen.jsx`) but was retired and folded directly into `HomeScreen.jsx` under Org mode. Don't recreate it as a separate route.
- **Club Library carries the org's own color** (Phase 2, seventeenth-session continued): a 4px colored bar under the "Club Library" title, same `activeOrg.color` already used for the Org-mode bottom tab bar (Layout.jsx) — "so it looks like their org" applied consistently in a second place. No bar renders if the org hasn't picked a color (falls back to nothing, not a default color).
- **Per-team Equipment tab dropped in Org mode** (Phase 2, same session): the team workspace's tab row (`Layout.jsx`'s `teamWorkspaceTabs`) no longer shows Equipment when in Org mode — a club's equipment is now managed centrally from Club Library's own Equipment tab, not duplicated at the team level. `TeamEquipmentRoute` (`App.jsx`) redirects to that team's Schedule tab if reached directly while in Org mode (stale link/back-nav), the same guard pattern already used there for a since-deleted team. Coach mode is unaffected — the tab is still there for a coach managing their own team's equipment.

### Library
- Drills, equipment/assets, templates — coach-owned, with org-sharing (see above) and manual drill reordering.
- **Library "+ Build Practice" now offers a real choice** (seventeenth session): used to always jump straight into an empty unscheduled build (`goToBuilder(null)`). Now asks first — build an unscheduled practice (unchanged Save/Template/Run Now flow) or pick an upcoming scheduled practice to plan instead, via a new `SchedulePracticePicker` (agenda/month view, read-only picker, hands off straight to Builder — deliberately not a reuse of the full `ScheduleScreen`, which owns its own PracticeDetail drill-in that isn't what a row-tap here should do).
- **Equipment duplication/leak, fixed** (seventeenth session): a real production bug, root-caused live — the Team-tab Equipment view (`EquipmentTab` with `sportFilter` set) filtered the *entire* RLS-scoped `data.assets` array by sport only, no owner/org check at all. That leaked in Public Library catalog equipment (rows with `source_catalog_id` set, made visible to every coach by a deliberate RLS carve-out for catalog content) alongside any other coach's owned assets that happened to share a sport — this is what produced "9 baseball items already there, cones twice" for a coach/org that had added none. Fixed by reusing the same `visibleEquipment()` scoping the Library tab's own Equipment view already used (own/org, catalog rows excluded), narrowed to the one team's sport. `TeamEquipmentRoute` also needed `mode` threaded through, which it wasn't receiving before.
- **Skill tags**: `skill_categories` exists today for **6 sports** — Baseball and Basketball (seeded 2026-07-07) plus Football, Soccer, Lacrosse, and Volleyball (added 2026-07-18 alongside the Public Library taxonomy). Softball, Hockey, Tennis, and Swimming (the rest of the app's 12-entry sport list in `constants.js`) still have zero categories. All 6 categorized sports now work identically: real `scope='global'` tag rows, visible to every coach immediately, no per-coach action needed, and not individually archivable by any one coach (consistent across all 6 — see Session Log for the Baseball/Basketball duplication this replaced). `ensureDefaultSkillTags` (client-side, runs on every sign-in) still exists and is still called, but is currently a no-op for every sport — kept in place rather than removed, in case a future sport ever wants true per-coach starter tags again.
- `SkillTagPicker`: selected tags show as removable chips; an "Add/Edit Skill Tags" button opens a full category-grouped, searchable picker in a bottom-sheet overlay.
- **Public Library** (built and shipped — the planning doc for this feature was written before implementation and is now stale on this point): a `content_catalogs` table backs a search-first Public Library browser (`PublicLibraryScreen.jsx`, 2026-07-19) — pick a sport first, never a dump of every drill; working Source/Publisher filters (currently one option each, future-proofed for multi-publisher catalogs); founder-admin add/edit/archive affordances built in. Seeded via `scripts/import-public-library.mjs` from the per-sport JSON files in the repo root (`ROP-Public-Library-*.json`). `copyDrillToMyLibrary` resolves a copied drill's equipment into the recipient's own asset pool by name-match-or-create; it does **not** currently copy skill tags of any scope onto the copy (a known gap, not yet fixed).
- **My Library publisher filter** (2026-07-29): `activity_library` now has `copied_from_owner_user_id`/`copied_from_organization_id`/`copied_from_catalog_id`, set once by `copyDrillToMyLibrary` at copy time — otherwise a drill copied from an assistant's shared library, an org library, or the Public Library became indistinguishable from a self-authored row the moment it landed in "My Library." `NewLibraryScreen.jsx`'s "mine" shelf now shows a "From {publisher}" line on copied drills and a Publisher section in the existing Filter modal (multi-select, alongside skill tags). Lineage only, same convention as `template_activities.library_activity_id` — never updated after the copy, and the FKs are `on delete set null` so a since-deleted source doesn't block the copy.
- **Library restructured: Explore lives under Drills only, and its per-org shelf is now per-coach ("Team Libraries")** (Phase 2, seventeenth-session continued): the 5-tab content-type row (Drills/Templates/Locations/Equipment/Skill Tags) is always visible now — it used to hide behind a page-wide My-Library-vs-Explore pill, so switching to Explore hid Templates/Locations/Equipment/Skill Tags entirely even though Explore never applied to any of them (only Drills has cross-coach/org content to browse). That pill moved down into just the Drills tab, relabeled "My Drills"/"Org Drills" vs. "Team Libraries." Within Team Libraries, the old flat "From {org}" shelf (every coach's shared drills lumped together) is now one shelf per coach who's shared something, labeled with their name and count (e.g. "Jane Smith (3)") — a director can now tell whose library is whose. Coach-name resolution deliberately checks `team_staff` (which stores names directly) as a fallback after `profiles`, since `profiles_select_org_co_member` only resolves a fellow *org_staff* member's name — a coach who's merely `team_staff` on one of the org's teams (the more common case) wouldn't otherwise resolve to anything but "A coach."
- **`copyDrillToMyLibrary` is mode-aware** (Phase 2, same session): copying a drill while in Org mode now lands it in the org's library (`organization_id` set, `owner_user_id` null) and resolves its equipment against the org's own asset pool instead of the director's personal one — previously every copy went to the copier's personal library regardless of mode, so a director "copying to the club" from Org mode was quietly building a personal collection instead. Button/screen copy read "Copy to Org Library" instead of "Copy to My Library" when in Org mode (`NewLibraryScreen.jsx` and `PublicLibraryScreen.jsx`, both driven by the same `doCopy`). Verified live: copying a coach's shared drill while in Org mode produced a row with `organization_id` set and `owner_user_id` null, visible on the "Org Drills" shelf with "From A coach" attribution.
- **Peer library sharing (Permissions) now defaults to off** (twenty-seventh session, correcting the twenty-fifth session's original default): `team_staff.head_coach_shares_library`/`assistant_shares_library` default `false`, not `true` — a head coach no longer automatically sees an assistant's drills (or vice versa) the moment an invite is accepted. Reaching another coach's drills is still fully possible, just opt-in: Library → Explore → find their shelf → copy in what's wanted, or either side turns sharing on from Permissions. The drill-menu toggle for a single drill's own private flag is now labeled "Keep Private"/"Make Public" (was "Hide from My Coaches"/"Share with My Coaches" — "Make Private" was already taken in the same menu for the unrelated org-share-clearing action, hence "Keep"). Going public shows a real confirm prompt first ("This drill will be visible to coaches you share your library with."). A small lock icon now sits next to a private drill's name on its card for at-a-glance visibility.
- **Drill card heat-color icon removed** (twenty-seventh session, direct feedback: "too distracting"): usage insights are reachable only via the drill's ellipsis menu ("View Drill Insights") now, never a colored icon on the card itself. The heat-tier math/colors (`drillUsageHeatTier`) still exist in `constants.js` for potential reuse but nothing currently renders them.
- **Drill list sort** (twenty-seventh session): a `Sort` picker above "My Drills"/"Org Drills" — Custom (drag-to-reorder, unchanged default), Most Frequently Used (trailing-12-month completed-use counts, the same batch summary the removed heat icon used to consume), Alphabetical, and Suggested (ranks a drill by the largest goal deficit — target minus current — among the categories any of its tags roll up to, for a team the coach picks if they have more than one; a team with no goals or a drill with no matching tags/categories sorts last). Only Custom keeps the drag handle active; the other three are derived, not persisted.
- **Untagged-drills deep link from Goals & Insights** (twenty-ninth session): `NewLibraryScreen.jsx` accepts `location.state.untaggedForSport`/`teamId`/`returnTo` and, when present, scopes "My Drills" to that one sport and (by default, toggleable) to drills with zero skill tags — plus a "Back to Goals & Insights" button that returns to the exact team/tab the coach came from. Tagging a drill via the existing ellipsis → Edit → Skill Tags flow needs no extra plumbing to "return" the coach to the untagged list: since the filter is a plain derived value recomputed from `data.activityLibrary` on every render, a drill that just got tagged simply falls out of the filtered list the next time it re-renders, in place, without any navigation at all. Empty state reads "All caught up -- every {sport} drill has a skill tag" specifically for this mode (vs. the generic "No drills match the selected filters" every other filter combination uses).

### Planning & scheduling
- **Builder**: practice/template editor — drills, checklist activities (7 selectable "Practice Components" types: Intro/Closer/Checklist/Water Break/Stretch/Station Block/Other, customizable per coach via a persisted `localStorage` picker, collapsible section), station blocks with rotation (each station individually collapsible), grouping (whole/partners/N groups), equipment, coaching points, per-activity coach/sublocation assignment. My Drill Library is collapsible too. A newly-added activity auto-expands and stays open until the coach taps elsewhere (anywhere outside it collapses it). The Run of Practice section is solid dark green (a measured backdrop, not a literal wrapping box, so the just-added row's "stay pinned while scrolling" sticky behavior has real room to work) with an animated stopwatch mark that spins once per activity added/removed. Practice Details collapses by default for an already-scheduled practice (open by default for a fresh build), takes on the team's own color, and includes an editable Date/Duration for a scheduled practice. Long-press-to-preview existed for one session and was removed -- it was being confused with scroll gestures on touch.
- **Recurring schedules**: a wizard (days of week → time/duration → date range defaulted from team season → location → preview with per-date deselect) backed by `create_practice_series` — atomic, server-capped (≤150 occurrences, ≤400 day range), timezone-correct (DST-safe via `AT TIME ZONE`).
- **Home screen**: adaptive hero card for the next practice (Plan/Review/Run depending on state), an "Upcoming Practices" list on a **7-day** rolling window (renamed from "This Week" once the team realized the underlying filter was never a calendar week), and a separate "N practices need a plan" nudge that intentionally uses a **14-day** window (wider, so the nudge has real run-status data before a practice is imminent) — these are two different, deliberately different windows, not a contradiction.
- **Schedule tab**: Agenda (day-grouped, team-color-coded dots) and Month (calendar grid with per-day dots) views, both using the same hollow-when-unplanned/filled-when-planned dot rule.
- **Planned absences**: capture from Practice Detail, Player Profile, or a Home quick action. Feeds Builder's default group assignment and the live session's attendance defaults.
- Cancel (this-occurrence or this-and-future for a series) / restore; a "missed" state for practices whose time passed with no run.
- All practice times display in the **team's** timezone, never the viewing device's; "today" is always computed via `localDateStr()` (local calendar day), never `.toISOString()`.
- **Planning-depth indicator**: any practice with a `scheduled_duration_minutes` shows "35/60 min" whether planned or not. Three states, purely derived (`planningState()`/`sumMins()`), never stored: red under 90% planned, green at or above it, and — added twenty-seventh session, direct feedback — amber/orange ("exceeds") when the planned drills actually add up to *more* than the scheduled duration, distinct from the under-planned case since running long is a different problem worth its own color. The green state keeps a `✓` glyph; red/amber deliberately have **no** icon (see Session Log for why not `⚠`). Builder's own duration pill (`.pill.over`/now also `.pill.exceeds`) got the same third state.
- **Getting-started checklist** (reworked seventeenth session): was a "?"-icon popup with a green-dot nudge and static, non-clickable rows — direct feedback was that a brand-new coach's first Home screen wasn't intuitive about how to proceed. Now a persistent `GettingStartedCard` on Home itself (gone once every step is done), same 6 steps (team → players → library → schedule → first plan → run live), but each row (except "Run it live," which has no single screen of its own to jump to) is real navigation to the right screen. The green dot on "?" is gone since the card itself is now the visible nudge; "Getting Started" was also removed from the "?" menu (now just FAQs + Send Feedback) since the checklist isn't a popup anymore.
- **New `/faq` page** (seventeenth session, `LegalPages.jsx`, same top-level-sibling-route treatment as Terms/Privacy): natural-voice accordion FAQ, includes "Can I delete my account?" (deactivation vs. contact-us-to-actually-delete) and "How do I set up an organization with multiple teams?" (links to the consultation request above).
- **Missing-location dialogs** (seventeenth session): a coach with zero locations had no inline way to add one from Builder (empty `<select>`, no placeholder) or from the quick-schedule modal (which already had a working "None" option, just no way to add a real one). Both now offer a shared `AddLocationDialog` (`NewLibraryScreen.jsx`, exported) — name + optional sublocations in one step, auto-selects the new location and lets the coach continue what they were doing. Ownership (personal vs. org) is decided by the specific team being scheduled/built for (`team.organizationId`), not the global Coach/Org mode toggle, so it works correctly even through Builder's own known-not-mode-scoped team picker.
- **SeriesWizard consolidated** (seventeenth session): was 5 sequential screens (team → days/time → date range → location → preview); the first 4 (everything that's just picking fields) are now one "details" screen, with only the review/per-date-deselect step kept separate. Team-picker defaulting to the team you opened it from (via `presetTeamId`) already worked correctly before this change — confirmed, not touched.
- **Schedule empty state**: the global Schedule tab now prompts "No teams yet" with a "+ Create a Team" button when `data.teams.length===0`, instead of an empty calendar with no obvious next step.
- **Practice plan PDF export** (2026-07-29, `PracticePlanPrint.jsx`, triggered from a "Print / Export PDF" button on Practice Detail): renders a standalone, print-optimized document — not a screenshot of the app UI — and hands off to the browser's native Print/Save-as-PDF (no client-side PDF library, no server round trip). `#rop-print-root` + a new `@media print` block in App.jsx's `CSS` constant isolate that one subtree regardless of what else is mounted. Every drill shows duration **and** a computed clock start/end range (anchored to the practice's own start time, walking cumulatively through `actMins()` — same formula already used on-screen, so the printed total never diverges from what's displayed in-app), plus description, coaching focus, skill tags, equipment, player-gear notes, grouping, and per-player focus notes (resolved the same category-matching way the live view already does it). Station blocks get the same treatment per station. Intentionally reuses already-loaded client `data`/`team` state rather than a new RPC — no new data-access surface, just a different render of what Practice Detail already has.

### Goals & Insights
- Lives as a sub-toggle inside a team's Plan-equivalent workflow (folded together because "how this team spends its practice time" covered both, and Goals' original placeholder scope had never been built out separately).
- **Internal Overview/Trends/History navigation** (twenty-sixth session, `GoalsSubnav`): same rounded-pill sub-toggle already used for Coach/Org mode and My Drills/Team Libraries, generalized to three options. Overview keeps the existing goal editor, glance view, and (new) Next Practice Guidance; Trends is new (below); History is unchanged except its own detail view now also shows the Practice Execution Scorecard. The selected view is plain component state, so it naturally survives opening/closing a session from History within the same mount.
- **Side margins fixed** (twenty-ninth session): `TeamGoalsRoute` (`App.jsx`) was the only per-team route missing the `16px 16px` horizontal-padding wrapper that Roster/Equipment's routes both use — Goals & Insights only got the outer `.screen` container's 14px, reading noticeably tighter than its sibling tabs across all three subtabs. Added the same wrapper; removed the now-redundant bottom-padding-only div `GoalsScreen.jsx` had instead.
- **Shared attribution math, extracted once** (twenty-sixth session, `supabase/migrations/20260802000000_goal_attribution_shared_helpers.sql`): every new report below reuses the exact category-attribution rules `get_team_goal_report` already established — a multi-tag drill's minutes split evenly across its tags, a station's full `stationDuration` counted per-station (stations run in parallel, never divided), `'break'`-type activities excluded from the denominator entirely. Server-side, four small `security definer` SQL functions (`session_activity_minutes`, `practice_activity_planned_minutes`, `category_minutes_from_rows`, `total_attributable_minutes`, plus a `public.activity_minute_row` composite type) compose into every new RPC; all four have `revoke all ... from public` since they trust the caller's own authorization rather than checking `auth.uid()` themselves — they're internal building blocks, never meant to be called directly, and PostgREST would otherwise expose them as callable RPCs that leak another team's session timing. Client-side, `categoryMinutesForPracticeActivities`/`calculateGoalGapGuidance`/`calculateProjectedGoalImpact`/`classifyDurationVariance`/`summarizeCategoryTrend`/`drillUsageHeatTier` (all in `constants.js`) mirror the same rules in pure JS so Builder can project an *unsaved* draft locally without a round trip per keystroke.
- **Skill-Development Trends** (twenty-sixth session): a new `get_team_goal_trends(p_team_id, p_window_weeks)` RPC returns every category with a currently-saved goal, bucketed into real Monday–Sunday weeks in the *team's own timezone* (never device timezone, DST-safe via the same `AT TIME ZONE` pattern `create_practice_series` already uses), for the team's saved rolling window (1–12 weeks, same cap as the existing report). Each category renders as a `GoalTrendCard`: current target/rolling-planned/rolling-actual percentages (averaged from the same weekly data the chart plots — see the Gotcha below for why this wasn't true in the first draft), a percentage-point variance badge, a compact inline-SVG line chart (`WeeklyTrendChart` — solid Actual line, dashed Planned line, dotted flat Target reference, so the three read apart without relying only on color), and a deterministic plain-language trend summary (`summarizeCategoryTrend`: 3+ usable weeks required, a 3-consecutive-same-direction streak called out first, then latest-vs-earliest movement, falling back to a planned-vs-actual execution-gap message when the movement itself is flat). Empty weeks render as real gaps in the chart, never a misleadingly-connected line. Tapping a card expands a plain weekly table. **Deliberately different from `get_team_goal_report`**: that report's Planned bucket is forward-looking (what's currently scheduled); Trends' Planned bucket is backward-looking instead, computed from each *completed* session's own saved plan via `practice_activity_planned_minutes`, so Planned and Actual always compare the same practice cohort per week — this is a real, intentional divergence between the two RPCs' math, not a bug, and is why Trends never reuses `get_team_goal_report`'s own percentages for its card headlines. Empty states: no goals ("Set team goals to see development trends"), goals but no completed sessions ("Run a practice live to begin building actual-time trends"), both distinguished from the ordinary loading state.
- **Next Practice Guidance** (twenty-sixth session, Overview, `NextPracticeGuidance`): no new RPC — reuses `get_team_goal_report`'s own minute totals (it already returns real minutes, not just rounded percentages, in `planned_minutes`/`actual_minutes`/`denominators`). Uses rolling Actual when usable actual time exists, falls back to rolling Planned (labeled "Based on planned practice time until actual timing is available"), or the bare goal mix when neither exists yet. Duration comes from the next upcoming non-cancelled practice's `scheduledDurationMinutes`; when none exists, falls back to the team's own most recently scheduled practice's duration rather than guessing 60 minutes (this app has no existing default-duration convention, confirmed by grep before writing this) — with no duration at all, shows percentages and priorities only. Shows the three largest below-target categories (goal-mix minutes for the duration, plus minutes needed to fully close the current rolling gap when mathematically possible, capped at "this gap cannot be fully closed in one practice" when it exceeds the practice length) with a "Show all categories" expansion. Categories at/above target never get a negative recommendation. **Each category row expands on tap** (twenty-seventh session, direct feedback, `CategoryGapRow`/`drillsForCategory`): lists the coach's own library drills tagged to that category (name + duration), or "No drills in your library are tagged to {category} yet" when none match — read-only, no add-to-practice action from this screen.
- **Builder library switcher** (twenty-seventh session, direct feedback): the drill section at the bottom of Builder defaults to "My Library" (own drills only) instead of the previous flat merge of every accessible drill (own + org + any peer sharing with the coach, with no way to tell them apart). A dropdown appears only when more than one source exists for the team being built for — the team's own org (if any) and each peer currently sharing with the coach on that team, both derived from what's already in `data.activityLibrary` (already correctly RLS-scoped) via the same per-owner grouping technique `NewLibraryScreen`'s own Explore shelves use. Selecting a drill from another source still only copies it into the practice, never into the coach's own library (unchanged, pre-existing `addAct` behavior).
- **Combined Builder Goal Guidance** (twenty-sixth session, new `src/components/BuilderGoalGuidance.jsx`, `<BuilderGoalGuidance>`): its own sibling section between Practice Details and The Run of Practice (never inside Practice Details' own body), starting collapsed on every entry with a real summary line (e.g. "2 priorities · plan projects closer to goal"). One `get_team_goal_report` fetch per team selection (cached, not refetched per keystroke); every recalculation as the draft changes — add/remove/resize a drill or station, change duration, switch teams — runs locally via `useMemo` over the same pure helpers Next Practice Guidance uses, with **zero additional network calls per edit** (confirmed live: added a drill to an empty draft and watched the projected-impact numbers update instantly, including correctly flagging an *overshoot* — adding too much of one category's time pushed its projected percentage past the target and read as "Farther from goal" even though it moved in the right direction, since the math measures distance-to-target, not direction alone). Three parts when expanded: current priorities (same gap logic as Next Practice Guidance, using this practice's own duration), the current draft mix (minutes per category, untagged minutes surfaced prominently, which target categories have zero planned minutes), and projected rolling impact (current % → projected % per category with a `Closer to goal`/`Farther from goal`/`At goal`/`No change` label). Builder has no Coach/Org-mode awareness anywhere today (a pre-existing gap noted below) — the section doesn't need it to be correct, since `get_team_goal_report`'s own `can_access_team` check already covers a director on an org team with no personal `team_staff` row; the "manage goals" link in the empty-goals state falls back to a personal head-coach check only.
- **Practice Execution Scorecard** (twenty-sixth session, `SessionHistoryDetail`, `PracticeExecutionScorecard`): a new `get_session_execution_scorecard(p_session_id)` RPC, rendered near the top of the session detail, before Skill Minutes/Planned vs. Actual. Deliberately no single overall score — structured measures only (planned/actual duration, planned/logged activity minutes, plan completion count, attendance, other/transition time) plus an activity-execution breakdown classified Extended/Shortened/On Plan/Skipped/Logged-but-not-originally-captured. Execution-unit grain was confirmed against the real logging model before writing this, not assumed: every non-station-block `practice_activity` is one unit keyed by `practice_activity_id`; every individual *station* inside a station block is its own unit keyed by `station_id` — the block container itself is never a loggable unit, so nothing gets marked "skipped" merely because logs live at the station grain. Classification uses a shared 60-second on-plan tolerance (`ON_PLAN_TOLERANCE_SECONDS`/`classifyDurationVariance` in `constants.js`, reused by Drill Insights too, so the two features never disagree about what "on plan" means). "Logged but not originally captured" needed a real signal, not a guess: `adjusted_by`/`adjusted_at` alone can't distinguish a live-captured row that was later corrected from one that never existed during the run at all, since both `adjust_session_activity` and `add_session_activity_row` stamp them the same way — a new `session_activity_log.manually_added` boolean (`20260802020000_session_activity_log_manually_added.sql`), set only by `add_session_activity_row`'s insert, makes this reliably derivable going forward (pre-migration manually-added rows just won't get the special badge, an honest simplification rather than a guessed classification). Refreshes automatically after any manager edit (exclude/restore, adjust a logged row, add a missing one) via the same `refresh()` call site the raw log already used.
- **Drill Performance Insights** (twenty-sixth session, owned-drill shelf only — My Drills in Coach mode, Org Drills in Organization mode; never Team Libraries/peer-shared or Public Library drills): reachable via a "View Drill Insights" ellipsis item, always available on an owned drill regardless of usage, opening an empty state when there's none yet. (The card itself no longer shows a colored heat icon — see the Library section above; direct feedback called it too distracting. `DrillInsightHeatIcon`/the heat-tier math still exist in case a less-prominent surface wants them later.) Two-tier query architecture: `get_drill_insight_summaries(p_library_activity_ids[])` is one batch call per shelf (capped at 300 ids, silently drops any id the caller doesn't own/manage rather than trusting the client, still called even though nothing currently renders its color — the count still drives the Most Frequently Used sort), `get_drill_insights(p_library_activity_id)` fetches full detail only when a coach actually opens one drill — usage counts (4wk/12wk/12mo/all-time), average planned vs. actual duration with a deterministic natural-language read-out, on-plan/extended/shortened percentages, reverse-chron usage history, teams, and recent notes (capped 5, each with a per-note "hide" button added twenty-seventh session — direct feedback wanted to hide one irrelevant note without touching the others, reuses the same `archiveNote` RPC `SessionHistoryDetail` already relies on). Lineage is strictly `library_activity_id`, confirmed through normal activities, stations, templates (a template's own `library_activity_id` copies through into the practice the same way a direct drill add does), and manually-added history rows (which can only target an execution unit already in the plan, so they inherit its lineage automatically) — no fuzzy name matching anywhere, so renaming a drill never breaks its history. A copied drill gets its own new `activity_library.id` via `copyDrillToMyLibrary`, so it starts its own insight history with zero special-casing needed. Every usage/note row is additionally filtered to a team the caller can *currently* access, since a personal drill's history can span teams the coach has since left.
- Coach sets per-skill-tag target percentages (category-grouped picker filtered to the team's sport, sum-over-100 validated), plus a 1–12 week rolling report window (`teams.goals_window_weeks`). Each category also has a typeable number input alongside its slider now (seventeenth session) — landing on an exact value across several sliders that need to add to 100 was fiddly by drag alone.
- **Save-when-not-100% confusion, root-caused and fixed** (seventeenth session): Save was already disabled unless totals hit exactly 100% (`canSave`), but no `.btn` in the whole app had any `:disabled` CSS at all — a disabled primary button looked pixel-identical to an enabled one, so a coach could tap it, watch nothing happen, and have no idea why. Added a global `.btn:disabled{opacity:.4;cursor:not-allowed;}`. Also added real dirty-tracking (`useBlocker` + `beforeunload`, same pattern Builder already uses) so navigating away with unsaved slider edits now prompts a confirm instead of silently discarding them. Both confirmed live: the confirm dialog fired with the exact right message and blocked navigation; the grayed-out Save button was visibly different at a glance. **Save also grays out again immediately after a successful save** (twenty-seventh session, direct feedback): `canSave` alone (totals=100) stayed true after saving, so the button looked exactly as clickable as before even with nothing left to save — `disabled` now also checks the existing `dirty` flag.
- **`get_team_goal_report`/`set_team_goals` stale-column bug, found and fixed** (seventeenth session, `20260730040000_fix_get_team_goal_report_stale_column.sql`): the live function still referenced `team_goals.skill_tag_id`, a column replaced by `skill_category_id` back on 2026-07-19 (`20260719030000`). A same-day migration (`20260719040000_goals_rpcs_category_level.sql`) already contained the correct `create or replace` definition, but it evidently never took effect live — the exact class of migration-history-vs-live-schema drift this file's Session Log has flagged before (thirteenth session). This broke the "Target vs. Planned vs. Actual" glance view for *every* team, silently, until caught by an actual browser test against production. Fixed by re-applying both functions' correct bodies verbatim; verified via a real (and cleaned-up) throwaway account before calling it done, not just a clean migration push.
- A bullet-chart-style glance view: target tick, planned/actual bars, delta chips (only shown at ≥3 points off), an "untagged" row, an "other/transitions" per-practice average. **"Tag Untagged Drills" button** (twenty-ninth session, `canManage` only, not gated on the untagged-time being "high" — a coach cleaning up a small amount shouldn't have to wait for it to become a problem first): deep-links into the Library's new untagged-only filtered view for the team's own sport, described in the Library section above.
- A History section, backed by `get_team_session_history(team_id)`, is the first real frontend read path for `session_activity_log`'s actual (not just planned) timing: reverse-chronological sessions with wall-clock duration, attendance, top-3 skills, and manager-only editing (exclude a session from goals, adjust a logged row's start/end, or log a row that was never captured live).
- The older, separately-used `HistoryViewer` component (`CommandScreen.jsx`, reused by `ScheduleScreen`/`ManageScreen`) was deliberately left untouched rather than retrofitted with this — it only ever receives a bare `practice` object, no `session_id`, so a new `SessionHistoryDetail` component was built instead to avoid destabilizing three already-working call sites.
- **`SessionHistoryDetail`'s Notes card** (added on top of the existing `notes` display, which used to just be plain text) now groups notes by drill vs. end-of-practice, shows each note's author + role (or "Helper" + whatever freeform label an anonymous submitter typed) and any @mentioned players as chips, and lets `canManage` archive a note (`archiveNote` — new; the underlying `notes` table had no update policy at all before this session).
- **Development Pulse** (twenty-eighth session, `src/components/DevelopmentPulseCard.jsx`): a single dynamic CTA card on Home, directly beneath the next-practice hero, Coach mode only (never Org mode — a cross-team director-facing version is explicitly a separate, future widget, not this card reused with a random team). Resolves to exactly one of 7 states via a pure, presentation-neutral `resolveDevelopmentPulseState()` (`constants.js`) — no categorical fifth variation of goal math: it's built entirely from `get_team_goal_report` (extended this session with one new field, see below) plus the same `categoryMinutesForPracticeActivities`/`calculateGoalGapGuidance`/`calculateProjectedGoalImpact` helpers Builder Goal Guidance and Next Practice Guidance already use. States in priority order: no categories for the team's sport → goals not configured → insufficient usable history (`DEVELOPMENT_PULSE_MIN_COMPLETED_SESSIONS=2`) → data quality too weak (`DEVELOPMENT_PULSE_MAX_UNTAGGED_PCT=25`, outranks a category recommendation on purpose) → largest gap missing from the next plan → next plan materially improves the largest gap → a meaningful gap not covered by the previous two → everything aligned (`DEVELOPMENT_PULSE_BALANCED_TOLERANCE_PTS=3`, with its own override: a currently-aligned team whose next plan would move a category materially *farther* from goal falls through to the gap state instead of a false all-clear). A bullet-bar SVG graphic (Actual/Projected/Goal on one 0–100 axis, solid fill + hatched-pattern projected extension + a goal tick, never color-only) — no Recharts, no new charting dependency, same inline-SVG convention as the Trends chart. **Focus-team selection** (`resolveDevelopmentPulseFocusTeamId`) is next-practice's team first, else the home team with the most recently completed session (one new batch RPC, `get_teams_recent_completed_session(p_team_ids[])`, so this never becomes an N+1 per-team query), else the first team. **Spec compliance bug found and fixed during live verification**: the "missing from next plan" state's `Plan {N} minutes` CTA didn't check `closable` the way the sibling "meaningful gap" state already did, so it could recommend more minutes than the practice's own scheduled duration (observed live: "Plan 63 minutes" inside a 45-minute practice) — the spec explicitly prohibits this ("Do not use `Plan N minutes` with a number larger than the practice duration"); fixed to fall back to the same "This gap will take more than one practice to close" / "Review Goal Guidance" copy the closable-gap state already used. **Navigation wiring bug found and fixed**: the CTA into Builder (`kind:"builder_goal_guidance"`) initially called `navigate()` directly, which left `BuilderScreen`'s `useState` initializers (which read `editPracticeId` from context) seeing a stale `null` on their first render — `editPracticeId` is only set a tick later via `BuilderRoute`'s own `useEffect`, by which point those `useState` calls had already locked in "new blank practice" defaults and never re-derive. Every pre-existing entry point into Builder avoids this by calling the existing `goToBuilder()` helper (which sets `editPracticeId` synchronously *before* navigating) instead of a bare `navigate()` — `goToBuilder` was extended with an optional 4th `navState` param (forwarded to `navigate()`'s own `location.state`) so Development Pulse's deep link (`openGoalGuidance`, `highlightedSkillCategoryId`) could go through the same safe path instead of bypassing it. `BuilderGoalGuidance` force-opens on arrival (`startOpen`) and gives the highlighted category a one-time CSS pulse (fades on its own via `animation`, plus a 2.6s timer clears the prop so it doesn't replay on an unrelated re-render) — never permanently changes Builder's own open/collapsed state or dirty-tracking. The "Review Untagged Time" CTA has no precise in-page anchor per spec, so it lands on Goals Overview and gets the same one-time pulse treatment on the Untagged row instead (`emphasizeUntagged` via `location.state`, consumed once by `GoalsScreen`). "View Trends" lands directly on the Trends subtab (`location.state.openGoalsView`). All of this verified live end-to-end with a disposable coach account walked through data-quality → missing-from-next-plan → aligned-with-a-moving-away-override → aligned states via direct data mutation (not 7 separate synthetic teams), plus a 19-case pure-function unit pass (`resolveDevelopmentPulseState`/`resolveDevelopmentPulseFocusTeamId`) covering every state, the tie-breaker (stable sort, first-configured-category wins), and focus-team priority order. Also verified: card absent in Org mode (code-inspection only — `!isOrgMode &&` gate, same pattern as every other mode-gated surface in this file, not re-tested live against a real org account), phone-width layout (375px, no clipping/overflow). **A second, unrelated FK gap found and fixed while cleaning up test data**: `session_activity_log.practice_activity_id`/`.station_id` had no `ON DELETE` action at all (unlike every other link in that same cascade chain, which already cascade), blocking deletion of a team/practice's history the moment any activity had been logged against it — fixed to `ON DELETE CASCADE` (`20260805000000_session_activity_log_activity_cascade.sql`, dry-run verified, applied to production).

**Twenty-ninth session addendum — collapsible, persisted, with a new-insight indicator:** the header now has a caret (`Ic_Caret`) that collapses the card down to just its header row; collapsed/expanded state is persisted per-coach (`localStorage`, keyed by `coachId` — this is one physical widget on Home, not a per-team preference, so it stays collapsed across sessions and across whichever team ends up as the focus team on a later visit) rather than per-team or in component state. A small red dot appears next to the header label whenever the card is collapsed *and* the resolved state has changed since the coach last had it open — tracked via a cheap fingerprint (`teamId|state|categoryId`, deliberately excluding percentages/minute counts so ordinary week-to-week wobble in the same underlying situation doesn't re-trigger it) also persisted in `localStorage`; expanding the card (or it simply being expanded when a new state resolves) marks the current fingerprint seen and clears the dot. **A real, pre-existing bug found during this session's live verification, not part of the requested work**: the `insufficient_history` state's goal-mix bar (`GoalMixStrip`) was fed raw `report.skills` rows (`target_pct`, snake_case) while reading `c.targetPct` (camelCase) — every segment computed a `NaN` width, so the bar rendered as nothing at all (invisible, not just wrong), and its `aria-label` read "Passing undefined%, Shooting undefined%". Fixed by mapping to camelCase at the call site, matching every other graphic component's prop convention in this file. Verified live: collapse/expand, persistence across a hard reload, the red dot appearing after a background data change (team goals set via direct SQL while collapsed) and clearing on expand, and the goal-mix bar rendering correctly (both the visual bar and the corrected aria-label) after the fix.

### Live sessions
- Real-time state machine (`practice_live_sessions`) with take-control/hand-off between coaches, pause/resume, ±1 min nudge, station rotation, append-only attendance (`session_attendance`), and per-activity dwell-time logging (`session_activity_log`).
- **Strict single controller, no exceptions** (confirmed decision, Assistant Coach handoff §1.3): whoever holds control is the only one who can advance, adjust the timer, mark attendance, or end the session — including the head coach; there is no "head coach always can regardless of controller" carve-out. This was already the shipped model (`isController` already gated every action) except for one place: **mid-run plan editing (`LiveEditBuilder`) is gated on role, not control** — an assistant who takes control still can't open it, unlike every other controller action. Fixed this session (`isController && amHeadCoach`, was `isController` alone).
- **Non-controller's action-button area** (where advance/±1min/timer normally render) now shows who's driving ("Coach X is running this") plus a deliberately low-emphasis (outline, not primary) "Take Control" button, in that exact spot rather than a blank area or a bold button — a bold button there risked an accidental tap seizing control from whoever's mid-drill. A brief top banner still shows a passive "Read-only" status; the actual tap target lives only in the controls area now.
- **Toast on control change**: the coach who just lost control gets a brief, non-blocking "Coach X took control" toast, so a habitual tap on Next/±1min a beat later is explained rather than silently doing nothing.
- Offline resilience: single-pending-write retry with backoff, distinguishes "network down" from "someone else moved the session" so a signal drop doesn't blank the screen.
- A coach can leave a live session without ending it — safe because the whole run state (timer, stage) re-derives from `current_phase_started_at` on mount, not from anything held only in memory.
- **"Turn On Audio?" prompt** (seventeenth session): audio defaults off and was easy to never discover via the small header speaker icon alone. A one-time popup now shows the moment any viewer — coach (`CommandScreen`) or anonymous helper (`HelperView`) — lands in a live session (`stage==="live"` for the coach, session-active for the helper), reminding them to turn their device volume up. Fires once per session id per mount (same guard pattern the station-leader warning modal already used); the two modals don't overlap (station warning waits for the audio prompt to be dismissed first).
- **Audio cues** (coach view only): a silent background `<audio>` loop + Screen Wake Lock keep the tab alive when the phone locks; real audio files play alongside or instead of `speechSynthesis` (`tennis-beep.wav` for the fixed 2-minute warning; time's-up is coach-selectable, see below). Time's-up plays the chosen sound, then a spoken **"Time"** once it ends (`audio.onended` triggers `speak("Time")`, reusing the same `speechSynthesis` call already used for "Two minutes remaining" — not baked into the audio file, since that's real browser TTS, not a recording). `HelperView` (parent-facing) is still speech-only — not yet extended with the sound+"Time" treatment.
- **Live Practice Audio settings** (2026-07-30, Settings screen): the coach picks a time's-up sound (Whistle/Buzzer/Ding/Beep — `public/audio/{whistle,gym-buzzer,ding,beep}.wav`, the latter two newly synthesized) and an announcer voice, each with a tap-to-preview. Stored in `localStorage` (`AUDIO_CUES`/`getAudioCuePref`/`getVoiceURIPref`/`resolveVoiceByURI`/`loadVoices`, `constants.js`), not the database — deliberately per-device, since available `speechSynthesis` voices differ entirely by browser/OS and a voice chosen on one device may not exist on another. Voice selection is a **full picker**, not a Male/Female toggle: an initial version guessed gender via a name-based heuristic (the Web Speech API has no real gender metadata) and just took the first match, which surfaced a dated, robotic-sounding voice ("Daniel") ahead of much better ones the heuristic's hint list didn't know about — there's no way to infer voice *quality* from a name. Replaced with a real list of every voice installed on the device (`loadVoices()` handles `getVoices()`'s async population via `voiceschanged`), each with a preview button, remembered by the specific `voiceURI` chosen. **Currently narrowed to just Samantha/Daniel** (`CURATED_VOICE_NAMES` in `SettingsScreen.jsx`) after Jax tried the full device list and found every option "simply awful" except those two — a stopgap until real recorded voice clips replace `speechSynthesis` entirely for the fixed phrases the app speaks (see Known Gaps).
- **Notes, captured live**: a "Quick note..." bar during the run and an "End of Practice Notes" textarea on the completion screen (both pre-existing, `createNote`/`notes` table) now support typing `@` to tag a player — a picker filtered to the roster inserts the name into the text and records the tag structurally (`note_player_tags`), surfaced later in Goals & Insights and on `PlayerProfile`. See the Notes section below for the full model.
- **Station leader assignment** (2026-07-29): `stations.team_staff_id`/`helper_name` already existed but had no visibility before a practice started and no safety net once it did. `get_preview_view` now also returns `can_manage` (server-computed off `auth.uid()` via `can_coach_team`, since `/preview/:token` is the same route/component for both a signed-in coach and an anonymous shared link) and, only when true, the team's `team_staff` roster, each station's real `id`, `practice_id`, and the caller's own `my_coach_name` — `PreviewView` uses this to show "No leader assigned -- tap to assign" per station plus a "Share Setup Link" button, reusing `updateStationLead`. Separately, in the live `CommandScreen`, the first time a session reaches `stage==="live"` after attendance, a one-time warning modal lists any station_block stations still lacking a leader, letting the coach assign one, share the live link, or explicitly leave it unmanned. Home's hero presents "Practice Setup" and "Review Plan"/"Start Practice" as co-equal side-by-side buttons for a planned practice (sized `blg` with `minWidth:0` — an earlier version used two `bxl` buttons, which overflowed the viewport on phone-width screens since flex items default to `min-width:auto` and can't shrink below nowrap text's own width). `PreviewView` also gained a "Back to Home" link and a persistent "Start Practice" bar (both `can_manage`-only) — previously a coach reaching Practice Setup from Home had no way back except the browser's own Back button, and no way to actually start the run from that screen.
- **"Who has this open" presence** (2026-07-29): a small live badge on `PreviewView`, the staff `CommandScreen`, and `HelperView` showing which coaches currently have a practice's setup/live view open and how many anonymous viewers there are. Pure Supabase Realtime Presence (`subscribeToPracticePresence`, `src/supabase.js`) — no new table, no RLS, just ephemeral client-to-client state over the same websocket infra `subscribeToLiveSession` already uses (a *public* channel, not the newer "private channel" feature, so no extra Realtime authorization config was needed). Keyed by `practice_id` (not `live_session_id`) specifically so a coach still in Practice Setup and a helper who already opened the live link show up on the *same* channel — required adding `practice_id` to `get_live_session_view`'s output too (it never returned one before). Each client tracks `{kind:'coach',label}` or `{kind:'anon'}`; verified end-to-end with two real anonymous browser tabs on the same preview link — the badge correctly showed "2 people watching" and dropped back to "1" the instant the second tab closed.

### Notes (Assistant Coach handoff §2)
- Two grains: per-drill/station (`practice_activity_id`/`station_id` set) and end-of-practice (both null). Backed by the `notes` table (existed since 2026-07-14 for the staff-facing capture UI above) plus a new `note_player_tags` join table for @mentions.
- **Anonymous helper composer reworded** (seventeenth session): the "End-of-practice note" checkbox (checked = general, unchecked = tagged to the current drill) is now a two-option toggle showing the actual choice — the current drill's name vs. "General" — same underlying `endOfPractice` boolean and data model, just a clearer control than a checkbox whose two states didn't read as a real choice.
- **Unreviewed-note red dot** (seventeenth session): `notes.viewed_at` (nullable, `20260730020000_note_review_tracking.sql`) plus a `has_unviewed_notes` flag added to `get_team_session_history`. Goals & Insights' History list shows a red dot per session with any non-archived, unviewed note; opening that session's detail as the head coach marks its notes viewed automatically (gated on `canManage` specifically, not any viewer, so an assistant/helper glancing at read-only history doesn't silently clear the head coach's unread indicator); a "Mark all as viewed" action clears every visible session at once. Marking is a plain client-side update against `viewed_at`, covered by the same `notes_update_archive` RLS policy `archiveNote` already uses — no new RPC needed for either the single-practice or whole-team case.
- **Anonymous helpers can now write notes too** — a first for this app; every other anonymous surface (`/preview/:token`, `/live/:token`) is read-only by design. Token-validated server-side (`submit_practice_note_by_token`, reuses the existing `validate_token()` — never a client-asserted author), with a flat 40-notes-per-token rate limit and a 500-character cap (both confirmed defaults, not assumed). `HelperView` got a new "Notes" panel (there was no note UI there at all before) with the same @mention picker, using the same minimized roster (first name + last initial + jersey) helpers already see everywhere else, plus an optional freeform name field (`author_label` — display only, never trusted as identity).
- Archive-don't-delete: `notes` had no UPDATE policy at all before this session (comment called it "quick-capture-only") — added one, gated the same as read/insert (`can_access_practice`).
- Surfaced in `SessionHistoryDetail` (Goals & Insights, grouped by drill/end-of-practice, author+role shown, tagged players as chips) and, new, on `PlayerProfile` ("Practice Notes" section, reverse-chronological across every practice this player was ever @mentioned in, via `fetchNotesForPlayer`).

### Helper/parent-facing (anonymous, token-based, no login)
- `/preview/:token` — pre-practice setup view (equipment, roster, countdown to start).
- `/live/:token` — live view during a run (current drill/station, roster, optional attendance-marking scope), now also a note-writing surface (see Notes above) — the first anonymous write path in this app. For a group/station drill, each player is shown as a tappable chip; tapping reveals that player's per-category focus note (if the coach has set one) alongside the drill's own skill tags — resolved server-side inside `get_live_session_view` (tag names via `library_activity_id → drill_tags → skill_tags`, notes via that tag's `category_id → player_focus_areas`, team-scoped), since the anonymous helper has no client-side access to `activityLibrary`/`skillTags` the way the staff view does.
- One-tap **Practice Setup View** on Home's next-practice hero card, for any signed-in coach (head coach or assistant): finds/creates a preview token for that practice and opens the same `/preview/:token` page a shared link would, reusing the identical component/query rather than a second one. Always visible once a plan exists, deliberately not time-gated the way the anonymous link is — that gating exists only because the anon link has no auth, which doesn't apply to a signed-in staff member.
- Player data is minimized (first name + last-initial + jersey), everything else (equipment, coaching points, location) is shown in full — a deliberate privacy choice, not an oversight.

### Public landing page, feedback, and legal
- `/` is adaptive on session state: signed-out renders `LandingPage`, signed-in renders the app — deliberately not a hard route split, so an installed PWA icon (`start_url: '/'`) keeps launching straight into the app.
- Landing page is a full 14-section product-walkthrough marketing site with real app CSS-class-built visuals (not screenshots, not stylized mockups) using realistic sample data — pixel-consistent with the actual UI by construction.
- `/terms`, `/privacy` render final, attorney-cleared copy via a single catch-all `vercel.json` rewrite.
- In-app feedback is authenticated only (Home "?" → "Send Feedback"). The landing page's own anonymous feedback form was removed; `submitPublicFeedback`/`feedback` table were kept in place, unused, in case a future non-authenticated contact path wanted them -- **no longer dead code as of the twentieth session**: the Settings/FAQ "Request a Consultation" form (`ConsultationRequestForm`, `LegalPages.jsx`) reuses both `submitFeedback` (authenticated) and `submitPublicFeedback` (anonymous), exactly the future contact path this was speculatively kept around for.

---


## Database & permissions (architecture, not a live snapshot)

Point-in-time row counts and table counts age out fast on a system under active development — the last full live-DB introspection was 2026-07-21, and several sessions have touched the schema since (the org-model rework and Coach/Org mode work alone add and drop tables/columns). What's stable and worth keeping here is the **architecture**, not a specific count. Run a fresh introspection against the live project before trusting any specific number.

**Ownership patterns**
- **Team-scoped**: practices, stations, session data — access via `can_access_team`/`can_manage_team`, which check `teams.owner_user_id`, org membership (`teams.organization_id` + `is_org_member`/`is_org_admin`), or a non-archived `team_staff` row.
- **Coach-or-org ("owned")**: drills, assets, locations, templates, skill tags — `owner_user_id` XOR `organization_id`, checked via `can_access_owned`/`can_manage_owned` (the latter requires org-admin, not just org-member, for org-owned rows — keeps shared libraries curated).
- **Anonymous/token tier**: `session_access_tokens` (scope, expiry, revocation) gates `/preview/:token` and `/live/:token` entirely through `SECURITY DEFINER` RPCs — `anon` has no direct table grants beyond `REFERENCES/TRIGGER/TRUNCATE` on real tables. As of this session this tier can **write**, not just read, for the first time (`submit_practice_note_by_token`) — same pattern (token validated inside the function, `anon` still has zero direct grants on `notes`/`note_player_tags`).
- **Platform admin**: `admin_users`/`is_admin()` is a flat, separate superadmin gate (founder metrics, public-catalog curation rights) — unrelated to org roles.

**RLS conventions used everywhere**
- Almost no DELETE policy anywhere — archive via `archived_at`, never hard-delete through the app. Exceptions: join tables where removing an association isn't destroying history, and one explicit audit-adjustment delete policy on `session_activity_log`.
- Actor-identity columns (`created_by`, `marked_by`, `noted_by`, etc.) are enforced via `WITH CHECK (col = auth.uid())`, never trusted from the client.
- `service_role` bypasses RLS entirely but **not** base-table GRANTs — every new Edge Function or import script that queries app tables directly (rather than through an RPC, which runs as the function owner) needs its own explicit grant. This has bitten more than one session (first `profiles`, later `org_invites`/`organizations`) — check for it early if a new Edge Function mysteriously returns empty/errors.
- `practice_activities`/`template_activities` are full copies of the source drill's fields at add-time, never live references — editing a library drill or template later never retroactively changes anything already built from it. Same lineage-not-binding pattern applies to `templates.default_team_id` and `practices.template_id`.

**Legacy POC tables — dropped 2026-07-27.** `app_data`, `coaches`, and `live_sessions` (the old table) predated the current migration history entirely (created for the pre-rewrite POC, before Postgres RLS/auth existed in this app) and had carried their original wide-open `anon`-era RLS the whole time. Once `app_data`'s last client-side reference was traced and removed (see Session Log), all three were confirmed zero-reference in `src/`, and dropped via `20260727000000_drop_legacy_poc_tables.sql`. If you're looking for them, they're gone on purpose — see Session Log for the verification trail before dropping.

---


## Known gaps / deliberately deferred (do not build without checking first)

- Hard/GDPR-style account deletion — still not started as an actual feature (the FAQ tells coaches to email support), but several real pieces of the "FK-cascade ordering issue" this entry used to gesture at vaguely are now confirmed and fixed: `practice_series.created_by`, `organizations.created_by`, `feedback.user_id`, `practice_live_sessions`' actor columns (all earlier sessions, all the nullable + `ON DELETE SET NULL` actor-identity pattern), and — twenty-sixth session, found live while tearing down that session's own disposable test account — `activity_library`'s four lineage-pointer FKs (`practice_activities.library_activity_id`, `stations.library_activity_id`, `template_activities.library_activity_id`, `template_stations.library_activity_id`, all previously `no action`; fixed to `on delete set null` in `20260802050000_library_lineage_fk_actor_pattern.sql`, since all four are copy-at-add-time lineage pointers, not live content). **A new instance found the same session, not yet fixed, and deliberately left for a real product decision rather than a unilateral guess**: `session_attendance.player_id references players(id)` also has no `ON DELETE` action, so deleting a player (which cascades from deleting their team, which cascades from deleting their account) blocks on any session they were ever marked present/absent/left-early in. Unlike the lineage-pointer case, `SET NULL` isn't obviously correct here — `session_attendance` is itself the historical record (append-only, "current status = the most recent row"), and `get_team_session_history`'s own attendance count uses `distinct on (sa.player_id)`, which would silently collapse multiple different deleted players' rows into a single NULL group and undercount a session's attendance. Needs a real decision (cascade-delete the attendance rows entirely vs. some anonymized-but-still-counted representation) before fixing, not just an FK migration. Other tables may still have the same gap unaudited; every instance found so far was found by hitting it, not by a systematic sweep — worth an actual audit before this becomes a real self-service feature.
- Week-view / drag-to-reschedule, games/events, series-level default templates, moving a plan between practices, availability polling — explicitly out of scope for now.
- Any other parent/player-facing surface beyond the existing anonymous helper links — explicit product stance: "this is a coach tool, not TeamSnap."
- Push notifications/reminders.
- A second org role beyond `director` — proposed as `admin` in the original design, but `org_staff.role`'s CHECK constraint only allows `'director'` today, so it doesn't exist as a usable value yet. No billing model exists to define what a second tier would even gate. (An earlier, since-replaced org model had `coach`/`viewer` roles too — see Session Log's "Sharing & org model" — but those aren't part of today's `org_staff` table at all, not just unenforced.)
- Automated test suite — every feature is verified empirically in a live browser against production with disposable Supabase test accounts, by design (no local Supabase environment in this setup).
- ~~Dropping the legacy `app_data`/`coaches`/old `live_sessions` POC tables~~ **Done 2026-07-27** — see Session Log.
- `copyDrillToMyLibrary` copying skill tags onto the copy — currently skips tags entirely regardless of scope.
- ~~Equipment visibility for a peer-shared drill~~ **Fixed, same day** — `assets_select_access` extended with the same `is_library_peer`/`is_private` branch already used for drill visibility (`20260801090000_assets_peer_sharing_rls.sql`). `can_link_asset_to_practice_activity`'s own comment about this class of limitation for personal-team co-coaches is now stale for the specific peer-sharing case; it may still apply to a plain (non-Permissions-linked) co-coach scenario, not re-verified.
- ~~Two equipment-mismatch dialogs~~ **Done, twenty-fifth session continued** — see Session Log. Now fully closes the loop for every sharing source (org, Public Library, and peer), following the `assets_select_access` fix above.
- Pro+/plan-based gating on who can be granted "Share Practice Planning" — the delegation mechanism itself (twenty-fifth session) is real and RLS-enforced, but whether granting it requires the assistant to be Pro/org-affiliated (unless the head coach has Pro+) is not wired to anything, consistent with `src/entitlements.js` staying fully inert until real billing exists.
- A real, app-writable "add a skill category" flow — `skill_categories` is service-role/SQL-editor seeded only. 6 of the app's 12 sports have categories today (Baseball, Basketball, Football, Soccer, Lacrosse, Volleyball); Softball, Hockey, Tennis, and Swimming have none — no proposal or seed exists for them, this just hasn't been done yet.
- ~~Baseball/Basketball skill-tag duplication~~ **Fixed 2026-07-27** — see Session Log.
- Anonymous helpers can tag a note to a drill (`practice_activity_id`) but not a specific station within a station block (`station_id` is accepted by `submit_practice_note_by_token` but `HelperView`'s composer never passes one) — deliberately kept simple for v1, not a hard limitation of the schema.
- `PlayerProfile`'s new "Practice Notes" section shows the note text/author/date but not which practice it came from — fine for now with one team's worth of history, would want a practice reference once there's enough volume that "which practice was this" becomes a real question.
- `HelperView`'s notes panel is write-only by design (matches the confirmed scope: staff-only screens are where notes get read back) — a helper gets a "Note added" confirmation flash, never a list of what they've submitted.
- **Replace `speechSynthesis` with recorded human audio for the live-practice voice cues** — every synthetic voice tried (a name-based Male/Female heuristic, then the full device voice list) fell short; Jax is arranging real recordings of the four fixed phrases the app ever speaks (`"Two minutes remaining."`, `"Time"`, `"Next up!"`, `"Audio on"`) to send as `.wav`/`.mp3` files. Once received: add them as static files (same pattern as `whistle.wav` etc.), likely as one or more new selectable "voice" options alongside/replacing the current curated Samantha/Daniel `speechSynthesis` picker (`LivePracticeAudioSection`, `SettingsScreen.jsx`) — probably means `beep()`/`speak()` in `CommandScreen.jsx` branching on whether the chosen "voice" is a recorded-clip set or a `speechSynthesis` voice, since a recorded clip can't take arbitrary text the way TTS can (fine here, since only ever these 4 fixed phrases are spoken).
- **Migrate transactional email off Resend's free tier to Google Workspace's SMTP relay** — not urgent, but worth doing soon. Confirmed (research + a second check against Jax's actual account): included at no extra cost on the paid Workspace plan already in use, official per-user limit 10,000/day vs. Resend free tier's ~100/day. One real catch: **new Workspace tenants are capped below that full limit until $100 of billing spend and 60 days of account tenure have passed** (Google's standard anti-abuse ramp-up for new accounts, not anything specific to this setup) — but even under that ramp-up cap, it's still roughly 5x Resend's current ceiling, so it's a strict improvement even before the account "matures." Two separate integration points, not one: Supabase Auth's SMTP setting (dashboard-only, no CLI/API access from this environment) and the two Edge Functions that call Resend's API directly (`notify-team-staff-added`, `notify-org-invite` — code Claude Code *can* edit once Jax has SMTP-relay credentials to hand off via `supabase secrets set`). Use "Require SMTP Authentication" in the relay setup, not IP allowlisting — Supabase's outbound sending IPs aren't static/publishable.
- ~~`org_create_team` still auto-adds the creating director as `team_staff` head_coach~~ **Fixed (Phase 2 follow-up)** — see Session Log and the Organizations section above.
- ~~A large org/library/equipment restructuring queued~~ **Done, Phase 2 (seventeenth session continued)**: `org_create_team`/`canManageTeamInMode` retrofit, org multi-sport, org equipment-at-location auto-merge, Explore moved under Drills as "Team Libraries" with a per-coach breakdown, Club Library's colored bar, the per-team Equipment tab dropped in Org mode, and `copyDrillToMyLibrary` mode-awareness — see the Organizations and Library sections above and this session's Session Log entries for each. Multi-org library *switching* specifically reuses Home's existing org picker (no new UI needed) rather than being a separate feature, per the decision that scoped this pass.
- ~~Org mode silently reverts to Coach mode on a fresh full page load~~ **Fixed (Phase 2 continued)** — see Organizations section above.
- A director-facing, cross-team version of Development Pulse (Org mode) — explicitly out of scope for this pass; the shipped card is Coach mode only, one team at a time, and should not be reused with an arbitrary/rolled-up team for Org mode.
- `session_attendance.player_id`'s missing `ON DELETE` action (noted above) is still not fixed — a second, unrelated instance of a similar gap on `session_activity_log.practice_activity_id`/`.station_id` *was* found and fixed this session (see Goals & Insights/Development Pulse above), but that fix doesn't touch this one; still needs the same real product decision before a migration is safe.

---


## Working conventions

- No client-side insert loops for bulk operations — always one atomic, server-capped RPC (`create_practice_series` is the model to copy).
- "Archive, don't delete" (`archived_at`) almost everywhere; see RLS conventions above for the exceptions.
- Migrations are always dry-run (`BEGIN;...;ROLLBACK;`) before applying for real, and applied via `npx supabase db push` (the CLI is linked in this environment). If a migration is ever applied via the raw Management API instead, run `migration repair --status applied` before the next `db push --dry-run`, or the CLI will misjudge it as pending.
- Testing pattern for anything requiring a signed-in user: a disposable Supabase test account via the Admin API's `generate_link` + `verifyEmailOtp`, verify via direct SQL, delete after. Never test destructive flows on the real coach's account. Note: `generate_link` and the app's own "Send Code" share a rate-limit bucket.
- Sign-in email rate limit: `auth.rate_limit.email_sent` in `supabase/config.toml`, currently 30/hour — this is the limit that specifically governs custom-SMTP-sent emails (its own comment says so), not something Resend bypasses. The real ceiling in practice is likely Resend's own account-level plan cap (the project is on Resend's free tier per a code comment in `_shared/emailTemplate.ts`), since that applies across sign-in codes *and* the invite-notification Edge Functions combined — check the Resend dashboard directly for the current number rather than trusting either Supabase setting as authoritative.
- Timezone: always compute/display via the team's own `timezone` column, through `scheduledAtToTeamLocal`/`teamLocalToScheduledAt` — never the viewing device's zone, never bare UTC truncation (`localDateStr()` exists specifically to avoid the UTC-rollover bug described in Session Log).
- Secrets never go into migration files (git-tracked) — Vault or Edge Function secrets only. A `pg_net`-triggered function needs `verify_jwt:false` plus a shared-secret header.
- `.upsert()` still needs an UPDATE grant even for `ON CONFLICT DO UPDATE` — use `{onConflict, ignoreDuplicates:true}` when a table's RLS is insert/delete-only by design.

---



## Gotchas (read before you re-debug something already debugged)

This section exists so a root cause, once found, never has to be found twice. Two kinds: real bugs whose *pattern* is likely to recur, and operational/tooling quirks specific to this environment.

### Bugs whose root cause is worth remembering

- **Drill/equipment tags "not persisting"**: `syncDrillTags`/`syncDrillEquipment` never checked `.error` on insert/delete, so a rejected write (e.g. a failed RLS check) looked identical to success — the modal closed and data silently reverted. Not a broken permission model; purely missing error surfacing. Fixed by propagating `{error}` through the whole chain and keeping the modal open with a visible error on failure.
- **"Today" rolling over early for western-hemisphere coaches**: `new Date().toISOString().slice(0,10)` converts to UTC before truncating, which rolls "today" to the next calendar day hours before local midnight west of Greenwich (surfaced by a Phoenix, UTC-7 coach). Fixed with `localDateStr()` (same local-getter construction Month view's grid already used correctly) swapped in everywhere the UTC pattern appeared.
- **Team creation broken for any coach with no last name**: the auto-head-coach trigger copied `profiles.last_name` (optional) straight into `team_staff.last_name` (`NOT NULL`), silently rolling back the whole `teams` insert with no UI error. Fixed with `coalesce(...,'')`.
- **Schedule duration field un-typeable**: `+e.target.value || 60` meant clearing the field to type a new number evaluated to `60` on every keystroke (`+"" || 60`), so a coach could never get past the leading digit. Fixed by allowing a transient empty-string state, defaulting only on blur/submit.
- **iOS auto-zoom on some inputs but not others**: the shared input font-size was 15px, one pixel under the 16px threshold where iOS Safari auto-zooms on focus. Bumped to 16px app-wide.
- **`org_staff` member list silently empty**: the table has two FKs to `profiles` (`user_id`, `invited_by`), so a bare `profiles(...)` PostgREST embed was ambiguous and errored — silently swallowed by the function's own `catch`, returning `[]`. Fixed with an explicit `profiles!org_staff_user_id_fkey(...)` hint.
- **A director seeing a stranger's pending org invite**: `org_invites_select`'s RLS deliberately lets a director see every pending invite for orgs they administer (needed for a sent-invites list) — but the client rendered all of those as if addressed to the viewer. Fixed by filtering client-side to the caller's own verified email, matching the same server-side-truth principle the accept/decline RPCs already use.
- **The under-planned pill's warning glyph**: `⚠` (U+26A0) renders with its own built-in emoji-yellow color in most browsers regardless of the CSS `color` applied to it — looked like a permanent yellow warning triangle no matter the pill's actual state. Removed; the red under-planned state deliberately has no icon now. Don't reintroduce one there without checking it isn't in the emoji-default-presentation set.
- **`useBlocker` + an unconditional per-render effect is a real infinite-loop risk**: PlayerProfile's leave-prompt work added `useBlocker` alongside a *pre-existing* effect that re-registered `subViewBack` on every render (no deps, safe on its own for years) — combined, they fed back into each other and threw React's "Maximum update depth exceeded" the instant the screen mounted. If you add `useBlocker` to a screen that already has any unconditional every-render effect touching shared/context state, register that effect once (a ref for "always see the latest closure" instead of "re-run every render") rather than assuming the existing pattern is still safe.
- **A modal fed a snapshot object at open time goes stale the moment its own action refetches data** (twenty-ninth session, `PermissionsModal`): `App.jsx` held `permissionsCoach` (state) as the *whole coach object*, captured once when a roster row was clicked. Toggling a permission called `refreshTeams()`, which correctly updated `data.teams` with a brand-new coach object reflecting the new value — but `permissionsCoach` itself was never reassigned, so the already-open modal kept rendering the old object's old value. The toggle only looked "stuck" until the coach closed and reopened it (which re-derived a fresh object from the click). Fixed by storing just the id and deriving the live object from `team.coaches` on every render instead. **Any `openX(fullObject)`-style modal/panel state that's meant to stay open across its own refetch should hold an id and re-derive, never the object itself** — check for this pattern (`useState` seeded from a list-item click, no re-derivation afterward) before shipping a new modal that mutates the thing it's displaying.
- **A second, confirmed instance of the exact same pattern above, found live during the twenty-sixth session's Execution Scorecard verification — no `useBlocker` involved this time, so the bug isn't specific to that hook.** `SessionHistoryDetail`'s own `setSubViewBack({onBack})` effect depended on `onBack` directly, and `onBack` is a fresh closure every time `GoalsScreen` renders (`onBack={() => setOpenSessionId(null)}`) — so the effect re-ran on every render, called `setSubViewBack` with a *new* object every time (that state lives in `Layout.jsx`, an ancestor), which re-rendered Layout and everything it wraps including this route, which recreated `onBack` again, forever. This one had apparently been dormant/borderline for a while (the component predates this session) — the extra state-driven re-renders this session's new scorecard fetch introduced were seemingly what tipped it into an actual runaway (confirmed via a render counter: climbing unbounded within a second of opening any session, not merely "a bit more than before"). Fixed the same way as PlayerProfile's version: a ref holding the latest `onBack`, registering the effect once on `[setSubViewBack]` alone. **The general lesson, now confirmed twice**: any effect in this codebase that takes a freshly-created callback prop (an inline arrow function passed down fresh every render) as a dependency, and writes that callback into shared/ancestor state, is a latent infinite-loop risk regardless of whether `useBlocker` is anywhere nearby — audit for this shape specifically (`useEffect(() => { someAncestorSetter({callbackProp}) }, [..., callbackProp])`) before assuming an existing screen is safe to build on top of.
- **A report's "Planned" bucket means different things in different RPCs on purpose here — mixing them silently produces a real, confusing UI bug, not just a documentation nit.** `get_team_goal_report`'s Planned is forward-looking (currently *scheduled* practices); the new `get_team_goal_trends`' Planned is backward-looking (what was actually saved for each *completed* session, so it compares the same cohort as Actual). The first draft of `GoalTrendCard` mixed the two — showing the *report's* forward-looking Planned percentage as the card's headline number, right next to a chart plotting the *trends RPC's* backward-looking Planned line — which live-verified as a real "Planned 0%" summary sitting directly beside a chart visibly showing ~55% planned. Fixed by deriving the card's own rolling summary from an average of the same weekly data the chart already plots, never touching `get_team_goal_report` for anything Trends-specific. If a future report needs *both* concepts on screen at once, label them distinctly (e.g. "Currently Scheduled" vs. "Historical Average") rather than calling both just "Planned."
- **Navigating straight to `/builder/:practiceId` via a bare `navigate()` (instead of the existing `goToBuilder()` helper) silently loads the wrong practice — a real bug, found live while wiring Development Pulse's Builder deep link, not merely a hard-refresh quirk.** `BuilderScreen`'s local state (`existingId`, `teamId`, `acts`, `locId`, etc.) is seeded via `useState(editP ? ... : ...)`, where `editP = data.practices.find(p=>p.id===editPracticeId)` and `editPracticeId` lives in top-level context (`App.jsx`). `BuilderRoute` only sets that context from the URL's `:practiceId` param inside a `useEffect` — one render tick *after* mount — specifically because every existing entry point (`goToBuilder(practiceId)`) already sets it synchronously before calling `navigate()`, so this effect is meant only as a direct-link/refresh fallback. A caller that skips `goToBuilder` and calls `navigate("/builder/"+id)` directly lands on `BuilderScreen`'s first render with `editPracticeId` still `null` — `editP` is `null`, and every one of those `useState` initializers locks in "blank new practice" defaults that React then never re-derives, even once `editPracticeId` correctly updates a moment later (a `useState` initializer only runs once, on mount). The route and URL are both correct; only the rendered content is wrong (today's date, no scheduled duration, empty Run of Practice) — easy to miss unless you specifically check the practice's real saved fields against what's rendered. Fixed by extending `goToBuilder()` with an optional `navState` param (forwarded straight into `navigate()`'s own `{state}`) rather than adding a second, parallel way into Builder. **Any future deep link into Builder must go through `goToBuilder()`, never a bare `navigate("/builder/"+id)`.**
- **`SortableActivityRow`'s per-row stacking context traps any popover inside it, and the fix generalizes beyond this one component.** Direct feedback: a drill's ellipsis dropdown menu in the Library's drag-sortable "My Drills" list was visually clipped by the *next* row's opaque background — including on the *last* row in the list, ruling out a simple "menu extends past the container" theory. Root cause: `SortableActivityRow` (`ActivityConfigs.jsx`, shared by Builder and the Library drill list) gives every row `position:relative` plus an explicit `zIndex` (at minimum `1`, even when not dragging/sticky) — that combination always creates a new CSS stacking context, and every row gets the *same* zIndex level, so z-index comparisons for something deep inside one row (a `.mini-menu` at `z-index:50`) only resolve *within that row's own stacking context* and can never escape to paint above a sibling row, which is a separate context painted after it in DOM order regardless of any z-index set inside the first one. No amount of raising the menu's own z-index fixes this — the trap is one level up. Fixed with a new `raised` prop on `SortableActivityRow` that bumps that specific row's own zIndex above its siblings while something inside it needs to render over what comes after it (`zIndex:10` when raised, vs. the normal `1`). Any future popover/dropdown living inside a `SortableActivityRow` (or any other component using this same "give every item a zIndex for legitimate reasons" pattern) needs the same treatment — check for exactly this shape before assuming a z-index bump on the popover itself will work.

### Operational gotchas worth not re-learning
- `supabase config push` (verified against the actual Supabase CLI Go source, not guessed) unconditionally overwrites only `site_url`/`additional_redirect_urls`; everything else (SMTP, email templates, MFA, rate limits) is nil-gated and untouched unless locally set in `config.toml` — check those two values are still correct production values before ever running this, not stale localhost/dev placeholders. Piping `"n"` to its interactive per-service confirmation prompts did **not** reliably decline in past testing — treat any piped-input dry run against this CLI with suspicion. Its per-service `up_to_date` output on a completed push is a **success** status, not a "nothing changed" signal — don't read it as a no-op.
- `service_role` bypasses RLS but not base-table GRANTs (see RLS conventions above) — this has caused silent empty results from more than one new Edge Function.
- No automated test suite exists anywhere in this project, by design — there's no local Supabase environment in this setup, so everything is verified empirically in a live browser against production, using disposable test accounts, before being called done.
- Edge Functions are **not** auto-deployed by pushing to `main` (only the Vercel frontend is) — `npx supabase functions deploy <name>` is required any time an edge function or a shared file it bundles (`_shared/*.ts`) changes. Every function that imports a changed shared file needs its own redeploy; they each bundle their own copy at deploy time, not a live shared reference.
- `read_console_messages` returned an identical stale error after a real fix landed, across a full dev-server restart and several genuinely fresh page loads (confirmed via `script[type=module]` src querystrings) — don't treat it as authoritative when re-verifying a fix in a long-running tab/session. Cross-check with an independently in-page-patched `console.error` counter (or a fresh screenshot/interaction check) before concluding a fix didn't work. **Reconfirmed useful this session** for a different reason too: the counter technique is also how a real infinite-render-loop bug (see Gotchas above) was confirmed as *actually looping* rather than stale/buffered output — a raw error count climbing across two back-to-back reads, immune to any buffering question, is a stronger signal than either a screenshot or the console-messages tool alone.
- **`supabase db diff --linked` and `db push --dry-run` don't do what their names suggest in this environment.** `db diff`/`db push --dry-run` both require a local Docker shadow database to actually diff or dry-run SQL against (`Cannot connect to the Docker daemon`) — with no Docker in this setup (same "no local Supabase environment" constraint as everywhere else in this file), `db push --dry-run` silently degrades to only checking the CLI's own applied-migrations bookkeeping table, never actually executing a single line of the pending SQL. The real dry-run mechanism this project's own Working Conventions ask for (`BEGIN;...ROLLBACK;`) needs a different command: `supabase db query --linked --file <path>`, which runs arbitrary SQL directly against the linked production database via the Management API — wrap a migration file's contents in `BEGIN;`/`ROLLBACK;` in a scratch file and pass that. This also validates runtime correctness, not just syntax: adding `set local role authenticated; select set_config('request.jwt.claim.sub','<a real user's uuid>',true);` before calling a `security definer` RPC lets you exercise its real `can_access_team`/`can_manage_team` authorization checks and see real output against real data, still safely rolled back. Twenty-sixth session used this to validate all five new RPCs against the real coach's own production data before ever running `db push` for real.

---



## Persistent design themes (longer-running decisions, not single sessions)

These track *why* something is shaped the way it is, across many sessions — read the numbered sub-entries in order where present, since the design evolved.

### Auth
- Email OTP chosen over magic links specifically because magic links broke for the installed-homescreen-PWA sign-in case. `admin.inviteUserByEmail` is avoided for staff invites for the same reason (it's a magic link) — a custom `pg_net` + Resend Edge Function handles that notification instead.


### Skill tags & taxonomy
- Skill tags are seeded as **personal** `scope='coach'` copies per coach rather than true `scope='global'` rows: global rows are only writable by `service_role`/`is_org_admin` under existing RLS, so one coach archiving a shared global tag would silently remove it for every other coach. Per-coach seeding sidesteps that with zero RLS changes, reusing the machinery that already existed for a coach's own custom tags. `ensureDefaultSkillTags` re-runs (idempotently) on every sign-in so a coach picks up new tags from later migrations, not just what existed at signup.
- `skill_categories` has no app-level INSERT policy anywhere, by design — Jax seeds/curates these directly. This is why 4 of the app's 12 sports (Softball, Hockey, Tennis, Swimming) still have zero taxonomy: nobody has seeded them yet, not a technical blocker.
- A unified taxonomy and public-catalog drill content were drafted for a 6-sport initial pass (JSON-per-sport deliverable, so 150+ drills could be imported mechanically rather than hand-transcribed) with category names deliberately reused across sports on purpose (enables cross-sport reporting). A system publisher profile (e.g. `content@runofpractice.com`) was the chosen design for content attribution over relaxing ownership constraints — real `profiles` row means the existing `activity_has_owner` constraint needs no changes, and gives real "Published by Run of Practice" attribution for free. **This plan is what became the shipped Public Library feature** (see Library section above) for Baseball, Basketball, Football, Soccer, Lacrosse, and Volleyball — the original planning doc undersold this by never being updated to say so. Softball/Hockey/Tennis/Swimming were never part of this pass and still have no categories, tags, or public-library content.


### Sharing & org model (schema evolved twice — read in order)
1. **Original design (2026-07-06)**: a single `shared_with_organization_id` column on `activity_library`/`templates` — private by default, share with exactly one org, org-owned "Org Library" kept as a separate concept from coach-owned-shared-into-org "From Our Coaches." Public visibility explicitly deferred entirely (became the Public Library work above). The "copy, don't reference" rule was set here specifically to avoid a shared thing referencing something its viewers can't see (this bug class was hit five times during the original schema build) — copying a shared drill resolves equipment by name into the recipient's own pool rather than pointing at the sharer's rows.
2. **First org-model implementation (2026-07-21)**: `organization_members` (owner/admin/coach/viewer, zero real rows ever) was replaced wholesale by `org_staff` (single `director` role, extensible without a migration later). New RPCs (`org_create_team`, `org_assign_team_staff`, `org_assign_player`) were added as explicit auditable entry points, even though the underlying RLS already permitted an org-admin's direct write — deliberately not narrowed, since narrowing wasn't asked for. The single-org `shared_with_organization_id` column was recognized as insufficient the same day (a coach can be director of one org while coaching a team in a different org — a single column can't hold "share with more than one"), so it was replaced by the `activity_library_org_shares` join table before this design ever shipped as originally spec'd. `org_invites` got a real accept/decline consent flow (never the informal `team_staff` auto-link pattern) specifically because joining an org is a bigger commitment than being added to a team roster.
3. **Rework into Coach/Org mode (later session)**: the standalone Org Home page (`/org/:orgId`) was retired and folded directly into `HomeScreen.jsx` behind a persisted Coach/Org toggle — this is why `fetchPendingOrgInvites`/`orgAssignTeamStaff`/`orgAssignPlayer`/`promoteDrillToOrgLibrary` ended up as dead exports (superseded by different call patterns) cleaned up in the thirteenth session above. Team/asset/location/skill-tag creation inside Org mode reuses the same coach-or-org ownership pattern the rest of the app already had (`createOrgLocation`/`createOrgAsset`/`createOrgSkillTag` — no new RPCs needed, existing RLS already permitted a director's direct insert with `organization_id` set).


### Navigation (restructured multiple times — read in order)
1. Home originally had a pill/chip row for picking a team — removed entirely (not reskinned) once direct user feedback said it read as a filter control (tap to toggle in place) rather than navigation (tap to leave the page). Replaced by a dedicated global **Teams** tab, reachable from anywhere including Library.
2. The team workspace went from a 4-tab shape (Schedule/Plan/Goals/Team) to a 3-tab shape (Schedule/Plan/Team, Goals folded into Plan as a sub-toggle) — Plan's original scope had never actually been built past a placeholder, so nothing real was lost merging it in. Team Equipment/Player Gear moved out of global Settings into the Team tab and became sport-aware (auto-stamps the current team's sport, filters lists to `sport === team.sport || 'General'`) once showing all-sport gear while coaching one team's context was flagged as "weird." Skill Tags and Locations stayed in global Settings deliberately — they're coach-wide vocabulary/resources with no team ownership in the schema, so editing them from inside one team's context risked silently rippling to every other team.
3. Later flattened further: the nested Schedule/Plan/Team (with Plan hiding a Build/Goals toggle, Team hiding a Roster/Equipment toggle) became 5 direct top-row routes so nothing was two taps deep — then reduced to the current 4 (Schedule/Roster/Equipment/Goals & Insights) once Build's tab-bar entry was dropped as redundant with Home/Schedule's own "+ Practice" entry points (its route/component lived on as dead code until the thirteenth session).
4. The original `App.jsx` `view`-string state machine with manual `window.location.pathname` regex checks was replaced by real `react-router-dom` v6 routes; the `guardedSetView`/`priorView` unsaved-changes mechanism was replaced by `useBlocker` + `beforeunload` (the old mechanism never protected against a hard refresh or tab close either).
5. The old single "Manage" screen mixed coaching content and one-time setup in one junk drawer. Split: `SettingsScreen.jsx` (Account, Locations, Equipment & Gear, Skill Tags) for setup; Library's My Library/Explore structure for content; `ManageScreen` now purely serves the team workspace's Team tab. A critical catch made during planning, not after: the old Manage "My Teams" list held the app's only reachable "+ Team" button — it was rehomed onto the new Teams tab's header before the old list was deleted, not after.


### Landing page & legal
- No real product screenshots on the landing page, and no stylized mockups either — visuals are built from the real app's own CSS classes with realistic sample data, so they stay pixel-consistent with the actual UI as it changes, with one-line prop slots left for real screenshots later. Copy is deliberately sport/level-agnostic ("coaches"/"players", not "youth"/"kids"/"parents"), per an explicit positioning decision.
- An inline landing-page feedback form was removed after the user felt the copy read as AI-generated (em dashes, listy cadence) — em dashes and choppy list prose were removed project-wide on that page at the same time. The backing `submit_public_feedback` RPC/table were deliberately kept (see Library section) in case a future non-authenticated contact path wants them.


### Live sessions & timing
- The "leave a live session without ending it" feature was judged safe specifically because the live-run timer/stage was already fully re-derived from `session.current_phase_started_at` on mount (built earlier for multi-coach handoff/reconnect) — unmount/remount naturally resumes correctly, no separate persistence needed.
- Real audio-cue files via `<audio>` elements plus a silent background loop were chosen over an earlier, abandoned approach (a raw `AudioContext` oscillator tone) because Safari blocks Web Audio output but has a real background-audio exemption for actual media elements. The new tone was added strictly additively alongside the existing, working `speechSynthesis` cues first, specifically so a tone failing on some device wouldn't break the fallback — the redundant spoken "Next up!" cue was only removed later, after confirming the tone worked. `HelperView` (parent-facing) was deliberately not given the same treatment — the original complaint was specifically about the coach's own phone locking mid-session.
- `session_activity_log` has no stored/computed duration column (duration = `ended_at - started_at`, null if never explicitly ended) and no backfill/cleanup mechanism for a dangling open row from an abandoned session — flagged as needing a product decision (exclude? clamp to session end? clamp to now?) with no existing convention to follow yet.


### Goals & Insights architecture (twenty-sixth session)
- Every category-attribution calculation across Trends, Next Practice Guidance, Builder Goal Guidance, the Execution Scorecard, and Drill Insights is built on the same four small SQL functions and one composite type (`activity_minute_row`), extracted once from `get_team_goal_report`'s own logic rather than copied five more times — this project already paid for that mistake once (the `get_team_goal_report`/`set_team_goals` stale-column incident, seventeenth session) and the whole point of extracting shared functions instead of shared *documentation* is that a fix to the real logic only has to happen in one place. The functions are deliberately `security definer` with `revoke all from public` (not `grant ... to authenticated`) — they take a bare id with no authorization check of their own, trusting the outer RPC to have already verified access, which only works because PostgREST can't reach them directly once PUBLIC execute is revoked. Don't add a new one of these without the same revoke, or it becomes a direct, unauthenticated-relative-to-content data leak.
- Two genuinely different "Planned" concepts coexist on purpose (`get_team_goal_report`'s forward-looking vs. `get_team_goal_trends`'s backward-looking, completed-session-cohort version) — see the Gotcha above for the real bug this caused once already when a component quietly assumed they were interchangeable. Any future report that needs its own notion of "planned" should ask which cohort it actually wants before reusing either RPC's number by convention.
- Builder's projection math (`categoryMinutesForPracticeActivities`/`calculateProjectedGoalImpact`) is intentionally pure client-side JS, not a server round trip per keystroke — the baseline (rolling history) is fetched once per team selection and cached; every recalculation as the draft changes is a local `useMemo`. This is the same "don't send the unsaved draft to a new RPC on every change" instruction the original spec called for, and mirrors how Builder's own local `acts` state has always worked (nothing round-trips to the server until Save).
- The Execution Scorecard and Drill Insights share one on-plan tolerance constant (`ON_PLAN_TOLERANCE_SECONDS = 60`, `classifyDurationVariance`) specifically so "extended"/"shortened"/"on plan" means the same thing regardless of which screen a coach is looking at — a practice-level scorecard and a drill-level insights view giving different verdicts on the same underlying timing data would undermine trust in both.


## Session Log (chronological history — append new sessions here)

Every session's work gets one dated, numbered entry below, **newest first**. When you finish a session:

1. Write a new entry using the same voice as the ones below: what was asked, what was actually built (call out anything that turned out to already exist, or that you deliberately didn't build), any real bug found along the way (not just requested changes), and how it was verified.
2. Insert it at the **top** of this section (immediately below this instructions block), not the bottom — this list reads newest-first.
3. Update "Last updated" and the Status line at the top of this file to reflect the new latest session.
4. If the session changed a known gap, a working convention, added a real gotcha, or changed the feature inventory, edit that section directly — don't rely on someone reading the whole log to find out the app now works differently.

Sessions are grouped by theme rather than strictly re-sorted when a later session revisits an earlier decision — check the inline dates in each entry if exact ordering matters. Numbering is sequential per work session (a same-day follow-up is "Nth session continued," not a new number).

### Twenty-ninth session (2026-08-03) — margins fix, untagged-drills tagging flow, Development Pulse collapse/persist/new-insight-dot

Jax gave a 3-item feedback batch after using Development Pulse and Goals & Insights live: (1) Overview/Trends/History's side margins read too tight compared to sibling team tabs, (2) the Overview tab's untagged-time section should have a CTA into a filtered, tag-as-you-go view of the team's untagged drills, with a way back, and (3) Development Pulse should be collapsible with its collapsed state persisted across sessions, plus a small red dot when a new insight arrives while collapsed.

**Built:** (1) `TeamGoalsRoute` was the one per-team route missing the `16px 16px` wrapper Roster/Equipment both use — added it, removed the now-redundant duplicate bottom padding `GoalsScreen.jsx` had on its own top-level div. (2) A "Tag Untagged Drills" button (Overview, `canManage` only) navigates to `/library` with `location.state.untaggedForSport`/`teamId`/`returnTo`; `NewLibraryScreen.jsx` reads that once on arrival, scopes "My Drills" to the team's sport and (by default) to zero-tag drills, and shows a "Back to Goals & Insights" button. No extra plumbing was needed to "return to the untagged list" after tagging a drill — the filter is a plain derived value recomputed from `data.activityLibrary`, so a freshly-tagged drill just falls out of the list on its own next render, in place. (3) `DevelopmentPulseCard.jsx` gained a caret that collapses it to just its header; collapsed/expanded state and a "last-seen state fingerprint" (`teamId|state|categoryId`) both persist in `localStorage` keyed by `coachId` (one physical widget, not per-team); a small red dot shows next to the header whenever collapsed and the fingerprint has changed since last seen, clearing automatically the moment the card is (or becomes) expanded.

**One real, pre-existing bug found and fixed during live verification, unrelated to any of the three requests**: the `insufficient_history` state's goal-mix bar (`GoalMixStrip`) read `c.targetPct` (camelCase) off raw `report.skills` rows, which actually carry `target_pct` (snake_case) — every segment's width computed to `NaN`, so the bar rendered as nothing at all, and its `aria-label` read literally "Passing undefined%, Shooting undefined%." This has been broken since the twenty-eighth session shipped; nothing in that session's own verification matrix happened to exercise this specific state's graphic closely enough to catch it. Fixed by mapping to camelCase at the call site (`{skillCategoryId, name, targetPct}`), matching the prop convention every other graphic in the file already uses.

**Verified live**, one disposable coach account + team (real Soccer skill categories, real seeded goals, two library drills — one tagged, one not): confirmed Overview/Trends/History margins now match Roster's; confirmed the untagged CTA lands on a correctly sport-and-tag-scoped Library view, confirmed tagging the one untagged drill via the existing Edit → Skill Tags flow made it disappear from the list automatically with no navigation, confirmed "Back to Goals & Insights" returns to the right team's Overview tab; confirmed the Development Pulse caret collapses/expands, confirmed collapsed state survives a hard reload, confirmed the red dot appears after mutating team goals directly in the database while the card was collapsed and clears on expand, confirmed the goal-mix bar renders correctly (bar and aria-label both) after the fix. All test data (team, drills, tags, the one auth user) confirmed fully deleted via a residue query afterward; temporary setup/cleanup scripts removed from the repo.

**Continued, same day — Permissions toggle stale-state bug.** Jax reported "Share Drill Library" and "Share Practice Planning" (`PermissionsModal.jsx`) didn't visually reflect a tap until the modal was closed and reopened. Root cause (see the new Gotcha above for the general pattern): `App.jsx`'s `permissionsCoach` state held the *whole coach object*, snapshotted once when the roster row was clicked — toggling correctly called the right RPC and `refreshTeams()`, which updated `data.teams` with a fresh coach object, but nothing ever reassigned `permissionsCoach` itself, so the already-open modal kept reading the stale pre-toggle value. Fixed by renaming to `permissionsCoachId` (just the id) and deriving the live coach object from `team.coaches` on every render, so the modal reflects the real value the instant `refreshTeams()` resolves. Verified live with a disposable head coach + assistant pair (a real invite → accept relationship, not synthetic staff rows): opened Permissions on the assistant, toggled both "Share Drill Library" and "Share Practice Planning" on, confirmed both switches visibly flipped without closing the modal, then toggled both back off and confirmed the same in reverse. Both test accounts and their team fully deleted afterward.

### Twenty-eighth session (2026-08-03) — Development Pulse, a dynamic Home CTA for Goals & Insights

Jax provided a spec doc (`RUN-OF-PRACTICE-DEVELOPMENT-PULSE-HOME-WIDGET.md`) for a new Home widget: a single, dynamic card resolving to exactly one of 7 states, shown in Coach mode beneath the next-practice hero, with a bullet-bar graphic and one CTA that deep-links to the exact useful place — including opening Builder with Goal Guidance pre-expanded and a specific category highlighted. The spec's own explicit constraints: reuse the twenty-sixth session's shared goal-attribution math exactly (no "fifth variation" of goal math), avoid N+1 queries, no new charting dependency, named threshold constants, live verification before calling it done.

**Built:** one extended RPC (`get_team_goal_report` gained `usable_actual_session_count`, verified byte-identical to live via `pg_get_functiondef` before editing — not trusted to migration-history bookkeeping alone, per this file's own documented drift history) plus one new batch RPC (`get_teams_recent_completed_session(p_team_ids[])`, one call across every candidate team rather than per-team, used only when there's no next practice to short-circuit focus-team selection). Two pure helpers in `constants.js` (`resolveDevelopmentPulseFocusTeamId`, `resolveDevelopmentPulseState`) — presentation-neutral, deterministic, independently unit-tested (19 cases covering all 7 states, the aligned-but-moving-away override, tie-breaking, and focus-team priority — all passing). A separate `presentState()` in the new `DevelopmentPulseCard.jsx` translates a resolved state into copy/CTA, keeping the resolver itself free of JSX/routing concerns. Bullet-bar graphic, goal-mix strip, and completeness bar are all inline SVG — no Recharts, matching the Trends chart's existing convention. Wired into `HomeScreen.jsx` (Coach mode only, `!isOrgMode &&` gate — same pattern as every other mode-gated surface in this file), `BuilderScreen`/`BuilderGoalGuidance.jsx` (force-open + one-time category highlight on arrival), and `GoalsScreen.jsx` (deep-link to the Trends subtab, or a one-time pulse on the Untagged row when there's no precise in-page anchor).

**Two real bugs found and fixed during live verification, not just the planned build:**
1. **A spec-violating over-duration recommendation.** The "missing from next plan" state's `Plan {N} minutes` CTA didn't check `closable` the way the sibling "meaningful gap" state already did — live-verified as an actual "Plan 63 minutes" CTA on a 45-minute practice, which the spec explicitly prohibits ("Do not use `Plan N minutes` with a number larger than the practice duration"). Fixed to fall back to the same "This gap will take more than one practice to close" / "Review Goal Guidance" copy the closable-gap branch already used.
2. **A navigation bug that silently loaded the wrong practice.** The Builder CTA's first version called `navigate()` directly instead of the existing `goToBuilder()` helper, which left `BuilderScreen`'s `useState` initializers reading a stale `null` `editPracticeId` on first render (that context value is only set a tick later, by `BuilderRoute`'s own effect, specifically because every *other* existing entry point already calls `goToBuilder()` first) — Builder opened for the right practice ID in the URL but rendered blank-practice defaults (today's date, no duration, empty plan) instead of the real saved one. See the new Gotcha above for the full mechanism. Fixed by extending `goToBuilder()` with an optional `navState` param instead of adding a second, parallel way into Builder.

**A third, unrelated bug found while tearing down test data**: `session_activity_log.practice_activity_id`/`.station_id` had no `ON DELETE` action at all, unlike every other link in that cascade chain — blocking deletion of a team/practice's history the moment any activity had been logged against it. Confirmed via dry-run (`BEGIN;...ROLLBACK;`), then applied to production after explicit sign-off (`20260805000000_session_activity_log_activity_cascade.sql`) since this session's sandbox blocks direct non-dry-run schema writes without it.

**Verified live** with one disposable coach account + team (real Soccer skill categories/tags, not synthetic ones) walked through several states via direct data mutation rather than 7 separate synthetic teams: `missing_from_next_plan` (confirmed the closable-CTA fix, confirmed Builder opens with Goal Guidance expanded and the right category pulsed), `aligned` (confirmed "View Trends" lands on the Trends subtab), `data_quality` (confirmed "Review Untagged Time" lands on Goals Overview with the Untagged row pulsed), and the aligned-but-moving-away override (found unintentionally — a next-practice plan skewed entirely toward one category pushed that category "Farther from goal," correctly overriding a would-be `aligned` state, exactly as the spec's own override rule intends). Phone-width layout (375px) confirmed clean. Org-mode suppression confirmed by code inspection only (the gate is a trivial, consistent `!isOrgMode &&` boolean already used everywhere else in this codebase) — not re-tested against a live org account, since this disposable account had no organization. All test data (team, drills, 3 stray auth users left over from earlier failed setup attempts) confirmed fully deleted via a final residue query, not just assumed.

### Twenty-seventh session (2026-08-02) — 14-item direct-feedback batch from testing the twenty-sixth session's work live

Jax tested the Goals & Insights enhancements and the Permissions/Drill-Insights work live and came back with 14 items in one message, spanning a real permissions-default bug, a real CSS stacking-context bug, one new notification feature, one new Builder feature, one new drill-recommendation feature, one new sort feature, and several smaller copy/UX/color fixes. Organized into a task list and worked through all 14 in one pass, live-verifying the highest-risk ones (permissions defaults, the menu-clipping fix, the Builder switcher, the invite-accept notification, the drill-recommendation drill-down, and the goal-based sort) against real production data with two disposable test accounts (a head coach + an assistant, to exercise the real invite/accept/notification chain end to end), then tore both down.

**Two real bugs, not just requested changes:**
1. **Library sharing defaulted to shared, not private.** `head_coach_shares_library`/`assistant_shares_library` (added twenty-fifth session) both defaulted `true` — a head coach could see an assistant's whole library the moment an invite was accepted, with no opt-in step, contradicting the actual intent (browse via Explore, copy in what's wanted). Fixed the default to `false` (`20260803000000_library_sharing_default_off.sql`) and backfilled the 8 existing rows uniformly, confirmed via direct query that none had been explicitly toggled by anyone yet so backfilling didn't clobber a real choice.
2. **A drill's ellipsis dropdown was visually clipped by the next row.** Root-caused live (not guessed): `SortableActivityRow`'s per-row `position:relative` + explicit `zIndex` (present on every row, not just dragging/sticky ones, all at the same level) creates a new stacking context per row, which traps a `z-index:50` popover inside it — it can never escape to paint above a sibling row painted after it in DOM order, regardless of the popover's own z-index. See the new Gotcha above for the full mechanism. Fixed with a `raised` prop that lifts one specific row's zIndex above its siblings while its popover is open.

**The other 12, briefly (full detail in the Feature Inventory sections above):** Builder's drill picker now defaults to "My Library" with a switcher for the team's org/peer libraries instead of a flat unlabeled merge of everything accessible; a new `team_join_notices` table + Home card notifies the head coach when an invite is accepted and deep-links straight into `PermissionsModal` for that exact coach; the drill-menu private toggle is now "Keep Private"/"Make Public" (not "Hide from"/"Share with My Coaches" -- "Make Private" was already taken in the same menu) with a real confirm prompt before going public; a lock icon marks a private drill on its card; the heat-color card icon is gone (insights only via the ellipsis now, per "too distracting" feedback); Next Practice Guidance's below-target category rows expand to show which of the coach's own drills are tagged to that category; the Goals editor's Save button now grays out again immediately after a successful save; My Drills gained a Sort picker (Custom/Most Frequently Used/Alphabetical/Suggested-by-goals, the last ranking a drill by the largest deficit among its tagged categories for a team the coach picks); a small spacing fix between the new Goals subnav and the team tab row above it; a new amber "exceeds" planning-depth state (Home/Schedule pills and Builder's own pill) when planned drills add up to more than the scheduled duration, alongside the existing red/green under-planned states; and the global `/schedule` route now correctly underlines the Home tab (previously lit up none of them), with all active-tab underlines 50% thicker for PWA visibility.

**Verified live**, two disposable accounts (head coach + assistant) plus real seeded team/goals/drills (direct SQL, not click-through -- same reasoning as the twenty-sixth session's own verification: the point was exercising the new report/UI code against real data shapes, not re-testing already-shipped click paths): confirmed the Suggested sort correctly ranked a goal-tagged, high-deficit drill first; confirmed the ellipsis menu no longer clips and the wording/lock-icon/confirm-prompt all render correctly; confirmed Builder defaults to "My Library" with no switcher when there's only one source; confirmed the full invite → accept → notification → deep-linked Permissions modal chain end to end, including both toggles correctly defaulting off for the new relationship; confirmed a below-target category expands to the right drill; confirmed the Save button visibly grays out once nothing is dirty. Both accounts and all their data (including two drills, which needed the twenty-sixth session's own FK fix to hard-delete cleanly this time with zero manual SQL workaround needed) deleted afterward; temporary mint/seed/cleanup scripts removed from the repo.

### Twenty-sixth session (2026-08-02) — all five Goals & Insights enhancements from Jax's spec doc, in one pass

Jax handed over a full, detailed implementation spec (`RUN-OF-PRACTICE-GOALS-INSIGHTS-ENHANCEMENTS.md`) covering five enhancements: Skill-Development Trends, Next Practice Guidance, combined Builder Goal Guidance, the Practice Execution Scorecard, and Drill Performance Insights, plus explicit instructions to read this whole file first, inspect the live `get_team_goal_report` definition before writing any new math, dry-run every migration, and live-verify everything with disposable accounts. Asked two scoping questions up front — push through all five phases in one session vs. checkpoint after each, and what to do with an already-reorganized-but-uncommitted `BUILD-STATUS.md` sitting in the working tree — and got "push through all five" and "commit the reorg separately first," respectively; the reorg commit landed before any feature work started.

**Investigation before writing any SQL, per the spec's own instructions.** Read `get_team_goal_report`/`set_team_goals` (`20260730040000_fix_get_team_goal_report_stale_column.sql`, the most recent correct body) line by line rather than trusting the migration history alone, given this exact file's own documented history of migration-history-vs-live-schema drift. Confirmed via `npx supabase migration list` that local and remote migration bookkeeping matched exactly (no drift), and — since this sandbox has no Docker for `db diff`'s shadow database — used `supabase db query --linked` wrapped in `BEGIN;...ROLLBACK;` as the real dry-run mechanism instead (see new Gotcha above), including exercising real RLS/authorization by setting `request.jwt.claim.sub` to a real user's uuid inside the rolled-back transaction. Traced practice-activity type constraints (`'activity'/'station_block'/'checklist'/'break'`), confirmed the shipped "Practice Components" system (Water Break, Stretch, etc.) all use `type:"checklist"` client-side despite a `'break'` type existing in the schema for exactly this purpose — a pre-existing, unrelated-to-this-session quirk, not something to fix here, but load-bearing knowledge for getting the new attribution math to match existing behavior exactly rather than inventing a new split (the spec's own explicit instruction).

**Phase 1 — shared math layer** (`20260802000000_goal_attribution_shared_helpers.sql`): extracted `get_team_goal_report`'s attribution rules into four `security definer`, `revoke all from public` SQL functions plus one composite type — see the new "Goals & Insights architecture" theme above for the full reasoning, including why the revoke matters (these have no authorization check of their own).

**Phase 2 — Trends** (`20260802010000_get_team_goal_trends.sql`): built and validated against the real coach's own production team ("Scorpions 7-8," 6 goal categories, `America/Phoenix`) via the rolled-back-transaction technique before ever pushing for real — confirmed correct Monday–Sunday local week boundaries, correctly-null empty weeks, and real percentages for weeks with actual completed sessions. Client-side `GoalsSubnav`/`GoalTrendCard`/`WeeklyTrendChart` (inline SVG, no new chart dependency) built into `GoalsScreen.jsx`.

**Phase 3 — Next Practice Guidance + Builder Goal Guidance**: Next Practice Guidance needed no new RPC at all, since `get_team_goal_report` already returns real minute totals, not just rounded percentages. Builder Goal Guidance is a new standalone `src/components/BuilderGoalGuidance.jsx`, wired into `BuilderScreen` as its own sibling section between Practice Details and The Run of Practice.

**Phase 4 — Practice Execution Scorecard** (`20260802030000_get_session_execution_scorecard.sql`): confirmed the real execution-unit grain (station vs. station-block container) against the schema before writing any skip-detection logic, per the spec's explicit warning not to guess here. Added `session_activity_log.manually_added` (`20260802020000_...sql`) as a real, reliable signal for "logged but not originally captured," rather than trying to infer it from `adjusted_by`/`adjusted_at` (which turned out not to actually distinguish the two cases, since both existing RPCs stamp them identically).

**Phase 5 — Drill Performance Insights** (`20260802040000_drill_insight_rpcs.sql`): confirmed `library_activity_id` lineage survives templates and copies before writing the batch/detail RPCs (a copied drill gets a new id and starts fresh, by construction — no special-casing needed). Wired the heat icon and "View Drill Insights" ellipsis item into `NewLibraryScreen.jsx`'s existing `isMine`-gated drill card (the same conditional that already scopes to My Drills/Org Drills, never Team Libraries or Public Library, so no new gating logic was needed there).

**Two real bugs found and fixed during live verification, not just the requested features.** Both are written up in full in the Gotchas section above (worth reading there for the full mechanism), summarized here:
1. An infinite render loop in `SessionHistoryDetail`, triggered by opening any completed session once the new Execution Scorecard's extra state-driven re-renders were added — root cause was a pre-existing effect depending on a freshly-created `onBack` closure and writing it into an ancestor's state every render, the same bug *class* (not the same bug) as the already-documented PlayerProfile `useBlocker` gotcha. Confirmed as a real, unbounded, live-reproducing loop (not stale tooling output) via an in-page render counter before attempting a fix, and reconfirmed fixed the same way afterward. Fixed with the same ref-based pattern already established for this bug class.
2. A confusing mismatch in the first draft of `GoalTrendCard`: its summary line reused `get_team_goal_report`'s forward-looking Planned percentage (0%, since nothing was currently scheduled) directly beside a chart plotting `get_team_goal_trends`' backward-looking Planned line (~55%, from real completed-session history) — caught live, not in code review, by actually looking at the rendered card. Fixed by deriving the card's rolling summary from the same weekly data the chart itself plots.

Also found, live, while cleaning up this session's own test data: a previously-undiscovered instance of the FK-cascade-blocks-account-deletion gap this file already tracks, this time on `activity_library`'s lineage pointers (`practice_activities`/`stations`/`template_activities`/`template_stations`, all `no action`). Fixed to `on delete set null`, the established lineage-pointer pattern (`20260802050000_library_lineage_fk_actor_pattern.sql`). A second instance was also found (`session_attendance.player_id`) but deliberately **not** fixed — see Known Gaps above for why this one needs an actual product decision rather than a mechanical migration, and the manual-SQL-cleanup workaround used to finish this session's own account teardown without it.

**Live verification, fully live, not just built.** A disposable test account (`generate_link`+`verifyOtp`, tokens injected into the Browser pane's `localStorage`, the standard pattern this file already documents) with a real team, real goals (Shooting 40%/Ball Handling 60%), two real drills, and three real completed practices spanning three distinct weeks — seeded via direct SQL as the `postgres` role rather than through the app's own UI, since building three weeks of realistic session history purely by clicking through live-run screens would have cost far more time than the verification itself, and the goal was exercising the *new report code* against real data shapes, not re-testing the already-shipped live-run flow. Confirmed live in a real browser: `GoalsSubnav`'s three tabs; Trends rendering real weekly charts and correct trend-summary copy; Next Practice Guidance computing real gap/goal-mix minutes including the most-recent-scheduled-duration fallback; Builder Goal Guidance's collapsed-by-default state, its three expanded sections, and — the one genuinely hard-to-fake part — a drill actually being added to an empty draft and the projected-impact numbers updating instantly with zero network calls, correctly flagging an "overshoot" (adding too much of one category pushed its projected percentage *past* the target, correctly read as moving farther from goal even though the raw percentage went up); the Execution Scorecard's full breakdown (Shortened/On Plan buckets, correct 60-second-tolerance boundary behavior, correct planned-vs-actual category comparison); and Drill Insights' full detail view (usage counts, average duration copy, on-plan/extended/shortened percentages, and reverse-chron history) all matching the seeded data exactly. The two real bugs above were both caught during this same pass, confirmed fixed, then re-verified once more before moving on. Test account and all its data (including the two library drills, which needed the FK fix above before they could be deleted) fully torn down afterward via a mix of the Admin API and direct SQL; confirmed zero residual rows. Temporary scripts (`tmp-verify-mint.mjs`, `tmp-verify-cleanup.mjs`) removed from the repo, not left behind.

**Deliberately not built, per the spec's own explicit exclusions**: season phases/historical goal versions, player-focus follow-through, practice-to-practice comparison, notes-to-actions, a standalone confidence feature, any new charting dependency (confirmed via a final `npm run build` + grep for `recharts`: the vendor and lazy-loaded founder-metrics chunks are untouched, the main app chunk grew from ~156KB to ~161KB gzip for all five features combined, no heavy dependency added), and no automatic drill insertion, plan editing, or AI-generated recommendations anywhere in the new guidance surfaces.

### Twenty-fifth session continued (2026-08-01) — comparison table, second pass: cut the redundancy, checked GameChanger's real page

Follow-up feedback on the redesign above: it "still feels redundant." Root cause was the "Core coaching tools" group -- five rows that were checked identically across all four plan columns, which reads as noise once you notice the pattern rather than as reassurance. Asked directly to review GameChanger's actual comparison page (not go from memory) and bring back concrete recommendations as if pricing were the whole job.

**Checked GameChanger's live page** (`web.gc.com/pricing`) via the Browser tool -- WebFetch failed outright on it (JS-rendered SPA, metadata only), and the in-app scroll container turned out not to be `document.body` (a `.app__mainGrid` div with its own internal scroll, `document.body.scrollHeight` stuck at 720px the whole time), so reaching the real content needed scrolling that container directly rather than the page. Once visible, their table opens with only two or three shared rows before differentiating -- not a whole section of them -- and every plan header is a colored pill (gray/green/blue) that the checkmarks below are subtly tinted to match. Not-included cells are a plain muted dash, not an empty shape. Rows are lightly zebra-striped for scanability, and a persistent "Try Plus" pill floats at the bottom of the viewport the entire time you scroll the table.

**Applied here:** the "Core coaching tools" group is gone entirely -- the shared foundation (practice building, live practice mode, personal equipment, player focus areas, unlimited parent-helper links) is now one line of prose above the table instead of five repeated checkmark rows, so every remaining row in the table is a genuine differentiator, addressing the redundancy directly. Table header cells now use a colored pill per plan (`pp-plan-pill`: Free gray, Pro green, Pro+ black, Org white-bordered) instead of plain text, echoing GameChanger's treatment and tying each column back to this page's own existing brand colors (Pro's green already matches its "Most Popular" card badge and primary CTA color; Pro+'s black echoes the Organizations section's own black band lower on the page). Data rows are lightly zebra-striped (`pp-row-alt`, alternating within each group) for the same scanability reason GameChanger stripes theirs. The floating persistent CTA was deliberately **not** added -- with `BILLING_ENABLED` still false, a floating "start trial" button that leads to a plain early-access CTA would read as a checkout flow that doesn't exist yet; worth reconsidering once billing goes live.

**A real CSS specificity bug caught before it shipped, not after.** The new zebra-stripe rule (`tbody tr.pp-row-alt td`) has three type-selector terms versus the existing Pro-column tint rule's two (`tbody td.pp-featured-col`), so on alternating rows the stripe would have silently outranked and erased the green Pro-column tint -- confirmed by computing both selectors' specificity by hand before it was ever visually checked. Fixed with an explicit combined rule (`tbody tr.pp-row-alt td.pp-featured-col`) that unambiguously wins regardless of source order. Verified live via `getComputedStyle` on real DOM nodes (not screenshots, which were flaking in the Browser pane again this session): all four pill colors confirmed, stripe alternation confirmed within a group and reset at each new group boundary, and the featured-column tint confirmed intact on both plain and striped rows.


### Twenty-fifth session continued (2026-08-01) — pricing comparison table redesigned for fast scanning

Direct feedback: the comparison table was "wild" -- good detail, but every cell was a text value ("Personal plan still applies," "Based on organization agreement," "No plan-based cap"), so nothing was scannable at a glance. Jax pointed at GameChanger's own plan-comparison table as the model: easy-to-read feature rows, a checkbox per plan, core features checked everywhere, and the pattern of checks telling the upgrade story as you scroll down.

**Redesigned from the ground up**, not just restyled. Every row now has a `type`: `"check"` (plan values are true/false, rendered as a small circle -- filled green with a white checkmark when included, an empty light-gray outline when not) or `"value"` (plan values are short, meaningfully different text -- a real number or a specific word like "Unlimited," never a full sentence). The checkmark-in-circle treatment isn't a new shape invented for this page -- it's the exact same convention already used for attendance marks and checklist items elsewhere in the app (`Ic.Check` inside a green-bordered circle), reused here specifically so it reads as "brand consistent" rather than a one-off. Row count dropped from 24 to 20 and content got much shorter: "Personal plan still applies" became "Org-managed," "No plan-based cap" became "Unlimited," and the two rows that only ever said "Future capability" (cross-team development reporting, advanced adoption reporting) were dropped outright rather than shown as an org checkmark -- a comparison table is not the place to market something that isn't built yet, same principle the rest of this page already follows. Groups were re-cut around the story rather than the old schema-shaped categories: "Core coaching tools" leads with five rows that check every single plan (the reassurance beat Jax described), followed by "Teams and staff," "Drill library and templates," "Player development," and "Organization oversight" building the upgrade case group by group. The Pro column also picked up a light green tint down its whole cell column, tying the table back to the "Most Popular" card above it.

**A real bug found and fixed during live verification, not just visual polish.** At certain horizontal scroll positions on mobile, value cells like "Org-managed" and "Unlimited" rendered with their first few characters invisible ("-managed," "nlimited"). Root cause: the sticky feature-name column (`position: sticky; left: 0`) had no width constraint, so it sized itself to its longest phrase ("Active personal teams") via the table's default auto-layout -- at 254px wide, meaningfully wider than the now much-narrower value columns (single words, numbers, or a 22px circle). Once scrolled roughly one column's width, the sticky column's own visual footprint bled into and painted over the next column's content, since sticky positioning doesn't respect column boundaries the way normal table flow does. Fixed by switching the table to `table-layout: fixed` with an explicit `<colgroup>` (feature column 36%, each plan column 16%), which fixes every column's width regardless of content length and eliminates the mismatch that let the sticky column overhang its neighbors. Confirmed fixed live: scrolled to the exact position that previously clipped text, values now render in full, wrapping onto a second line when needed instead of being cut off. Also confirmed via direct `getBoundingClientRect` checks that desktop (wide enough that the table never needs to scroll at all, `scrollWidth === clientWidth`) was never affected by this specific bug and renders correctly.


### Twenty-fifth session continued (2026-08-01) — closing the peer-equipment-visibility gap

Small, focused follow-up the same day: Jax asked directly to extend `assets_select_access` for peer sharing, closing the one gap flagged when the equipment-mismatch dialogs shipped earlier. Added a fourth branch to the policy (`20260801090000_assets_peer_sharing_rls.sql`), same shape as the existing org-share branch just above it in the same policy: an asset is visible if it's linked to a drill that's visible via `is_library_peer()`, respecting `is_private` the same way drill visibility itself already does. Templates' own (separate, org-only) equipment-sharing branches were left untouched -- peer sharing was scoped to drill libraries only, never templates.

Verified live with a fresh disposable head-coach/assistant pair on a personal team: the head coach could not previously see an asset linked only to the assistant's peer-shared drill (confirmed both via direct query and, live in the browser, the drill's "Needs:" line resolving to nothing) -- after the fix, the same asset resolves by name, `is_private` and the sharing toggle both still correctly suppress it (and re-enabling correctly restores it), and the equipment-mismatch "Copy to My Library" dialog now actually fires for this drill instead of silently letting the gap through unnoticed, confirmed both via a direct RLS check and by reproducing the flow in the real UI.


### Twenty-fifth session continued (2026-08-01) — equipment-mismatch dialogs, and a real silent-failure bug found while building them

Direct continuation of the same day's work. Jax asked for the two remaining equipment-mismatch dialogs from the original list: one when copying a drill into your own library (from Public Library, an org, or a peer) and you don't have its equipment yet, and one when adding a drill straight into a practice in Builder.

**Investigated before building.** `copyDrillToMyLibrary` (`src/supabase.js`) already resolved a copied drill's equipment by name+type match against the destination pool -- but had zero user choice: any unmatched item was unconditionally auto-created, silently, every time. Builder's own add-to-practice path (`addAct` in `App.jsx`, and `StationConfig`'s equivalent `chooseFromLibrary` in `ActivityConfigs.jsx`) did no resolution at all -- it copied a drill's raw equipment asset ids verbatim into local activity state. **Found a real, live bug along the way**: those raw ids get written to `practice_activity_equipment` at save time via `saveActivityTree`'s `replaceEquipment`, whose insert result was never checked for an error at all. For a drill whose equipment belongs to someone else personally (an org-shared drill's equipment is fine, since `can_link_asset_to_practice_activity`'s RLS allows org-owned assets -- but a peer-shared drill's personally-owned equipment is not), that insert silently fails under RLS: the practice looks saved, the equipment link just never lands, with no error surfaced anywhere. This was already possible before today, but became a much likelier path to hit now that this same session's earlier peer-library-sharing work makes browsing and adding another coach's drills directly into a practice a normal, encouraged thing to do.

**Shared resolver helpers, not duplicated per call site** (`src/supabase.js`): `findMissingEquipment(equipmentIds, sourceAssetsById, ownPool)` is a pure, synchronous, no-DB-call comparator (extracted from `copyDrillToMyLibrary`'s existing name+type logic) used to decide whether either dialog needs to show at all, before anything is written. `resolveDrillEquipmentForCoach(coachId, equipmentIds, sourceAssetsById, ownPool, createMissingEquipment)` is the async resolver, shared by Builder and `StationConfig` -- it returns asset ids that actually belong to the coach (matched or newly created), never a foreign id RLS would reject, which is exactly what fixes the silent-failure bug above as a side effect of doing the dialog correctly. `copyDrillToMyLibrary` itself was refactored to call the same shared resolver internally (`createMissingEquipment` now a real option, defaulting `true` to preserve every existing caller's behavior unchanged) rather than keeping its own separate copy of the same logic.

**New `EquipmentMismatchDialog.jsx`** (standalone, matching `PermissionsModal.jsx`'s pattern), the first 3-action modal in the app (every prior multi-option confirm was a plain two-button `.modal` or a native `window.confirm`, OK/Cancel only). Same component, different copy via a `context` prop, reused across all three trigger points (`NewLibraryScreen.jsx`'s `doCopy` -- which `PublicLibraryScreen.jsx` already calls as a passed-down prop, so it got the new dialog for free with zero changes of its own; Builder's `addActChecked`; `StationConfig`'s `chooseFromLibrary`). Builder and `StationConfig` deliberately always resolve into the coach's own **personal** pool, never org-scoped -- `BuilderScreen` has no Coach/Org mode awareness at all today (a known, pre-existing gap noted elsewhere in this file), and a coach's own equipment already satisfies the relevant RLS for any team they can build for, so this doesn't need mode-awareness to be correct.

**Verified live**, using the same disposable-test-account pattern as the rest of this session: confirmed a Public Library catalog drill (visible equipment, unlike a peer-shared drill's -- see below) correctly triggers the dialog listing exactly what's missing; "Add Drill Anyway" copies the drill with zero equipment created or linked (confirmed via direct query: 0 assets, 0 equipment rows); "Add Equipment to Library" creates the missing asset and links it correctly (confirmed via direct query). In Builder, confirmed the fast path (a drill whose equipment the coach already owns) still adds immediately with no dialog interruption -- a regression check, since this is the common case for the one real user today -- and that the added activity's equipment carries through correctly end to end into the practice's own equipment field.

**A real residual gap surfaced, not fixed, and worth flagging explicitly**: testing with a peer-shared drill (this session's earlier Permissions feature) showed its equipment name resolves to nothing in the UI ("Needs:" with no list) for the viewing coach -- confirmed as an already-known, already-documented limitation (`can_link_asset_to_practice_activity`'s own comment, and this session's own `20260801060000_library_peer_sharing_rls.sql` note), not something newly introduced. Because `assets_select_access` was deliberately not extended for peer sharing, a peer's personally-owned equipment is invisible under RLS to begin with -- `findMissingEquipment` can only flag a gap for equipment it can actually resolve, so for this specific case (peer-shared drill referencing the *other* coach's personal, non-org equipment) neither dialog fires at all, and the old silent-drop behavior still applies. The dialogs fully solve the problem for org-shared and Public Library drills (equipment always visible) and for peer-shared drills where the coach happens to already own a matching asset; the fully-invisible peer-equipment case would need `assets_select_access` extended the same way this needs its own future pass.


### Twenty-fifth session continued (2026-08-01) — a real drill-library sharing and practice-delegation Permissions system

Direct continuation of the same day's invite-consent work. Jax asked to build "permissions" next: a head coach and a rostered assistant/helper each get a reciprocal "Share Drill Library" toggle, plus a head-coach-only "Share Practice Planning" toggle delegating practice-building to one assistant per team, reached from a new Permissions entry on the roster's Coaches tab.

**Investigated before building, since this touches live RLS with real security stakes.** An Explore pass confirmed drill-library visibility today is entirely org-scoped (`can_access_owned`'s `owner=me OR org-member` chain) -- a plain head-coach/assistant pair with no shared organization has zero visibility into each other's libraries, and that exact 3-branch OR-chain (own / org-share / public-catalog) turned out to be **duplicated independently across five places**: `activity_library_select_access`'s policy, `can_access_activity()`, `can_link_drill_to_practice()`, and two template-linking functions (left untouched -- templates were out of scope, this is about drills going into practices, not templates). Also confirmed practice-editing access today is 100% derived from `team_staff.role = 'head_coach'` specifically -- assistants/helpers cannot build or edit a practice at all currently, so "Share Practice Planning" is a genuine new grant, not something already partially there.

**Schema** (`20260801050000_library_sharing_and_delegation_columns.sql`): three new `team_staff` columns -- `head_coach_shares_library`/`assistant_shares_library` (both default `true`, matching Jax's "default shared with peers" framing) and `can_build_practices` (default `false`, a materially bigger grant than visibility). A partial unique index (`team_staff_one_delegate_per_team`, on `team_id` where `can_build_practices and archived_at is null`) enforces the one-delegate-per-team cap at the database level, not just in the UI. `activity_library.is_private` (default `false`) lets a coach exclude one drill from the new default sharing -- a separate axis from the existing, already-opt-in organization sharing, which this does not touch.

**RLS** (`20260801060000_library_peer_sharing_rls.sql`, `20260801070000_delegated_practice_planning_rls.sql`): a new `is_library_peer(owner_user_id)` function, deliberately checking `team_staff.role = 'head_coach'` directly rather than reusing `can_manage_team` -- that function's org-admin branch is exactly what a **personal-team-only** mechanism needs to stay clear of, so an org director's broader reach can't leak into unrelated personal-library visibility. Threaded into all three drill-visibility call sites that needed it (matching the existing "duplicate the chain, don't introduce a policy-calls-function-for-its-own-table pattern into a live security path" convention already established there), respecting `is_private`. Equipment visibility for a peer-shared drill was **deliberately left as-is** -- not a new gap, `can_link_asset_to_practice_activity`'s own existing comment already flags this exact scenario for personal-team co-coaches as a known, accepted limitation.

For delegation: a shared `can_build_practice_for_team(team_id)` helper, threaded into `can_manage_practice`/`can_manage_practice_activity`/`can_manage_station_block`/`can_manage_station` (all four independently already re-derived access from `can_manage_team` via their own join to `practices`/`teams`, found by grepping for that exact shape) plus `practices_insert_manage`/`practices_update_manage`'s own policies directly, since those two check `can_manage_team` inline rather than routing through `can_manage_practice` (practice creation has no `practice_id` yet to check it against). Deliberately never touches `can_manage_team` itself, which also gates team settings, roster management, and staff administration -- a delegated assistant should be able to build this team's practices and nothing else.

**RPCs** (`20260801080000_permission_toggle_rpcs.sql`): `set_own_library_share` (self-service, mirrors `set_team_staff_show_on_home`'s exact narrow shape -- `team_staff_update_manage` is `can_manage_team`-gated only, so a plain assistant can't update their own row directly), `set_manager_library_share` (head-coach-only), and `set_practice_delegate` (head-coach-only, with an explicit pre-check ahead of the unique index so a second-delegate attempt gets a friendly, specific error message instead of a raw constraint violation).

**UI**: a new standalone `PermissionsModal.jsx` (kept out of `ModalLayer.jsx`'s already-large if/else chain, same reasoning as `AbsencePicker.jsx`), reached via a "Permissions" item in the roster's existing manager-only coach menu, plus a plain text button on an assistant's own row when they can't manage the team (no ellipsis menu exists there today). Toggle-switch styling copied verbatim from `SettingsScreen.jsx`'s "Show on Home" control. Each blurb explains the toggle's actual effect (no em dashes, per direct instruction). A new "Hide from My Coaches"/"Share with My Coaches" drill-menu item toggles `is_private` -- deliberately never labeled "Private" in the UI, since the drill menu already has an existing "Make Private" action that means something different (clearing all organization shares); reusing that word for a second, unrelated meaning in the same menu would have been genuinely confusing. `NewLibraryScreen.jsx`'s Explore/Team Libraries shelf gained a parallel "one shelf per peer" grouping (same treatment the existing per-org shelves already use) for personal-team peer-shared drills; the Builder's own drill picker needed no changes at all -- it already renders whatever `data.activityLibrary` contains with no owner/org filter of its own, so once RLS started returning peer-shared drills, they appeared there automatically.

**Verified live against real RLS boundaries, not just built.** Three disposable test accounts (a head coach, two assistants) on a real personal team, added via the actual `invite_team_staff`/`accept_team_invite` flow verified earlier the same session. Confirmed directly against the database with each account's own authenticated session (not service-role, since the point was testing what a real signed-in user can and cannot do): default sharing visible both directions immediately on accept; assistant-to-assistant visibility correctly absent (peers are only ever visible relative to the shared head coach, never symmetric between two assistants on the same team); `is_private` correctly hides a drill from peers while the owner still sees it; the manager toggle correctly scopes per-relationship (turning it off for one assistant left a second assistant's visibility untouched); the self-service toggle works and immediately affects the head coach's visibility; cross-actor authorization holds (an assistant calling the manager-only RPC gets a hard "not authorized" error; a manager calling the self-only RPC on someone else's row silently affects zero rows, confirmed by re-reading the row unchanged afterward); a delegated assistant can create a practice and add practice_activities to it while a non-delegated assistant is flatly rejected by RLS on the same action; the one-delegate-per-team cap produces the friendly error, confirmed both via direct RPC call and by reproducing it live in the Permissions modal itself (toggle visibly stayed off, red error text rendered). Copying a peer-shared drill into a viewer's own library was confirmed to work with zero new authorization code, relying entirely on the RLS extension already being sufficient. All three test accounts and the test team were deleted afterward, with cascade confirmed to leave zero orphaned rows.

**Deliberately not done this session:** the Pro+/plan-based gating on who can be granted "Share Practice Planning" (Jax's fuller description mentioned needing the delegated assistant to be Pro or org, unless the head coach is on Pro+) -- the delegation mechanism itself is real and enforced, but which plan is required to use it is not wired to anything, consistent with this session's earlier, deliberate choice to keep all entitlement checks inert until real billing exists. The two equipment-mismatch dialogs (add-to-library and add-to-practice) remain unbuilt, unrelated to this piece.


### Twenty-fifth session continued (2026-08-01) — Pro+ tier, pricing polish, and a real assistant-invite consent flow

Jax came back the same day with pricing-page fixes, a new Pro+ tier to add, and a much bigger item: a real production gap hit live -- an assistant coach added to a team got no notification and was just silently, immediately active, with no accept/decline anywhere. Handled the small, unambiguous pieces directly, then investigated the invite/roster/library-sharing code before scoping the larger bundled feature request (Pro+ delegation permissions, drill-library default-peer-sharing, equipment-mismatch dialogs) rather than guessing at all of it in one pass.

**Pricing-page fixes, all straightforward:** the header was replaced with a reimplementation of `LandingPage`'s own `lp-header` pattern (sticky white bar, brand left, nav right) instead of the plain centered-logo bar it started with -- self-contained per this file's own convention, not an import from LandingPage. The "PRICING" eyebrow was bumped from 13px to 18px (matching `lp-eyebrow`'s own size) since it read as too small. The Annual/Monthly toggle now defaults to Monthly (overriding the original brief's "Annual is default" call, per direct instruction).

**Pro+ re-added** ($15/month or $124/year, no annual discount stated separately -- the toggle's shared helper text now reads "Save $45/year on Pro or $56/year on Pro+" since a single dollar figure stopped being accurate for two differently-priced tiers). Grants everything Pro does, plus delegating practice planning to one assistant coach per team, 2 concurrent live practices, and (per Jax's fuller description, not yet built -- see below) sharing those same permissions with assistants who aren't on Pro themselves. `src/entitlements.js`'s `PLAN_LIMITS` gained a `pro_plus` entry (`concurrentLivePractices`/`delegatedPlanningPerTeam` fields added to all three plans for a complete config) and `user_entitlements.plan_type`'s CHECK constraint was widened to allow `'pro_plus'` (`20260801000000_user_entitlements_pro_plus.sql`, written to find-and-drop the existing constraint by inspecting `pg_constraint` rather than assuming Postgres's default auto-generated name, since there's no local/Docker environment here to verify it against first) -- verified live by setting a real row to `pro_plus`, confirming a bogus value still gets rejected by the same constraint, then reverting. Same inert-scaffolding treatment as free/pro: nothing enforces the delegation/concurrency limits yet.

**The bigger piece: a real accept/decline consent flow for team invites**, since `add_team_staff` (shipped testing-round-1) auto-linked and granted access immediately with zero consent step -- fine when a solo coach was the only real user, not fine once a second real person is actually being added. Investigated first (an Explore pass over the invite/roster/library-sharing code) rather than guessing: found `org_invites` already had the exact pattern needed (real `pending`/`accepted`/`declined`/`cancelled` states, email matched against `auth.jwt()`, a Home accept/decline card), so a new `team_invites` table mirrors it closely (`20260801020000_team_invites.sql`) instead of retrofitting a status column onto `team_staff` itself -- `team_staff` is read by `can_access_team`/`can_manage_team` and referenced across most of the app, so gating it with a new pending state would have meant auditing every one of those call sites for a miss; a separate table keeps `team_staff` meaning exactly what it always has ("a real, already-consenting member") and needed zero changes to any existing access check.

Confirmed with Jax before building: existing active `team_staff` rows keep working exactly as before (nobody currently active loses access or sees a surprise invite prompt) -- the new consent flow only applies to invites sent going forward. The one class of existing row that _did_ need migrating: unlinked (`user_id is null`) `team_staff` rows never represented real access in the first place (nobody could act as that person yet), so those were copied into `team_invites` as pending and deleted from `team_staff` in the same migration -- zero of these existed for the one real account by the time this ran, confirmed before and after. `claim_pending_team_staff` (the old signup-time auto-link trigger) and `add_team_staff` itself were dropped outright, not left as dead code -- `team_invites` needs no claim trigger at all, for the same reason `org_invites` never did: a plain-text email row is already visible to its recipient the moment they sign in, matched against their own verified email, nothing needs pre-linking.

New RPCs: `invite_team_staff` (upserts by team+email regardless of the existing row's status, so it doubles as the resend/re-invite-after-decline path with no separate RPC needed), `accept_team_invite`/`decline_team_invite` (email-verified against `auth.jwt()`, mirroring `accept_org_invite`/`decline_org_invite` exactly, including reviving an archived `team_staff` row for someone being re-invited after leaving), `cancel_team_invite` (head-coach-side "Clear," pending or declined), and `edit_team_invite` (fixes a typo'd email/name/role, puts a pending-or-declined invite back to pending). The notify email (`notify-team-staff-added`, kept at the same deployed URL rather than standing up a new function name) now reads `team_invites` instead of `team_staff` and actually asks for a response instead of saying "no need to log in until you're ready" -- and branches its copy on whether the invited email already has a Run of Practice account ("Sign In to Respond" vs. "Create Your Account"; both point at the same OTP sign-in link since Email OTP creates an account on first use either way, only the wording differs). The "Invite Sent" confirmation modal and the roster's Coaches tab both got matching copy/UI updates: a "Pending Invites" section below the real roster shows each invite's status with Resend/Request Again, Edit, and Clear actions.

**A real RLS bug found and fixed during live verification, not just code review.** The new Home accept/decline card initially rendered "You've been invited to join **as an Assistant Coach**" with the team name missing entirely. `team_invites_select`'s RLS correctly let the invitee see their own invite row, but the client's `teams(id, name)` embed on that same query hits `teams`' own SELECT policy separately -- and someone with a still-pending invite has no `team_staff` row, no org membership, and isn't the owner, so `teams_select_access` blocked it. Same class of gap as the seventeenth session's `organizations_select_member` relaxation ("org name shown on team cards"), same fix shape: one narrow additional `OR` clause on `teams_select_access` (`20260801040000_teams_visible_to_pending_invitee.sql`) exposing only the team row (not its roster/practices/anything else) to someone with a genuine pending invite addressed to their own verified email. Confirmed fixed live via the real invitee's own session.

**Verified fully live, not just built.** Three disposable test accounts (a head coach, an assistant, a decliner) via the established `generate_link`+`verifyOtp`+localStorage-token-injection pattern: created a real team, sent a real invite through the actual Add Coach modal, confirmed the "Invite Sent" copy and the roster's Pending Invites section, switched to the assistant's session and confirmed the Home card (including the team-name fix above), accepted it and confirmed a real `team_staff` row was created with the correct role while `team_invites.status` flipped to `accepted`. Repeated with a second invite through Decline instead, confirming the roster showed "Declined" with no access granted, then exercised Request Again (declined → pending again), Edit (changed role, correctly did **not** appear to persist on the first attempt -- turned out to be a test-script artifact, two synchronous DOM clicks fired before React re-rendered between them, not a real bug; confirmed by re-running the same two actions as separate steps), and Clear (invite disappeared from the roster, `status` set to `cancelled`). All three test accounts deleted afterward via the Admin API, with cascade confirmed to have cleaned up the test team, its `team_staff` rows, and its `team_invites` rows down to zero.

**Also done, trivial and zero-risk:** the "What do I do if my sport isn't listed?" FAQ item, added both to the app's main `/faq` page (right after the existing "different sports" question, `LegalPages.jsx`) and to the pricing page's own FAQ (added earlier the same day during the pricing-scaffolding work, before this message's fuller list arrived).

**Deliberately not done this session** (the rest of the same original message, explicitly scoped as later phases): the two-way drill-library sharing permission system (head-coach/assistant "Share Drill Library"/"Share Practice Planning" toggles), the default-shared-with-peers library model with a per-drill private flag (today's sharing is entirely org-scoped -- a plain team_staff pair with no org has zero visibility into each other's libraries, confirmed via the same investigation pass), the Pro+-gated delegation-permission logic itself, and the two equipment-mismatch dialogs (add-to-library and add-to-practice).


### Twenty-fifth session (2026-07-31) — pricing/entitlements brief: new 3-tier structure, inert plan scaffolding

Jax shared a full pricing/entitlements/upgrade-UX implementation brief (Free/Pro/Organizations, replacing the prior 4-tier Free/Pro/Pro+/Org concept from last session) and asked to update the pricing page and start moving toward feature gating and upgrade opportunities, explicitly inviting scoping questions before work started.

**Phase 1 repo review (per the brief's own instructions) found this is a fully greenfield build.** No `plan_type`/`subscription`/`billing_status` column existed anywhere in the schema, no feature-flag system, no analytics event pipeline -- every limit in the prior pricing page's copy (2 assistants, 15 drills, etc.) was pure marketing text, never enforced anywhere. The app is live with exactly one real user (a solo coach) currently on completely unrestricted access. Given the brief itself is realistically weeks of work (a central entitlement service, server enforcement across 9 gates, trial/downgrade flows, an upgrade-prompt catalog, analytics), asked two scoping questions rather than guessing: how far to go this session, and how the one real early-access user should be treated once entitlement checks exist in the code at all. Confirmed: pricing page + inert entitlement scaffolding only (no live gates, no upgrade-prompt UI, no trial/downgrade flows this session), and every current/future account gets an implicit early-access bypass so nothing observable changes for the real coach until `BILLING_ENABLED` is deliberately flipped on in a future session.

**A real security gap identified and designed around before writing any schema.** The obvious approach -- add `plan_type`/`billing_status`/trial columns straight onto `profiles` -- would have been silently exploitable: `profiles` already has `grant select, update on public.profiles to authenticated` with no column-level restriction, and `profiles_update_own`'s RLS only checks `id = auth.uid()` (row-level, not column-level). Any signed-in coach could have set their own `plan_type` to `'pro'` via a plain `.update()` call. Solved by keeping billing state in a brand-new `user_entitlements` table instead (`20260731110000_user_entitlements.sql`) with a `select`-only grant to `authenticated` and no insert/update grant at all -- server-authoritative only, same "flat, separate gate" pattern `admin_users`/`is_admin()` already uses elsewhere in this schema, not a retrofit of profiles. One row per user, auto-created via a new `on_profile_created_entitlements` trigger (mirrors `handle_new_user`'s own pattern) and backfilled for all 22 existing profiles, defaulted to `plan_type='free'`, `early_access_eligible` left `null` ("not yet evaluated") rather than guessing the brief's not-yet-decided eligibility rule. Verified live: row count matches `profiles` exactly (22/22), `service_role` explicitly granted per this project's own documented "bypasses RLS but not GRANTs" gotcha.

**`src/entitlements.js`, new file: `FEATURE_FLAGS`, `PLAN_LIMITS`, and `can(user, action, resourceContext)`**, implemented close to verbatim from the brief's own recommended shapes. All six billing-state flags reflect the real current state (`EARLY_ACCESS_ACTIVE: true`, `BILLING_ENABLED: false`, `PRICING_PAGE_PUBLIC: false` since still unlinked, `PRO_PREVIEW_ENABLED`/`EARLY_ACCESS_OFFER_ENABLED`/`ORGANIZATION_LEAD_FORM_ENABLED: false`). `can()` is fully real logic, not a stub -- it correctly resolves plan limits and upgrade keys for every action in the brief's structured-denial catalog -- but every call resolves to `allowed: true` today because of the `EARLY_ACCESS_ACTIVE`/`BILLING_ENABLED` short-circuit at the top. **Not called from anywhere in the app yet**, on purpose, per this session's confirmed scope; the next session that actually wires up a gate should use this rather than reimplementing the same limit logic per-screen.

**`/pricing` fully rewritten to the brief's exact copy**, replacing the prior 4-tier version entirely (Pro+ removed, confirmed zero other references to it anywhere in `src/`/`supabase/` afterward). New: an early-access notice banner (shown/hidden by `FEATURE_FLAGS.EARLY_ACCESS_ACTIVE`), an accessible Annual/Monthly billing toggle (real buttons, `aria-pressed`, changes Pro's displayed price only -- Free/Organizations never show it, per the brief), a 2-card Free/Pro grid (was 4 cards) with Free stacking above Pro on mobile, a full-width Organizations section, a grouped feature-comparison table, and an 11-item FAQ accordion (reusing the same open/closed button pattern `LegalPages.jsx`'s `FAQItem` already established, not a new pattern). Added real per-route SEO for the first time on this page -- this app has no head-management library anywhere, so a small `useSeo()` effect sets `document.title`/`meta[name=description]` and restores them on unmount, plus a `noindex` robots tag gated on `PRICING_PAGE_PUBLIC` (currently on, since the page still isn't linked from anywhere, matching the brief's own "during internal review, use noindex" instruction). Both CTAs that would otherwise go to a real checkout instead point at `/` (start using the app now) or a plain `mailto:` for Organizations, matching `BILLING_ENABLED: false` and `ORGANIZATION_LEAD_FORM_ENABLED: false`.

**One real bug caught and fixed during live verification, not just code review.** The comparison table's mobile treatment (a horizontally-scrollable table with a sticky first "feature name" column, per the brief) initially used a single `<th colSpan={4}>` for each category's group-header row (e.g. "Teams", "Plan and run"). `position: sticky; left: 0` on that cell computed correctly (confirmed via `getComputedStyle`) but visibly did nothing when scrolled -- a real, if narrow, browser quirk where sticky positioning on a column-spanning table cell doesn't reliably pin the way it does on a single-column cell, even though the exact same CSS worked perfectly for the (single-column) row-header cells one row below it. Fixed by restructuring each group-header row into a single non-spanning `<th>` (using the same sticky mechanism already proven to work for feature-name cells) plus a separate `colSpan={3}` `<td>` just for the gray background band. Verified live at 375px width via `scrollLeft`-based JS scrolling (the Browser pane's synthetic horizontal-scroll gesture was unreliable in this sandbox -- consistent `computer timed out, pane hidden` errors on `scroll_direction: "right"` specifically, while vertical scrolls with the identical error still visibly worked; cross-checked with direct DOM inspection rather than trusting the gesture tool alone, similar in spirit to the twenty-third session's `read_console_messages` staleness lesson) -- group labels now stay pinned alongside the feature column as the table scrolls right to reveal Pro/Organizations values.

**Deliberately not done this session, all per the confirmed scope:** no upgrade-prompt modals/banners from the brief's catalog, no actual blocking anywhere (team/drill/template/assistant creation all remain fully unrestricted for the real coach), no trial countdown or 14-day-preview flow, no downgrade-selection UI, no analytics events, and `/pricing` is still not linked from navigation or the landing page. A server-side mirror of `can()` (a Postgres function, for when gates need real enforcement rather than a client-trusted check) also wasn't built -- there's no caller for it yet, and the brief's own Phase 2/3 sequencing puts real gate wiring after this scaffolding, not alongside it.


### Twenty-fourth session (2026-07-31) — a forward-looking pricing page, not yet linked

Jax shared a 4-tier pricing spreadsheet (Free/Pro/Pro+/Org) and asked for a `/pricing` page built from it, deliberately not linked from the landing page or anywhere else yet, plus expert pricing feedback and clarifying questions before building.

**Feedback given before building anything:** the Free tier's "view 2 previous practices" was flagged as a data-*retention* cap rather than a usage cap -- coaches remember losing access to something they already built, which tends to erode trust more than it drives upgrades; recommended keeping full history visible on Free and gating deeper analytics instead. Also flagged the Pro→Pro+ jump ($96→$150/yr, +56%) as resting on a fairly narrow value prop (one more concurrent live session, assistant write access) that's worth naming a persona for on the page rather than just listing deltas, and that Org's flat per-team rate combined with "unlimited coaches & helpers" sends mixed signals unless volume pricing is real (Jax confirmed it is -- see below).

**Clarified before building:** URL is `/pricing` (not `/plans`, the more standard SaaS convention); the page is explicitly forward-looking ("here's what pricing looks like at general availability") since the rest of the site still says "free during early access" everywhere and no billing system exists at all yet; every tier shows a plain "Coming Soon" badge instead of a real button, since wiring up fake checkout would be worse than no CTA; Pro+'s blank spreadsheet cells (templates, history, player focus, home program) inherit Pro's values and are stated outright rather than left blank, Pro+ has no monthly option, and "tryout mode" (empty in every tier) is left off the page entirely until it's defined.

**Org pricing is deliberately not public.** Jax confirmed per-team pricing actually does decrease as an organization adds more teams, so the page shows no number for Org at all, just "Custom pricing -- talk to us for a quote, pricing per team goes down as your organization grows." (matches how org creation already works in this app: white-glove, admin-gated, sales-assisted, not self-serve.)

**Every feature rewritten as a natural-language benefit sentence**, not a spec label, e.g. "concurrent live practices: 2" became "Run two practices live at the same time, ideal when your teams practice at overlapping hours." Each tier also got a one-line "who this is for" pitch (Free: a coach trying it out with one team; Pro: a coach running more than one team; Pro+: a coaching staff that plans together; Org: a club coaching multiple teams under one roof).

**Follow-up:** Jax reviewed and asked to drop the retention cap after all -- Free's practice-history line now matches Pro/Pro+/Org exactly ("Keep your entire practice history forever, and look back on any practice you've ever run"), consistent with the feedback given up front.

New file `src/components/PricingPage.jsx`, self-contained (its own scoped `<style>` block reusing the app's existing global CSS variables and Barlow Condensed type, not LandingPage's LP_CSS, which is only injected when LandingPage itself mounts). Routed at `/pricing` as a top-level sibling of `/terms`/`/privacy`/`/faq` (renders regardless of auth state). Verified live at desktop and mobile widths, and confirmed via grep that nothing else in the app references `/pricing` yet -- reachable only by typing the URL directly, as asked.


### Twenty-third session (2026-07-31) — a real regression caught and fixed, a 4th instance of the FK bug class, and an app-wide em-dash sweep

Jax asked three things: review the Upcoming-vs-History logic for practices run early/ahead of schedule and present options (not a silent fix); fix an em-dash in the "added to a team" email; and sweep the whole app for em-dashes generally. Two unrelated real bugs were found and fixed along the way.

**Reviewed: why a practice run ahead of schedule can still show in Upcoming Practices.** Both Home and `ScheduleScreen` already exclude a practice from their "upcoming" list the moment its live session's `practice_live_sessions.status` reads `'completed'` -- date-agnostic (a practice run this morning counts immediately, not just after midnight), confirmed via source and an explicit prior-handoff comment ("Completed practices leave the list entirely"). Live-tested directly: scheduled a practice 3 days out, opened it in Builder, hit Run Now (ahead of schedule) -- it behaves exactly as designed once the session actually completes. The real gap is the *other* status this schema tracks: `'abandoned'` (a session started but not finished through the app's own end-of-practice flow) is deliberately **not** treated as completed -- CommandScreen's own abort screen says so directly: "it won't count as completed. Start a fresh run any time." That's almost certainly what's actually being observed: a practice that got started but wasn't walked all the way through Finish stays in Upcoming/Schedule by design, not by bug. Presented options rather than picking one: (a) leave as-is (abandoned = still plannable, matches "want to try again"); (b) always move a *started* session (completed or abandoned) out of Upcoming into History, with its status badge distinguishing them, so "I ran this" always reads as history even if it didn't finish cleanly; (c) something in between (e.g. auto-resolve an abandoned session after some idle time). No code changed for this item pending Jax's direction.

**Real bug found and fixed while live-testing the above: a 4th instance of the missing-`ON DELETE`-action bug class.** Hard-deleting a disposable test team failed with `practice_live_sessions_practice_id_fkey` violated -- that column (and `preview_sessions.practice_id`) had no `ON DELETE` action at all, unlike `practice_activities.practice_id` (already `cascade`) and every other practice-scoped child table. Both are pure content rows scoped to a practice, so cascade is correct here (not the nullable + `SET NULL` "historical record" pattern used for actor columns). Also found `practice_live_sessions.controller_user_id` -- `NOT NULL`, no `ON DELETE` action -- the same actor-identity gap already fixed three times this history (`practice_series.created_by`, `organizations.created_by`, `feedback.user_id`), except this one was additionally `NOT NULL` and had to be relaxed first. `20260731100000_practice_live_sessions_actor_fk.sql` fixes all three; verified by retrying the same team's hard-delete afterward, which then succeeded, and again with a second, unrelated test account.

**Real bug found and fixed: an infinite render loop in PlayerProfile, already live in production from last session's work.** Reproduced fresh (clean tab, cleared console) the moment a player's profile was opened -- React's "Maximum update depth exceeded" warning, immediately, every time. Root cause: last session's `useBlocker` addition (for the three-way Save & Leave prompt) combined with a *pre-existing* effect that re-registers `subViewBack` (the screen's Back-button hookup) unconditionally on every render -- a pattern that had been safe on its own but fed back into itself once `useBlocker`'s own re-render was in the mix. Fixed by registering once via a ref (`handleBackRef`) instead of re-running the effect every render, preserving "Back always sees the latest isDirty" without the per-render `setSubViewBack` call. Caught what looked like a second confirmation failure mid-fix: `read_console_messages` kept reporting the identical stale error after the fix, across a full dev-server restart -- turned out to be the *tool* returning buffered/stale output, not a real persisting bug; confirmed the fix was real via an independent in-page `console.error` counter (patched fresh, zero fires across opening the profile and editing a field) rather than trusting that tool alone. Worth remembering: don't take `read_console_messages` as authoritative after a fix in a long-running tab/session without cross-checking another way.

**Em-dashes removed app-wide.** The team-added email notification's line was rewritten to "This is just an FYI, no need to log in until you're ready." (both `notify-team-staff-added` and `notify-org-invite` edge functions redeployed, since both bundle the shared `_shared/emailTemplate.ts` at deploy time). Full sweep of `src/` and `supabase/functions/` for the literal em-dash character found and fixed real user-facing instances in `PracticePlanPrint.jsx`, `HomeScreen.jsx`, `LegalPages.jsx` (Privacy Policy), `CommandScreen.jsx` (several live-session status strings), and `FounderMetricsScreen.jsx` (subtitle prose) -- replaced with the app's existing " · " separator convention for short label-style fragments, and with plain sentence splits for full prose. Deliberately left the `"—"` used in `FounderMetricsScreen.jsx` as a null-value placeholder (an "N/A" convention, not punctuation). Also cleaned up a handful of code-comment section dividers that used real em-dashes purely stylistically, for full compliance, even though comments aren't user-facing.


### Twenty-second session (2026-07-31) — 8-item batch: two real bugs found and fixed, one new coach<->coach notification feature

Jax sent an 8-item batch touching Builder, Roster, the assistant-coach-add flow, My Team Assignments copy, the org-invite popup, the Teams list, per-team permissions, and a brand-new "someone left" notification.

**Builder: dashed outline removed from the green Run of Practice zone.** Purely cosmetic -- the `border:"2px dashed rgba(255,255,255,.4)"` on the absolute backdrop is gone; the solid green fill (and the real gap below it from last session) stays.

**Roster: PlayerProfile's Save button is now always present, and leaving with unsaved changes prompts to save.** Previously Save/Discard only rendered once the form was dirty; Save now always shows (disabled until dirty), matching the request that it "always be there." The old leave-guard was a plain `window.confirm("...Leave without saving?")` -- offering only leave-or-cancel, no way to actually save from the prompt. Replaced with a real three-way `.confirm-box` (Save & Leave / Leave Without Saving / Cancel), and widened its coverage: the screen's own Back button already went through a local handler, but bottom-tab and workspace-tab taps navigate via the router directly and previously bypassed it entirely. Added `useBlocker` (same guard `BuilderScreen`/`GoalsScreen` already use for their own unsaved-changes cases, both already importing it) plus a `beforeunload` listener for a hard reload/tab-close. Verified live: editing a jersey number, then tapping the bottom Home tab (a pure router nav, not the screen's own Back), correctly surfaced the three-way prompt; Save & Leave saved (confirmed the number persisted after re-opening the player) and navigated away in one step.

**Assistant-coach-add confirmation copy rewritten.** Was "{name} will get an email at {email}, and you can also just tell them: sign in at runofpractice.com with {email}." Now "{name} will get an email at {email} and will see {team name} when they log in." -- shorter, and tells the head coach what the invitee will actually see, not just how to reach them. `ModalLayer.jsx`'s `addedCoachInfo` now carries `teamName` (looked up from `modal.payload.teamId`) alongside name/email.

**My Team Assignments: em-dash removed from the "Show on Home" explainer.** The paragraph used a spaced `--` mid-sentence ("...Home agenda -- turning it off doesn't remove you..."), the same em-dash-substitute convention this file's own comments use, but which had leaked into user-facing copy. Split into two plain sentences instead.

**Real bug found and fixed: the "you were added to a team" welcome card could keep reappearing after being accepted.** Root cause: the mount-time effect that marks a `team_staff` row welcomed (`markTeamStaffWelcomed`) never refreshed the local `teams` state afterward -- the "Accept" button only ever set a component-local `dismissedWelcomeIds` Set, which resets to empty on remount. A full page reload always fetches fresh data (so `welcomedAt` reads correctly there), but *navigating away and back within the app* (tapping Teams then Home, no reload) remounts `HomeScreen` with a fresh empty dismissal set while `data.teams` still held the stale, never-refreshed `welcomedAt: null` -- so the card came back. Fixed by chaining `.then(() => refreshTeams())` onto the mark call, so the real data catches up within about a second of mount, independent of the local-only Accept dismissal. Confirmed live: `team_staff.welcomed_at` was set correctly (`2026-07-31T15:17:44Z`, moments after the assistant's first login), well before any subsequent navigation could have raced it.

**Teams page now shows the same coach-type/org shortcut Home's own team cards already do.** `TeamsListScreen.jsx`'s per-team row only showed sport/player-count/org name; added the same `myTeamRole(team, coachId)` label (e.g. "HEAD COACH" / "ASSISTANT COACH") Home's cards use, gated the same way (Coach mode only -- Org mode's own header already establishes context). Verified from both a head coach's and an assistant's view of the same team.

**Assistant coaches no longer see the team-options ellipsis.** `Layout.jsx`'s colored team-identity bar rendered the ellipsis (which opens "Edit Team") unconditionally for anyone viewing a team workspace -- an assistant coach, who can't actually manage the team, saw an affordance implying they could. Gated behind `canManageTeamInMode(team, coachId, mode)`, the same check already used elsewhere for Add Coach/Player, +Practice, etc. Required threading `coachId` down through `LayoutRoute` → `Layout` (wasn't there before). Verified live: the head coach's view of a team keeps the ellipsis; the assistant coach's view of the identical team has none.

**New: head coach gets notified on Home when an assistant coach or helper leaves a team.** `leave_team` was explicitly "silent to the head coach beyond the roster reflecting it" by original design (see the tenth-session migration's own comment) -- direct feedback asked for the same treatment the existing "you were added" card gets, in the other direction. Since the departing `team_staff` row gets archived and excluded from every existing fetch (`archived_at is null`), there was nowhere for the head coach's client to see it, so this needed a new table rather than another column: `team_departures` (migration `20260731090000_team_departure_notices.sql`), one row per self-initiated departure (populated inside `leave_team` itself, not `archiveStaff` -- a head-coach-initiated removal is something the head coach already knows about firsthand), visible via a `can_manage_team`-gated select policy, dismissed via a narrow `acknowledge_team_departure` RPC. Home renders it exactly like the requested pattern: "{name} ({role}) left {team}." with **Acknowledge** (a real server-side write, not a local-only dismissal like the welcome card's) and **View Coaches** (navigates straight to `/team/:id/roster`). Verified live end-to-end with two real accounts: added an existing coach as assistant, had them leave via My Team Assignments, confirmed the notice appeared for the head coach with correct name/role/team, confirmed View Coaches landed on the roster (now showing Coaches (1)), confirmed Acknowledge dismissed it, and confirmed it stayed dismissed after a full reload (server-side `acknowledged_at`, not session-local).

**Live verification.** Two disposable test accounts (a head coach and an assistant), a real team, a real player, a real assistant-coach add/accept/leave cycle, and a full Save & Leave round-trip on the roster -- all driven through the actual UI in a local dev server against production Supabase, not just reviewed as a diff. Both accounts and all their data deleted via the Admin API afterward.


### Twenty-first session (2026-07-30) — fifth same-day round: stripe side, real Run of Practice gap, a genuinely broken caret

Jax tested the twentieth session's changes live and came back with 3 more small items, same day.

**Practice Details stripe moved from right to left.** Purely a follow-up preference after last round's right-side placement -- `position:absolute` stripe now `left:0` instead of `right:0`, and the compensating 6px spacing moved from the chevron's `marginRight` to the "Practice Details" label's `marginLeft` (the stripe is now adjacent to the label, not the chevron).

**Real gap added between the green Run of Practice section and the black Practice Components bar below it.** This was a genuine bug, not a style preference: `runOfPracticeEndRef`'s `height:14` div was doing double duty as both the green backdrop's measurement endpoint (`eRect.bottom` in the `ResizeObserver` effect) and the intended bottom padding, so the green area painted through the entire 14px with zero actual gap before Practice Components started immediately after. Fixed by leaving that sentinel alone (it's still the green's correct bottom inset) and adding `marginTop:14` to the Practice Components bar itself instead -- that space is outside what gets measured into `runOfPracticeH`, so it now renders as real visible space rather than more green. Verified with an activity added (backdrop grows correctly, gap holds).

**Library Drills per-sport caret bug, fixed.** In `NewLibraryScreen.jsx`'s "My Drills" tab, the per-sport collapsible section header used a literal `"v"` character as its own hand-rolled caret, shown only when *expanded* (`collapsed[sport]?"":"v"`) -- meaning it disappeared entirely on collapse, backwards from what a disclosure indicator should do. Also matches Jax's separate complaint that it "looks too much like a v" -- it literally was the letter v. Replaced with the same `▶`/`▼` treatment already used by the three other per-sport/per-category collapsible sections in this same file (Equipment tab, Player Gear, Skill Tags), so it's now both correct and visually consistent with the rest of the screen.

**Live verification.** Local dev server, fresh disposable test account, a real team with an orange color (deliberately different from the app's own green, to make the stripe unambiguous), a scheduled practice opened in Builder, and a real drill added to the library. Confirmed live: the stripe renders on the left with clean label spacing, a real visible gap now separates green from black both empty and with a row added, and the Football drills section's caret flips between `▼` (expanded) and `▶` (collapsed) instead of vanishing. Test account and its data deleted via the Admin API afterward; temporary scripts removed from the repo.


### Twentieth session (2026-07-30) — fourth same-day round: margin regression, Practice Details walked back, real contact form, a third FK-deletion bug
Jax tested the nineteenth session's changes live and came back with 4 more items same day.

**Run of Practice margin regression, fixed.** The green backdrop (introduced last round to fix the sticky-containment bug) was rendering 14px wider on each side than Practice Components/My Drill Library. Root cause: it was `position:absolute; left:0; right:0` inside the padded `padding:"0 14px"` outer wrapper -- but an absolutely positioned element is placed relative to its containing block's *padding* edge, which by definition ignores that same padding, so `left:0` meant "flush with the wrapper's outer edge," not "inset by the wrapper's own padding" the way a normal-flow sibling (Practice Components' bar) automatically is. Fixed by anchoring the backdrop to `runOfPracticeStartRef` itself instead of the padded outer wrapper -- that sentinel is a zero-padding, normal-flow child of the padded wrapper, so it's already correctly inset, and `left:0`/`right:0` relative to *it* means what it looks like it means. Also simplified away the now-unneeded separate `top` offset tracking (`runOfPracticeBox` → plain `runOfPracticeH`), since anchoring to the sentinel makes `top:0` automatically correct.

**Practice Details walked back from a full team-color background to solid black with a stripe.** Direct follow-up: the full-bar team-color tint (added last round) read as too loud. Now always `var(--black)`, with `team.colorPrimary` (when set) rendered as a plain 6px `position:absolute` stripe down the right edge instead -- same "the team still shows up here" intent, more subtle execution.

**Real contact form replaces both "request a consultation" mailto links.** Previously a bare `mailto:` link on Settings' main list (non-admin coaches) and in the FAQ answer for "How do I set up an organization with multiple teams?" -- gave Jax nothing to prep for the actual sales call with, no phone number, no sense of club size. New `ConsultationRequestForm` (`LegalPages.jsx`, exported for `SettingsScreen.jsx` to reuse) collects name/email/phone/number-of-teams (required) plus an encouraged free-text field ("sport, location, timeline, anything that'll help us prep for the call"), and reuses the *existing* `feedback` table/`submitFeedback`+`submitPublicFeedback` RPCs rather than new backend surface -- exactly the "future non-authenticated contact form" this file's own Known Gaps entry already flagged `submitPublicFeedback` as being kept around for. `coachId` present (Settings, always authenticated) → the authenticated insert path with `user_id` attached; absent (FAQ, reachable signed out) → the anonymous RPC. The structured fields get formatted into the table's single `message` column (name/phone/team-count as labeled lines, then the free text) since a new dedicated table felt like overkill for what's still fundamentally a triaged-by-Jax-directly lead list. Real gap caught before shipping: the form's `send()` originally set "success" unconditionally without checking either helper's return value for an error -- for a sales-lead form, silently claiming success on a failed submit would be actively harmful, not just a minor bug. Fixed to check `result.error` and show a real error state with a fallback "email us directly" message instead.

**Copy reworded in both places** (no em dashes): "We set these up with you rather than offering instant self-service" → "Our team personally sets up every organization, so sports, teams, and coaches all start out configured correctly" -- same underlying process (org creation is still admin-gated, not self-serve), reframed from a limitation to a value-add, since a prospective club reading the old wording could reasonably read it as "this product isn't ready for real organizations."

**Settings card relocated.** Removed entirely from the main Settings list (the first thing anyone poking around Settings sees, arguably the worst place for anything that reads as "this needs us to configure it for you"); added to `AccountSection` under a new "Membership" header, between "Your Info" and "Legal," gated to `!isAdmin` (founder-admins keep the real self-serve create-org flow, unchanged, still on the main list).

**A third instance of the missing-ON-DELETE-action bug, found and fixed.** Hard-deleting the test account used for this session's verification failed the same way the `practice_series.created_by` and `organizations.created_by` bugs did in earlier sessions: an opaque `AuthRetryableFetchError` from the JS admin client, with the real Postgres error only visible via the raw REST endpoint -- `feedback_user_id_fkey` had no `ON DELETE` action, blocking deletion of any account that had ever submitted feedback (including, now, a consultation request). `feedback.user_id` was already nullable; `20260731000000_feedback_user_id_actor_fk.sql` drops and re-adds the FK with `on delete set null`, the same established pattern. Verified by retrying the hard-delete on the same account after applying the migration, which then succeeded. Two harmless test `feedback` rows (from verifying both the anonymous and authenticated submit paths) are still sitting in the table, clearly identifiable by their `claude-verify`/`claude-consult-test` emails -- that table deliberately has no delete policy for any role, including no `service_role` base-table grant for `DELETE`/`SELECT` (confirmed hitting `permission denied for table feedback` when trying), consistent with its own migration comment that triage is manual, by Jax, not through the app. Left for Jax to clear manually rather than granting broader table access just for this cleanup.

**Live verification.** Local dev server against the real Supabase backend, a fresh disposable test account, a real team (with a picked color) built through the actual UI. Confirmed live: the margin fix (screenshotted, all four sections' edges now align exactly), the black-bar-with-stripe Practice Details styling, both the anonymous (FAQ) and authenticated (Settings) consultation-form submission paths end-to-end (including a direct RPC call from a script to confirm `submit_public_feedback` returns `{success:true}`), the Settings card's new location under Account > Membership, and its removal from the main list. Test account and all its data (including the now-successfully-cascaded feedback row) deleted via the Admin API afterward; temporary scripts removed from the repo.


### Nineteenth session (2026-07-30) — third same-day Builder round: the sticky fix, and a real duration data-loss bug found along the way
Jax came back a third time after actually using the redesign, with 13 more Builder-specific items, again asking for clarifying questions or pushback where warranted. Asked one question up front (which section should take on the selected team's color -- Run of Practice, Practice Details, or both; answer: Practice Details only, Run of Practice keeps the app's own brand green) and flagged one real tension in the requests themselves before writing code: "auto-expand a newly-added drill" plus "keep it sticky-pinned to the top while scrolling" together recreate almost exactly the "very tall sticky element blocks scrolling" problem a code comment from two sessions ago was written specifically to avoid. Proceeded anyway since this session's *other* request (per-station collapse in Station Block) meaningfully shrinks the worst case, but said so rather than silently picking one behavior.

**What shipped, all in `BuilderScreen`/`ActivityConfigs.jsx` unless noted:**
- **Auto-expand + click-away collapse**: `addAct`/`addBlock`/`addComponentType` now all call `setExpandedId` (previously only the component/station paths did). A new `useEffect` attaches a single `document` `click` listener whenever a row is expanded, collapsing it (and clearing `lastAddedId`) on any click outside that row and outside any open overlay (`.closest(".movly")`) -- deliberately *not* trying to enumerate every button that has its own expandedId-setting logic (a Practice Component tile, another row's own header); instead both `setExpandedId`/`setLastAddedId` calls use functional updaters that only null out the id if it's *still* the same one the effect was attached for, so a same-tick update from some other handler (switching to a different row, adding a new activity) naturally wins regardless of which handler happens to run first in the click's bubble order, rather than being clobbered. Verified live via a scripted press-and-release sequence and by clicking Practice Details' own bar mid-edit.
- **The sticky fix (the real one).** Investigating "we had this before but something broke it" turned up the actual mechanism: `position:sticky` can never move an element past its own DOM parent's bottom edge, and the green box wrapping the activity rows (added when this section's redesign first landed, two sessions ago) is sized to fit almost exactly the rows themselves -- live-measured at just ~21px of "room" below the last row before the box's own bottom passed the stick point, nowhere near enough to visibly hold during a real scroll through the drill library below. Not something this session's own changes caused, but the real root cause, confirmed live via `getBoundingClientRect()` scroll traces both before the fix (row position tracked scroll 1:1, no hold at all past the ~21px window) and after. Fixed by a genuine restructure: the header/empty-state/rows now render as normal children of the same tall `padding:"0 14px"` wrapper Practice Components/My Drill Library already live in (so sticky has the *entire* rest of the page as room to work with), and the green background is a separate `position:absolute` backdrop sized via a `ResizeObserver` measuring two sentinel refs (`runOfPracticeStartRef`/`runOfPracticeEndRef`) rather than by actually containing the rows. `SortableActivityRow`'s `zIndex` is now always ≥1 (was `undefined` when not sticky/dragging) so every row reliably paints above the `zIndex:0` backdrop regardless of state. Re-verified live after the restructure: scroll-traced the same row and confirmed it now holds exactly at `top:55` across a real scroll range instead of sliding past it, and confirmed click-away-collapse still works correctly on a row that's simultaneously sticky.
- **Per-station collapse** (`StationConfig`): each station's header row gained a caret; collapsed shows just "Station N" + the drill/activity name, everything else (description, grouping, coach, equipment, players, ...) hides. Manual only, no auto-collapse -- new stations from "+ Add Station" still open expanded, matching how a freshly-added activity already behaves.
- **My Drill Library collapse caret**, matching Practice Details/Practice Components, defaulting open (`myDrillsOpen`, initial `true`).
- **Long-press preview removed entirely** (added just last round) -- a real gap found in actual use: a scrolling touch gesture was being misread as a long-press hold. Deleted `longPressHandlers` from `ActivityConfigs.jsx`, the preview modal, `previewItem` state, and the now-unused `assetsById`/`equipNames` helpers; drills and component tiles are plain tap-to-add again, unchanged from before that feature existed.
- **Practice Details**: moved inside the same `padding:"0 14px"` wrapper as its sibling sections -- it had been rendering *outside* that wrapper (relying on its own now-removed 14px `.card` padding to match), so once that padding became a black bar instead, its outer box was 28px wider than everything below it. Gained a Date field and a Duration (min) field for an already-scheduled practice (`editP`), reordered Date+Start Time paired above Location+Duration, both now backed by real `schedDate`/new `schedDuration` state. The bar itself now takes on `team.colorPrimary` when a team is selected (falls back to black).
- **Real bug found wiring up Duration, unrelated to anything asked for**: `handleSave`/`handleRun`/`doSchedule` never included `scheduledDurationMinutes` in their `savePracticeTree` calls, and that function does a full column `.update()` -- meaning saving an already-scheduled practice from Builder was silently resetting its target duration to `null` on *every single save*, the whole time this feature has existed. Threading `schedDuration` through all three calls (and reading it live for the progress pill, instead of the stale `editP` prop) fixes this as a side effect of the new UI, not a separate patch.
- **Unscheduled-build entry path**: `detailsOpen` now defaults to `!editP` (open for a fresh build, collapsed for an existing scheduled practice, was unconditionally `false`). The Schedule/Template button row is gone; Save's `onClick` is conditional (`editP` still saves directly, unchanged) -- for a fresh build it opens a small chooser (`bottomMode==="savechoice"`) offering "Template" (routes into the existing template-name flow) or "Add to Schedule" (opens the existing Schedule modal, which also gained the same Duration field).
- **Select-all on focus**: `ChecklistConfig`'s Name field (shared by all 6 checklist-type Practice Components, including "Other") now selects its existing text on focus, so typing immediately replaces a default name like "Other" instead of requiring a manual select-all first.
- **Practice Components persistence, investigated and confirmed already correct.** Re-read `getVisibleComponentTypes`/`setVisibleComponentTypes` carefully first (both looked right), then verified the actual reported scenario live rather than trusting the code review alone: toggled Station Block off, navigated all the way back to Home (not just within the same Builder mount), opened a brand-new unscheduled practice, and confirmed Station Block was still correctly absent from the tile row. No bug found or fixed here -- it was already working; flagged back rather than "fixing" something that wasn't broken.

**Live verification.** Same pattern as the prior two rounds: local dev server (`npm run dev`) against the real Supabase backend, a fresh disposable test session via the `.env` service-role key, a real team/player/location/drill built through the actual UI. Coordinate-based `computer` clicks continued to be unreliable in this environment (silently no-op'd more than once); every interaction in this round went through `javascript_tool` DOM dispatch instead (`.click()`, native-setter + `input`/`change` events), which was reliable throughout -- worth defaulting to that here going forward rather than `computer` clicks. Test account and all its data deleted via the Admin API afterward; temporary mint/cleanup scripts removed from the repo.


### Eighteenth session continued (2026-07-30) — Builder rebuilt again same day, this time verified live
Jax came back with 5 more Builder-specific requests immediately after the batch above, explicitly asking for clarifying questions first given the size (a new 7-type customizable "Practice Components" system, an animated brand mark, long-press previews). Asked 3 questions before starting -- long-press preview scope (drills only vs. every component type, including the empty generic ones), what the Practice Components "..." menu actually does (a persisted customize-the-toolbar picker vs. a one-shot add-anything menu), and whether the new component types (Water Break/Stretch/custom Checklist/Other) needed genuinely different live-run behavior or could just be more checklist activities with different defaults. Answers: preview everything (including natural-language "why you'd use this" blurbs for the 7 component types, since they have no real content otherwise; "Other" is explicitly a nameable one-off, never saved to the library), customize-the-toolbar with per-coach persistence, and reuse the checklist shape as-is (avoids touching the live-session runner, PDF export, or Goals bucketing at all, all of which already key off `act.type==="checklist"` generically rather than by name -- confirmed by grep before committing to this approach).

**What shipped, all in `BuilderScreen` (`App.jsx`) unless noted:**
- **Scroll-to-top after Done**: tapping Done inside an expanded drill/station/checklist config used to leave the scroll position wherever it was, which -- since the body collapsing back down shifts everything below it upward -- often dropped the coach somewhere in the middle of the drill library with no obvious context. Each `.ablk` row now keeps a plain ref in a `rowRefs` map; a new `collapseAndScroll(id)` collapses (`setExpandedId(null)`) and then `scrollIntoView({behavior:"smooth",block:"start"})`s that same row, via `requestAnimationFrame` so it runs after the collapse has actually shrunk the layout. All three configs' `onDone` now call this instead of the bare `setExpandedId(null)`.
- **Long-press preview**: a new non-hook `longPressHandlers(store,id,onLongPress,onTap,ms)` (`ActivityConfigs.jsx`) -- deliberately not a hook, since a whole list of these needs building one per row inside a `.map()`, which the Rules of Hooks don't allow; the mutable per-row timer state lives in one `Map` the caller keeps via a single `useRef`, so a re-render mid-press can't orphan a still-pending timeout the way a fresh-per-render hook closure would. A plain tap still adds directly (unchanged); holding ~500ms opens a preview modal (drill: name, sport/duration, description, coaching points, skill tags, equipment via new `assetsById`/`equipNames` helpers; component type: label, duration, and a short description) with "Back"/"Add to Practice" buttons, the latter running the exact same add path a tap would. Verified live that a real long-press-then-release does *not* also fire a normal add (the click-suppression `fired` flag), including via a scripted `pointerdown` → wait 700ms → `pointerup` + `click()` sequence that confirmed the row count stayed at 1.
- **Practice Components, reworked into 7 selectable types**: `PRACTICE_COMPONENT_TYPES` (`constants.js`) -- Intro, Closer, Checklist, Water Break, Stretch, Station Block, Other, each with a default name/duration and a natural-voice description (no em dashes) -- replaces the old hardcoded Intro/Closer/Station-Block-only tile row. `getVisibleComponentTypes()`/`setVisibleComponentTypes()` persist which types show as one-tap tiles in `localStorage` (`rop_practice_component_types`), defaulting to today's exact set (Intro/Closer/Station Block) so nobody's Builder changes shape unless they open the new "..." button and pick something different. The section header is now a black bar (matching Practice Details/My Drill Library) with a collapse caret and the "..." (a literal horizontal ellipsis, not the app's existing vertical 3-dot `.ell-btn` kebab, since Jax asked for "an ellipsis" specifically and this opens a whole picker, not a small dropdown) opening "Add/Remove Practice Components" -- a checklist-style picker with a green checkmark circle per type. `addChecklist(isClose)` was replaced entirely by a generic `addComponentType(key)` that looks up defaults from `PRACTICE_COMPONENT_TYPES` and either delegates to `addBlock()` (station_block) or creates a checklist activity -- confirmed via grep that no other screen (`NewLibraryScreen.jsx`'s TemplateWorkspace, `CommandScreen.jsx`'s mid-run LiveEditBuilder) references the old function; both have their own independent Intro/Closer logic and were deliberately left untouched, out of scope.
- **The Run of Practice, now solid green**: header and content (whether the empty message or the real list) share one `var(--green)` background with a dashed white-ish border framing the whole section, replacing the previous black canvas + separate light empty-state box. "The Run of Practice" is now bigger (20px, was 14px) and white. The duration pill is forced white-on-green for the normal case (the default `.pill` styling is light-green-on-white and would nearly vanish against solid green) but deliberately *not* forced when the under-planned `.pill.over` (red) state is active, so that warning still reads correctly rather than being silently hidden by the override -- caught and fixed before shipping, not found live.
- **Animated brand mark**: `RunOfPracticeMark` reuses `LoadingScreen`'s existing stopwatch SVG (the app's own loading-screen logo) but swaps its infinite CSS `tick` keyframe for a controlled `rotation` prop with a `transition:transform .6s` -- a `handRotation` state increments by 360 on every add (`addAct`/`addBlock`/`addComponentType`) and decrements by 360 on every remove (`remAct`), so the hand does one full spin forward or backward per action rather than spinning continuously. Verified live via the rendered `<g>` element's actual `style` attribute across an add/add/remove sequence: `rotate(360deg)` → `rotate(720deg)` → back to `rotate(360deg)`.
- **My Drill Library / Practice Details, black bars**: My Drill Library's bar recolored from green to `var(--black)` with an explicit `minHeight:40` (shared with Practice Components' bar) so the two read as the same thickness regardless of which buttons/icons they happen to contain -- previously they were both green but only incidentally similar heights. Practice Details got the identical black-bar treatment plus its own collapse caret, defaulting to **collapsed** (a new `detailsOpen` state, `false` initially) with a one-line summary (team + location) visible in the collapsed bar so collapsing it doesn't leave the coach with zero context.
- **Chevron moved**: the per-row expand/collapse chevron (added last round) now renders before the duration badge instead of between the duration and the red delete X, so a coach tapping near "expand" can no longer land on "delete" by mistake.

**Live verification, this time actually completed.** Started a local dev server (`npm run dev` via the `rop-dev` launch config, port 5173) against the real Supabase backend, minted a fresh disposable test session the same way as the seventeenth session, and this time the injection/interaction worked once auto mode was confirmed off (the Browser pane blocked the same category of action three times across this and the prior round before finally going through -- root cause still unclear, but retrying after explicit confirmation eventually succeeded both times). Coordinate-based `computer` clicks were unreliable in this environment (several silently no-op'd, e.g. the first two attempts at the name-capture Continue button and the Add Player Save button); dispatching real DOM events via `javascript_tool` (`.click()`, native-setter + `input`/`change` events for controlled inputs) was reliable throughout and is the better default here going forward. Built a real throwaway team ("Claude Testers," Basketball), player, location ("Test Gym"), and drill ("Layup Lines," with "Cones" equipment) entirely through the UI, then exercised every item above directly in Builder -- all confirmed working exactly as designed, including two bugs caught and fixed *before* this pass (the pill-override-hides-the-red-warning issue above, and an earlier over-engineered `SortableActivityRow` `outerRef`/hook-in-a-`.map()` design for long-press that was simplified before ever being tested). Also incidentally re-confirmed four items from the batch above live for the first time (Explore rename, sport-default/ordering, the skill-tags helper line, the equipment-not-added save guard) and sanity-checked that Station Block's existing Group By button still renders after `addComponentType` started delegating to `addBlock`. Test account and all its data (team, player, location, drill, practice) were deleted via the Admin API afterward; the temporary mint/cleanup scripts were removed from the repo, same as every prior round.


### Eighteenth session (2026-07-30) — 13-item direct-feedback batch, live verification attempted but blocked by tooling
Jax gave a single message with 13 distinct items spanning Builder, Home, the player-out picker, the skill-tag picker, the drill-equipment form, sign-in copy, and Library's Drills tab. Implemented all 13 and confirmed `vite build` clean, then tried to re-run the seventeenth session's real live-browser verification (disposable test account via the `.env` `SUPABASE_SERVICE_ROLE_KEY`, `generate_link`+`verifyOtp`, tokens injected into the Browser pane's `localStorage`) -- initially missed doing this at all until Jax pointed at the `.env` file directly. The mechanism itself still works end-to-end (minted a session, injected it, reloaded, landed correctly on the first-sign-in name-capture screen), but the sandboxed Browser pane's own permission layer then blocked further form-filling on `runofpractice.com` repeatedly and inconsistently -- it wasn't a clean auto-mode-on/off story: it blocked with auto mode on, blocked again immediately after Jax turned it off, then let one `javascript_tool` call through, then blocked `form_input` three more times in a row including right after being told auto mode was off. Given that inconsistency, stopped rather than keep guessing at a workaround. Both throwaway accounts (neither had gotten far enough to attach any real data) were deleted via the Admin API before stopping, and the temporary `scripts/tmp-verify-mint.mjs`/`tmp-cleanup.mjs` helper scripts were removed afterward -- not left in the repo. **Nothing in this session's changes has actually been seen live** -- flag anything that doesn't look right once it's actually seen, same caveat this file has raised before, but the credential/pattern are confirmed working for whoever picks this up next; the blocker was the Browser pane's permission handling, not the testing setup.

**Builder (`App.jsx`'s `BuilderScreen`), the largest single piece:**
- Station Block's "Group By..." button (`StationConfig`, `ActivityConfigs.jsx`) now shows which criterion was last applied ("Group By...Position") via a new local `groupByLabel` state, cleared by Generate Random/Clear Groups, set by picking Position or a handedness option. Purely a UI label, not persisted -- a coach can tap the button again to change the selection, same as before.
- A coach can now add a brand-new location straight from Builder's own Location `<select>`, not just when the team has zero locations configured: a `"+ Add New Location..."` option is always present in the dropdown (sentinel value `__add_new__`), routes to the existing `AddLocationDialog` (already wired for the zero-locations case, unchanged), which saves via `createLocation`/`createOrgLocation` (so it shows up in Library's Locations tab afterward) and auto-selects the new location on return.
- A margin gap below the Practice Details card, found by re-deriving the box model rather than guessing: `.screen` (`Layout.jsx`'s route wrapper) already gives every route 14px of horizontal padding, and the top nav row/`.card` each add their own 14px on top of that (28px total to their content) -- but everything below the card (the activity-count row, the activity list, Add Drills, the sport header, the drill list) relied on `.screen`'s 14px alone, so it read as flush against the edge by comparison. Fixed by wrapping that whole remainder in one `padding:"0 14px"` container to match.
- Two new dark-green (`var(--green)`) title bars, styled like the rest of the app's Barlow Condensed uppercase headers rather than a new pattern: "Practice Components" above the Intro/Closer/Station Block builders, and "My Drill Library" (with the existing "+ New Activity" button folded into it) above the actual drill list -- previously both lived under one flat "Add Drills" header with no visual distinction between "structural pieces" and "your saved drills."
- "Practice Setup" → "Practice Details" (the card's own `.clbl` label, wiring unchanged).
- The activity-count/duration row ("3 Activities" + the `35/60 min` pill) is now headed "The Run of Practice" and shows in both the empty and populated states (previously the whole row, including any header, was suppressed entirely until at least one activity existed or a scheduled duration was set).
- Save and Run Now moved up into the same row as Back (previously Back stood alone above a separate 4-button bar); the "Editing: Team · Date · Time" banner now sits directly under that row. Schedule/Template (only shown for a not-yet-scheduled practice) moved to their own row below the banner, same underlying `bottomMode` state machine, unchanged.
- Empty-state copy: "Nothing added yet. Select activities below to begin building your practice." → "...Add activities below to begin building your Run of Practice."
- Each drill/station row (`.abhdr`) now shows a chevron (`Ic.Chev`, already used elsewhere in the app) that flips between up/down with the expanded state, next to the existing duration badge -- direct feedback was that tapping the header to expand wasn't obviously interactive before.
- Once the practice has at least one activity, the whole list renders inside a `var(--black)` container so it reads as a distinct "this is what's actually being built" canvas, separate from the light Practice Details card above it and the light drill library below. The empty-state placeholder box stays its existing light/dashed style -- the dark treatment is specifically for the populated list, per Jax's own "being built" framing.

**Everywhere else:**
- Two text-wrapping fixes, same root cause in two files: `PlanPill` (`HomeScreen.jsx` and its independent copy in `ScheduleScreen.jsx` -- this codebase's existing per-file-duplication convention, not shared) rendered `{total}/{scheduledDurationMinutes} min` as plain adjacent text nodes with no `white-space:nowrap`, so a tight card could wrap between "0/60" and "min"; `HomeScreen.jsx`'s "N out" absence count had the same issue. Both now wrap in a `whiteSpace:"nowrap"` span so the phrase can't split mid-word, while the `" · "` separators around them can still wrap normally.
- Home's next-practice hero card gained a "Up Next" label (`clbl`) above it, distinguishing it from the "Upcoming Practices" list further down. The unplanned-state "Plan Practice"/"Not planned yet" button was still sized `.bxl` (a leftover from before the planned-state buttons below it were deliberately downsized to `.blg` in the sixteenth session to fix a phone-width overflow) -- now `.blg` too, matching every other button on the card.
- `AbsencePicker.jsx`'s "Mark out for..." step capped candidate practices to a 14-day window (`p.date <= in14Str`); removed entirely, so any scheduled, non-cancelled practice for the player's team is selectable, however far out. Covers both entry paths (`PracticeDetail`'s "Who's out?" and `PlayerProfile`'s "Mark Out For...") since both already route through this same component.
- Skill-tag picker (`SkillTagPicker`, `ModalLayer.jsx`): its search overlay is a `position:fixed` bottom sheet, and on mobile `100dvh` doesn't reliably shrink for fixed content when the on-screen keyboard opens (browser-dependent), so search results could end up partly or fully hidden behind the keyboard. Added a `useVisualViewportHeight()` hook (tracks `window.visualViewport`'s `resize` event) and sized the sheet's `height`/`max-height` to it instead of the default full-inset `.movly`. Not device-tested in this environment -- the mechanism is the standard fix for this exact class of bug, but flag it if it's not quite right on a real phone. Also added a gray explainer line under the "Skill Tags" field label ("Tagging skills helps power Goals & Insights and reporting on what your team actually practices"), no em dash.
- Drill add/edit form (`ModalLayer.jsx`): the equipment/gear "Add" inputs are plain uncontrolled DOM inputs (`new-equip-inp`/`new-gear-inp`), read only by their own Add button -- typing a name and tapping Save without ever clicking Add silently discarded the text with no linked equipment and no warning. `save()` now checks both inputs' live DOM values right before the `addActivity`/`editActivity` branches and blocks the save with an inline error if either has unsubmitted text.
- Sign-in screen ("Welcome, Coach"): removed the em dash from "Enter your email — we'll send you a sign-in code." → "Enter your email. We'll send you a sign-in code."
- Library's Drills tab: the My/Org Drills vs. "Team Libraries" pill (introduced the previous session's restructuring) relabeled to "Explore" per direct feedback.
- New-drill Sport default and ordering (`ModalLayer.jsx`): `defaultSport()` previously always preferred the most-recently-added library drill's sport whenever the library was non-empty, only falling back to the coach's own team sport if the library was completely empty -- so a single-sport coach whose library happened to contain a stray other-sport or General drill wouldn't reliably get their own sport as the default. Now a coach with exactly one team sport always gets that sport, full stop; a multi-sport (or team-less) coach still gets their last-added drill's sport, same as before. The Sport `<select>`'s option order (shared by the add and edit drill forms) is now the coach's own team sports first, then the rest of the full sport list in its existing order, via a new `orderedSports` array -- previously a flat, unordered `SPORTS` list regardless of what the coach actually coaches.


### Seventeenth session continued (2026-07-30) — Phase 2 org rework begins: org_create_team head-coach removal + canManageTeamInMode retrofit
Direct continuation of the seventeenth session's deferred org/library/equipment restructuring, scoped via 4 clarifying questions before starting: (1) include the `org_create_team` auto-head-coach removal, retrofitting all six affected screens rather than leaving it deferred again; (2) multi-org library switching reuses Home's existing org picker rather than a new UI; (3) org-owned equipment at a matching location auto-merges into a coach's available equipment when building/running a practice, rather than requiring a manual pull; (4) adding a team inside a multi-sport org restricts/defaults its sport to the org's own sports.

First piece landed: `org_create_team`'s auto-insert of the creating director as `team_staff` head_coach is removed (`20260730060000_org_create_team_no_auto_head_coach.sql`), and `canManageTeamInMode(team, coachId, mode)` — which already existed and already did the right mode-aware thing, but was only wired into `HomeScreen.jsx` — replaced bare `isHeadCoach`/`myTeamRole` checks in the six screens that had been blocking this removal: `RostersTab`, `GoalsScreen`, `PracticeDetail`, `ScheduleScreen`, `SeriesWizard`, `SchedulePracticeModal`. One naming collision surfaced during the retrofit: `ScheduleScreen` already had an unrelated local `mode` state (agenda/month view toggle); resolved by destructuring the incoming Coach/Org mode prop as `mode: appMode` and using `appMode` for all permission checks, leaving the view-toggle `mode`/`setMode` untouched.

Verified live in two passes. First, a scripted RPC-level check (fresh throwaway director → new org → `org_create_team` → confirmed zero `team_staff` rows for the new team → confirmed the director could still `insert into players` as an org admin with no personal staff row, i.e. RLS's `can_manage_team` org-admin branch was already correct and only the client-side gate needed fixing). Second, a full browser click-test with a real minted session injected into `localStorage`: switched to Organization Mode, opened the new org team, and confirmed Schedule showed "+ Practice"/"+ Series" (not the empty/no-permission state), Roster showed "+ Add" and "No players yet — tap + Add" (not the bare "No players yet." with no add affordance), and Goals & Insights rendered as a fully editable, unlocked goal-setting form — all despite the director having no `team_staff` row on that team.

**Incidental discovery while doing that live click-test**: reloading the page while in Organization Mode silently reverts to Coach Mode. Root cause: `App.jsx`'s guard effect that resets a stale persisted org selection (`if(mode.type==="org"&&library.myOrgs&&!library.myOrgs.some(...))setMode({type:"coach"})`) can fire on a fresh page load before `library.myOrgs` has actually finished its first fetch — an empty-but-already-truthy array satisfies `library.myOrgs&&...` and wrongly looks like "this org doesn't exist," resetting mode to Coach and then persisting that wrong value immediately via the adjacent `rop_mode`-writing effect. Not fixed yet (out of scope for this retrofit) — see Known Gaps.

**Second piece, same continued session: org multi-sport.** `organizations.sport` (single nullable text, 2026-07-22) replaced with `organizations.sports text[] not null default '{}'` (`20260730070000_org_multi_sport.sql`) rather than adding a parallel column — existing single values backfilled into one-element arrays first, then the old column dropped, so there's exactly one source of truth for an org's sports going forward (this project already paid once for a stale-parallel-column bug, on `team_goals`/`get_team_goal_report`, and it wasn't worth risking twice). Org Details' Sport dropdown became a multi-select chip row; adding a team inside an org with sports configured now restricts and defaults the new team's Sport field to just those sports (falls back to the old unrestricted full list for an org with none configured yet). Verified live end-to-end: created a throwaway two-sport org (Basketball, Soccer) via a real authenticated session, confirmed the Teams list showed "Basketball, Soccer," confirmed +Team's Sport dropdown contained exactly those two options with Basketball defaulted, then opened Org Details' edit form, added Baseball as a third sport via the chip toggle, saved, and confirmed the persisted read-back showed "Basketball, Soccer, Baseball."

**Third piece, same continued session: org equipment-at-location auto-merge, and a real leak found along the way.** Investigating how to merge org equipment into a coach's practice-building equipment picker turned up an existing, unrelated-to-this-feature bug: `ActConfig`/`StationConfig` (`ActivityConfigs.jsx`) filtered the equipment picker by sport and location only, with no owner/org check at all — under RLS that meant *every* asset for any org a coach belonged to showed up in every practice's equipment picker regardless of team or location, the exact same class of leak already fixed once this session in `EquipmentTab`'s Team-tab view (`visible Equipment`'s owner/org scoping, "9 baseball items already there, cones twice"). Fixed with a shared `ownedOrOrgAtLoc` helper: a coach's own equipment always shows; an org's equipment only merges in when this specific team belongs to that org *and* the asset is tagged to the activity's own location (an untagged org asset doesn't merge in the way a coach's own untagged equipment does — merging on "no location info" felt too permissive for equipment a coach doesn't own). Verified live: scheduled a practice for an org team at a "Club Gym" location, added a station, and confirmed an org-owned "Club Cones" asset tagged to that location appeared in the picker while a second org asset tagged to a different location did not.

**Fourth and final piece, same continued session: the Library restructuring** (`org_create_team`/`canManageTeamInMode`, org multi-sport, and the equipment auto-merge above were the three foundational, more schema-heavy pieces; this closed out the rest of the queued Phase 2 list in one pass, since all four remaining items — Explore-under-Drills, the per-org shelf becoming per-coach, Club Library's color, and dropping the per-team Equipment tab — touch the same one screen). The 5-tab content-type row (`NewLibraryScreen.jsx`) is unconditionally visible now instead of hiding behind the old page-wide My-Library-vs-Explore pill; that pill moved down into just the Drills tab as "My/Org Drills" vs. "Team Libraries." The old flat "From {org}" shelf became one shelf per coach who's shared a drill into that org, with a real gap found and fixed along the way: the first attempt resolved names via `data.profilesById`, which turned out to only ever resolve an org_staff co-member's name (`profiles_select_org_co_member`'s actual RLS condition) — a coach who's merely `team_staff` on one of the org's teams (verified live to be exactly what `can_share_drill_to_org` itself requires as the minimum bar to share at all) resolved to nothing but "A coach." Fixed by falling back to `team_staff`'s own stored first/last name (already fetched into every team's `coaches` array, no extra query needed) ahead of giving up. Club Library also picked up a colored accent bar under its title using the org's own color (same "so it looks like their org" treatment Layout.jsx's bottom tab bar already had), and the team workspace's Equipment tab is gone entirely in Org mode (`Layout.jsx`'s `teamWorkspaceTabs`, plus a redirect-away guard on `TeamEquipmentRoute` for a stale direct link) since equipment is now managed centrally from Club Library instead. `copyDrillToMyLibrary` also became mode-aware: copying while in Org mode now writes to the org's library (organization-owned, resolving equipment against the org's own asset pool) instead of always landing in the copier's personal library regardless of which mode they were actually working in. Verified live end-to-end with a real two-coach org: the per-coach shelf showed "CoachJane Smith (1)," opening it showed her shared drill with a "Copy to Org Library" button (mode-aware label), and clicking it produced a row with `organization_id` set and `owner_user_id` null, visible back on the Org Drills shelf with "From A coach" attribution. Test data (2 throwaway accounts, 1 org) fully torn down afterward.


### Seventeenth session (2026-07-30) — landing-page batch, live audio/notes UX, then a ~40-item backlog
First half of the session: a batch of landing-page and live-session requests, handled before the much larger backlog below. Landing page FAQ answers updated (schedule-adjustment wording, "different sports" now opens with "Yes"), the Practice History section's copy tightened and its mockup replaced (real drills with planned-vs-actual minutes and two notes tied to specific drills, one tagging a player by first name, instead of a made-up "88 min wall time"), the Practice Builder mockup updated to show the real app's `x/y min` under-planned pill instead of a flat total, and a stray parenthetical removed from the "smooth practice starts" copy. In the app itself: a one-time "Turn On Audio?" popup now shows for both the coach's `CommandScreen` and the anonymous `HelperView` the moment either lands in a live session (audio defaults off and was easy to never discover otherwise); the anonymous helper's note composer replaced its "End-of-practice" checkbox with a two-option "General"/current-drill toggle; Library's "+ Build Practice" now offers a real choice between an unscheduled build and picking an upcoming scheduled practice (a new `SchedulePracticePicker`, agenda/month view) instead of always jumping straight into an empty unscheduled plan; and Goals & Insights' History list gained a red dot for any session with a note nobody's reviewed yet plus a "Mark all as viewed" action, backed by a new `notes.viewed_at` column and an extended `get_team_session_history` (`20260730020000_note_review_tracking.sql`) — reviewing is marked automatically the moment the head coach opens that session's detail.

Second half: Jax dropped a large, unstructured list covering landing-page copy/visuals, live-practice audio/notes UX, and a wide batch of bug fixes and UX polish across scheduling, equipment, goals, onboarding, and auth, plus a genuinely large org/library/equipment restructuring mixed into the same message. Given the size, ran a parallel investigation pass first (4 Explore agents covering org/team model, equipment/library architecture, builder/scheduling UX, and settings/auth/onboarding) before writing any code, then asked 4 clarifying questions rather than guessing on the consequential/irreversible ones. Confirmed answers: (1) org creation becomes a consultation request for regular coaches, with a real admin-only create-org-and-hand-to-a-director flow; (2) the org/library/equipment restructuring gets its own dedicated future pass — everything else (this session) goes first; (3) the getting-started checklist becomes a persistent, clickable Home card, not just a popup, and the green dot goes away; (4) Goals gets both a disabled Save at non-100% *and* an unsaved-changes leave warning.

**Real bugs found and fixed, not just requested UX changes:**
- **Leave-team leak**: the global `/schedule` route (`ScheduleLegacyRoute`, `App.jsx`) passed raw unscoped `data.teams`/`data.practices` straight through, unlike `HomeRoute`/`TeamsRoute` which already scope via `teamsForMode`/`homeTeamsForMode`. A team a coach had genuinely left (their own `team_staff` row archived) stayed visible as a filterable schedule option and kept showing its practices — not an RLS bug (`can_access_team`'s org-membership branch correctly keeps a team visible to org staff independent of personal `team_staff` rows, by design, for org oversight), just a missing client-side scope. Fixed by scoping `ScheduleLegacyRoute` the same way; confirmed a plain owned-team deletion already worked correctly via a completely different path (`archived_at`), which is why only the org-team leave case was ever reported.
- **Equipment duplication/leak**: `EquipmentTab`'s `sportFilter` path (the Team tab's Equipment view) filtered `data.assets` by sport only, no owner/org check — leaking in Public Library catalog equipment (RLS deliberately makes `source_catalog_id` rows visible to every coach) plus, in principle, any other coach's owned assets sharing a sport. This is exactly what produced "9 baseball items already there, cones twice" for a coach/org that added none. Fixed by reusing the Library tab's own `visibleEquipment()` scoping (own/org, catalog excluded), narrowed to the one team's sport; `TeamEquipmentRoute` needed `mode` threaded through, which it wasn't receiving.
- **New-signup email was Supabase's stock unbranded template, confirmed live via `supabase config push`'s diff output**: `enable_confirmations=true` routes a brand-new email's *first* `signInWithOtp()` call through the `confirmation` mail type, not `magic_link` — only `magic_link` had ever been customized (branded, code-based via `{{ .Token }}`), so new signups got Supabase's default "Confirm your email address" link-based email instead of the code the app's own UI tells them to expect. Added a matching branded `confirmation.html` template and pushed config; the diff Supabase printed before applying (`content = "<h2>Confirm your email address</h2>..."`) is direct confirmation this was the actual live bug, not a guess.
- **Disabled buttons had no visual disabled state anywhere in the app**: `.btn` and every color variant (`.primary`, `.outline`, ...) set a solid background/color with no `:disabled` override. This directly explains the reported Goals confusion — Save was *already* correctly gated on totals summing to exactly 100% (`canSave`), but looked pixel-identical whether enabled or not, so a coach could tap it, watch nothing happen, and have no idea why. Added a single global `.btn:disabled{opacity:.4;cursor:not-allowed;}` rather than a Goals-specific fix, since the same invisible-disabled-state gap existed on every other disabled button in the app.
- **Builder margin gap**: the sticky action-bar header (border-bottom, no margin of its own) sat completely flush against the "Practice Setup" card's top edge with zero space between them, on every entry path (Home hero, PracticeDetail Edit, Library +Build Practice) — not a difference *between* paths as first suspected, but one consistent gap present regardless of how Builder is reached, matching Jax's own "any screen" framing once re-read carefully. Fixed with a small `marginTop` on that first card. Not visually confirmed against the real thing (no login in this environment) — flag if it's not quite the issue seen live.

**Also done:** `organizations_select_member` RLS relaxed so any team member (not just `org_staff`) can read their own org's name (org name now shown on Home/Teams team cards); added-to-team banner redesigned to "Accept"/"Manage Team Assignments" instead of a raw "Leave" link; Builder's drill-add list now excludes Public Library/catalog drills (was showing them mixed into "your own" library); inline add-location dialogs (with optional sublocations) added to both Builder and the quick-schedule modal for a coach with zero locations; SeriesWizard collapsed from 5 sequential screens to 2; Schedule's empty state now prompts to create a team; bottom-nav active-tab underline plus a persistent team-workspace back-to-Teams button; Coach/Org toggle relabeled "Coach Mode"/"Organization Mode"; Settings' redundant per-org "switch to Org mode" rows removed (Home's own toggle already covers it); NameScreen reworded (asterisk convention, dropped subtext); Deactivate Account section un-scared (was "Danger Zone," new confirm copy verbatim per Jax's wording) and reactivation is no longer silent (a real prompt: reactivate or exit); Terms/Privacy's back button now returns to Settings > Account (was hardcoded to Home) via `location.state.openSection`, the same mechanism the added-to-team banner's "Manage Team Assignments" link uses to deep-link into a specific Settings section; new `/faq` page (natural voice, no em dashes, covers account deletion and multi-team org setup) replacing "Getting Started" in the "?" menu now that the checklist lives on Home itself.

**Deliberately not done:** `org_create_team`'s auto-head-coach insert (see Known Gaps) — asked for, investigated, found to be load-bearing across six other screens' permission gating, and left alone rather than risk locking directors out of their own teams under time pressure with no way to test live. The full org/library/equipment restructuring bundled into the same original message was scoped as its own future pass per direct confirmation, not attempted here.

Every migration (`20260730020000_note_review_tracking.sql` from earlier in the session, `20260730030000_org_name_visible_to_team_members.sql`) and the `confirmation.html` config push were applied directly to production via `supabase db push`/`config push` — no local Supabase environment in this setup, same as always. At the time this batch shipped, nothing had been click-tested (no login credentials available yet in this environment) — flagged explicitly as a gap, with a follow-up recommended before relying on any of it. See the next entry for what happened once that gap was actually closed, same session.

**Real live-browser verification, same session, immediately after.** Jax asked for a login credential rather than leaving everything code-review-only. Talked through why a credential shouldn't live in this file (or anywhere git-tracked — permanent in history, readable by anyone with repo access) or even in the chat transcript itself, and landed on the safer version: a `SUPABASE_SERVICE_ROLE_KEY` in a local `.env` (already gitignored), used only to mint disposable test sessions via the Admin API, never to touch real user data. Verified the whole mechanism end-to-end before relying on it (create a throwaway user, mint a session, confirm it authenticates as that user, delete it) — then actually used it: real director + coach + org + org-team + personal-team test accounts, injected into the Browser pane by writing the minted session tokens directly into `localStorage` (`sb-<project-ref>-auth-token`), clicking through the real signed-in app against production via a local dev server (same Supabase backend either way).

Confirmed working, live, not just in code: the leave-team fix (left an org team, watched it vanish from both the Schedule filter and Teams tab), the equipment dedup fix (confirmed real catalog items like "Basketballs"/"Cones"/"Pinnies" exist for Basketball but correctly don't leak into Team Equipment), the Builder drill-scope fix (confirmed real catalog drills exist but don't appear in Add Drills), both location dialogs, the Builder margin fix on both entry paths, the Library Build-Practice picker (including creating a real 17-practice series via the now-2-step SeriesWizard and picking one from the scheduled-practice picker), Goals' numeric input + visibly-grayed disabled Save + the leave-confirm dialog (fired with the exact right message), the persistent Getting Started card, the trimmed "?" menu, the FAQ page, the added-to-team banner, org name display, the bottom-nav underline, the team-workspace back button, Settings' copy changes, and the full deactivate → sign back in → reactivation-prompt → reactivate flow. One thing from the prior session's own list didn't reproduce (Library's title reads "Library," not "My Library," in Coach mode, contrary to an earlier report) — noted as possibly stale rather than "fixed."

This pass also caught a real bug incidentally: `get_team_goal_report` erroring on every call (see the Goals & Insights entry above for the fix) — something no amount of code review would have surfaced, only an actual RPC call against production did.

**Cleanup, a real gotcha, and a real bug it led to (fixed same session, in a follow-up turn).** All disposable test data was torn down — teams/orgs archived (`archived_at`; `organizations` has no delete grant for any role and no app-level "delete an org" feature at all, so archiving is the only option even for cleanup), then the underlying auth users deleted via the Admin API, cascading through real `on delete cascade` chains (`profiles` → `teams.owner_user_id` → practices/team_staff/etc.) once the non-cascading `organizations.created_by` reference is cleared first. One of five test accounts — the one with a real recurring series attached — repeatedly failed hard-deletion via `admin.deleteUser()` with an opaque `AuthRetryableFetchError` / empty `{}` message. The JS SDK was swallowing the real error; calling the raw REST endpoint directly (`DELETE /auth/v1/admin/users/{id}`) surfaced it: `practice_series_created_by_fkey` — **`practice_series.created_by` was `not null references profiles(id)` with no `ON DELETE` action at all**, unlike every other actor-identity column in this schema (see `notes.created_by`'s established nullable + `ON DELETE SET NULL` pattern, "historical practice truth that should outlive the author's account"). This is very likely a real piece of the already-documented "hard account deletion blocked by an FK-cascade ordering issue" gap below — *any* real coach who ever created a recurring schedule would hit this same block if they asked for account deletion. Fixed properly (`20260730050000_practice_series_created_by_actor_fk.sql`: dropped the `NOT NULL`, re-added the FK with `ON DELETE SET NULL`) rather than just working around it for cleanup purposes — verified by retrying the hard delete, which then fully succeeded (profile, team, and all associated data physically gone, confirmed by direct query). A `shouldSoftDelete: true` retry earlier in the process had reported success while actually just anonymizing/banning the account with zero cascade — worth remembering that `deleteUser()`'s soft-delete path can look identical to a real success in its response shape.


### Sixteenth session continued (2026-07-30) — PDF branding/access, presence, Setup nav, audio settings
- **Home hero button overflow, fixed**: the Practice Setup / Start Practice buttons (added the prior session) overflowed off the viewport on phone-width screens. Root cause: both were `.bxl` (nowrap text, large padding) inside `flex:1` with no `min-width:0` — flex items default to `min-width:auto`, so neither could actually shrink below its own text width and the row overflowed instead of the two buttons splitting the space evenly. Fixed by sizing down to `.blg` and adding `minWidth:0` to both. Verified at 375px.
- **Practice Setup screen gained navigation**: a "Back to Home" link and a persistent "Start Practice" bar, both `can_manage`-only — previously a coach reaching Practice Setup from Home had no way back except the browser's own Back button, and no way to start the run from that screen at all. Needed `practice_id` added to `get_preview_view`'s output.
- **"Who has this open" presence**: a small badge on `PreviewView`, the staff `CommandScreen`, and `HelperView` showing which coaches currently have a practice's setup/live view open and how many anonymous viewers there are. Pure Supabase Realtime Presence (`subscribeToPracticePresence`, `src/supabase.js`) — no new table, no RLS, ephemeral client-to-client state over the same public-channel websocket infra `subscribeToLiveSession` already uses. Keyed by `practice_id` (not `live_session_id`) so a coach in setup and a helper already on the live link show up together — needed `practice_id` added to `get_live_session_view` too. Verified end-to-end with two real anonymous browser tabs on the same link: the badge correctly showed "2 people watching" and dropped back to "1" the instant the second tab closed.
- **PDF export, branded and made permanent**: `PracticePlanPrint.jsx` gained a small letterhead (app icon + "Run of Practice" wordmark) and the eyebrow line now reads "THE RUN OF PRACTICE FOR {team} ON {date}" instead of a bare "PRACTICE PLAN" — so a printed sheet handed to a parent or another coach is recognizably from the app. More importantly, **the Print/Export PDF button only ever existed on `PracticeDetail`, which a past/already-run practice never routes to** — `ScheduleScreen.jsx`'s own routing comment says so explicitly ("Any past/planned practice... goes to HistoryViewer"). Added the same button (reusing the same component, same data each screen already has) to `HistoryViewer` (`CommandScreen.jsx`, reached via Schedule/Calendar → a past date) and `SessionHistoryDetail` (`GoalsScreen.jsx`, Goals & Insights → History tab) — the two places Jax's own question ("calendar day and history from that page?") correctly guessed at. `SessionHistoryDetail` needed a new `data` prop threaded through from `GoalsScreen` (it never received the app's full `data` before, only `session`/`practice`/`team`).
- **Live Practice Audio settings** (Settings screen, new "Live Practice Audio" nav item): after several rounds of chasing one "right" whistle sound, made both the time's-up sound (Whistle/Buzzer/Ding/Beep) and the announcer voice coach-selectable instead, each with tap-to-preview. Two new synthesized files (`ding.wav` — a bell-like chime, `beep.wav` — a clean two-tone alert) reuse assets from the earlier "3 alternatives" detour that got rejected as the *default* but work fine as *options*; `gym-buzzer.wav` (unused since the original whistle swap) becomes the Buzzer option. Stored in `localStorage`, not synced server-side, since voice availability is inherently per-device. Voice picking started as a Male/Female heuristic, then got replaced same-day with a full list of every installed voice (see Session Log, "Whistle re-synthesis" v6) after the heuristic's first pick sounded bad.


### Sixteenth session (2026-07-29) — batch of 10 feature/UI requests
- Jax gave 10 requests in one message; working through them in order of complexity, verifying each against production before moving on rather than batching all 10 into one commit at the end.
- **Helper-view skill tags + player focus (`/live/:token`)**: the anonymous helper view never surfaced a drill's skill tags or a player's per-category focus notes, even though the staff-side `CommandScreen` already had both (`tagNamesForLibraryId`/`noteForPlayerAtDrill`). Since the anonymous view has no client-side `activityLibrary`/`skillTags`/`players.focusAreas` to work with, this had to be resolved server-side: extended `get_live_session_view` (new migration `20260729000000_helper_view_skill_tags_focus.sql`) to add `skill_tags` (name array) and `player_focus` (`{player_id: note}` map) to both `current_activity` (via `practice_activities.library_activity_id`) and each `stations[]` entry (via that station's *own* `library_activity_id` — different stations in the same rotation can run different drills with different focus areas). Matching is by `category_id` only, not `skill_tag_id`, mirroring the staff-side logic exactly and the 20260720000000 category-level migration. Verified against a real production session (a basketball "5-on-4 to 5-on-5" drill) before applying — real skill tags and real per-player notes resolved correctly. `HelperPlayerChip` became a real interactive button (was a static, non-functional "tap to focus" hint) that toggles the player's note inline; added a shared `HelperSkillTags` tag-row component reused across the plain-drill, station-intro, station-overview, and station-detail render paths.
- Landing page copy: hero reverted to "Plan the practice. Run it live. Keep everyone aligned.", the Live Adjustments & Timers paragraph rewritten per Jax's exact wording, the Transition Support mockup (`ROTATION_MOVES`) reordered to Location → Coach → Drill (matching the real app change below), and "Ava's Dad (helper)" renamed to a normal coach name in the Pre-Practice Setup mockup — all just mock-data/copy edits, no schema changes.
- Live rotation/transition screen (`CommandScreen.jsx`, both the real `CommandScreen` and `HelperView` transition-card labels): reordered from "Drill Name - Coach - Location" to "Location - Coach - Drill Name" to match what a coach actually needs first when hustling players between stations.
- Buzzer → whistle: `gym-buzzer.wav` swapped for a synthesized `whistle.wav` (no real whistle sample existed in the repo; downloading one would need explicit permission per the file-download policy, so generated one via a throwaway Node script — two frequency-modulated ~3kHz blasts, 16-bit PCM mono). **Re-synthesized once already** (still same session, see below) after Jax asked for something more authentic.
- The remaining 3 items from the same 10-item batch were completed in follow-up turns of this same session:
  - **Library publisher filter**: `activity_library` gained three nullable "copied from" columns (`copied_from_owner_user_id`/`organization_id`/`catalog_id`), set once by `copyDrillToMyLibrary` at copy time — previously a copied drill was indistinguishable from a self-authored one the moment it landed in "My Library." `NewLibraryScreen.jsx`'s "mine" shelf now shows a "From {publisher}" line per drill and a Publisher section in the existing Filter modal.
  - **Station leader assignment**: `get_preview_view` now returns `can_manage` (computed server-side off `auth.uid()` via `can_coach_team`, since the anonymous share link and the signed-in coach's own "Practice Setup View" hit the identical `/preview/:token` route/component) and, only for a manager, the team's coach roster and each station's real id — `PreviewView` shows "No leader assigned -- tap to assign" per station plus a "Share Setup Link" button. Separately, the live `CommandScreen` now warns once, right after attendance, if any station_block station still lacks a leader (assign/share/leave-unmanned). Home's hero presents "Practice Setup" and "Review Plan"/"Start Practice" as co-equal side-by-side buttons instead of Setup being a small link underneath.
  - **Practice plan PDF export**: a "Print / Export PDF" button on Practice Detail renders `PracticePlanPrint.jsx`, a standalone print-optimized document (not a screenshot of the app UI), handed off to the browser's native Print/Save-as-PDF via a new `#rop-print-root` + `@media print` block in App.jsx's `CSS` constant — no client-side PDF library, no server round trip. Shows duration *and* a computed clock start/end range per drill (anchored to the practice's start time, reusing the same `actMins()` formula already shown on-screen), plus description/coaching focus/skill tags/equipment/grouping/per-player focus notes, same treatment per station in a station block.
- **Whistle re-synthesis, four rounds** (still 2026-07-29, a chain of same-day follow-ups): Jax asked for the whistle to sound like an authentic basketball-referee whistle in a loud arena, not just "a whistle sound." Every round was sent directly to Jax to listen to first, since audio can only be verified here technically (valid WAV, no clipping, correct duration) — never by ear.
  - v2: modeled on a Fox 40-style *pealess* whistle — three close, non-harmonic carriers (2850/3020/3160 Hz) whose interference/beating is what makes a real pealess whistle harsh and able to cut through crowd noise (a single clean tone reads as a toy). Added filtered breath-turbulence noise, breath-pressure sag, and a slight tremolo; one long (~1s) hard blast.
  - v3: "too much breath" — pulled the noise/turbulence way down and removed the sag/tremolo (closer to a modern *electronic* whistle, which is a battery-driven tone generator, not blown, so it doesn't carry breath noise or fade). Changed pattern to a quarter-second blast, brief pause, half-second blast (a double-blow), per spec.
  - A detour: Jax asked for 3 non-whistle alternatives in case something less "sports" would be less obnoxious if several coaches' phones played it in sync — a bell-like chime (inharmonic decaying partials, a falling major third), a clean two-tone digital alert (single frequencies, no beating), and a rising chirp (urgency via pitch contour, short/percussive). All three were built and sent, but rejected — the whistle direction was preferred after all.
  - v4: single ~0.5s blast (dropped the double-blow) followed by a spoken **"Time"** once the blast ends, wired via `whistleAudioRef.current.onended` calling the existing `speak()` (`speechSynthesis`) function — reuses the exact mechanism already used for "Two minutes remaining," not a second audio system. The spoken word carries the "second blast"'s job now, so one clean whistle hit is enough.
  - v5 (2026-07-30, next session): rather than keep guessing at one "right" sound/voice, made both coach-selectable in Settings instead. See "Live Practice Audio settings" above.
  - v6 (2026-07-30, same day, a follow-up): the Male/Female gender-heuristic picker's first pick ("Daniel") sounded bad — replaced entirely with a full voice-list picker (every installed voice, previewable) rather than trying to fix the heuristic, since there's no reliable way to infer voice quality from a name. See "Live Practice Audio settings" above.
  - v7 (2026-07-30, same day, session-closing follow-up): even the full device voice list was "simply awful" per Jax, aside from Samantha and Daniel. Rather than keep hunting for a better synthetic voice, agreed the real fix is recorded human audio for the fixed phrases (`"Two minutes remaining."`, `"Time"`, `"Next up!"`, `"Audio on"` — no dynamic text is ever spoken, so this is fully feasible as static clips) — Jax is arranging recordings (own voice and/or others) to send as `.wav`/`.mp3` files. In the meantime, narrowed the voice picker's list down to just Samantha/Daniel (`CURATED_VOICE_NAMES`) so the coach isn't wading through two dozen novelty voices for no reason.


### Fifteenth session (2026-07-28) — a real regression from the thirteenth session's dead-code sweep
- **`fetchLibraryData()` has been throwing on every single call since the thirteenth session, silently emptying `assets`/`skillTags`/`activityLibrary`/`catalogs`/`myOrgs`/`pendingOrgInvites`.** Reported by the user as "I lost the Coach/Org toggle" — the toggle is genuinely the most visible symptom (`HomeScreen.jsx` only renders it when `data.myOrgs.length > 0`), but the actual blast radius was the entire `library` state, since one failed fetch inside `fetchLibraryData()` fails the whole function.
- Root cause: the thirteenth session's dead-code sweep deleted `fetchPendingOrgInvites` from `supabase.js`, having concluded it was unreferenced. The verification grep at the time was `grep -rn "\b$fn\b" src --include="*.jsx" --include="*.js" | grep -v "src/supabase.js:"` — deliberately excluding matches *within* `supabase.js` itself, to avoid a function's own `console.error('functionName:', ...)` line counting as a false-positive "usage." That same exclusion also hid the one real call site: `fetchLibraryData()` (same file) calls `fetchPendingOrgInvites()` directly. The other 8 functions removed in that same pass were re-checked this session and are confirmed to have zero references anywhere, including within `supabase.js` — this was a one-function miss, not a systemic pattern, but it's exactly the kind of miss that same-file exclusion in a grep will reliably produce, worth remembering next time a "same-file usage" filter feels like the right way to reduce grep noise.
- This also retroactively explains part of the earlier "production hang" incident (fourteenth-session-adjacent, actually thirteenth-into-fourteenth): `fetchLibraryData()` was *already* broken by the time the `Promise.all([refreshTeams(),refreshLibrary(),refreshPlanning()])` loading gate was introduced, so `refreshLibrary()` was rejecting on every load from the start. Switching to `Promise.allSettled` correctly fixed the *hang* (nothing should block the whole app on one fetch failing), but it also means the broken fetch's failure became silent instead of loud — the app loaded fine, just with empty library data, and nothing surfaced that until a coach who actually uses Org mode noticed the toggle was gone.
- Fixed by restoring `fetchPendingOrgInvites` verbatim from git history (`git show acfc8cf~1:src/supabase.js`), same implementation, same location, same comments. Verified the restored query directly against production (`SET LOCAL ROLE authenticated` + the real coach's JWT claims, rolled back) before considering this done, not just a clean `npm run build`.
- Lesson: a "verified via whole-repo grep" claim is only as good as what the grep pattern excludes. A function's *own* file is not a safe exclusion zone for "is this called anywhere" — its own other functions are exactly where an internal helper is most likely to be called from.

- **Auth email rate limit raised, and a real gap in what "Resend" actually buys you clarified.** Jax hit "email rate limit exceeded" on the sign-in screen and asked why, assuming Resend (custom SMTP) meant no rate limiting applied. It doesn't work that way: `auth.rate_limit.email_sent` (in `supabase/config.toml`) is the limit that specifically *governs* custom-SMTP-sent emails — its own comment says as much ("Requires auth.email.smtp to be enabled") — custom SMTP changes *which* limit applies, not whether one does. It was still at Supabase's default of 2/hour, trivially easy to exceed while testing. Raised to 30/hour (matching the file's other per-project defaults) and pushed via `supabase config push --yes`.

  While answering "what's the actual daily cap," traced every email-sending path in the app: Supabase Auth OTP emails are the only thing `email_sent` governs; the two Edge Functions (`notify-team-staff-added`, `notify-org-invite`) call Resend's API directly and aren't subject to that Supabase setting at all. A code comment in `_shared/emailTemplate.ts` confirms the project is on Resend's *free tier* — which almost certainly makes Resend's own account-wide plan cap (not anything in this app's config) the actual binding constraint across all email types combined. Flagged as directional, not verified (no access to the live Resend dashboard from here) — worth Jax confirming the real number there rather than trusting either Supabase limit as the ceiling.

  Before pushing the config change, checked the one thing this file already warns about: `config push` unconditionally overwrites `site_url`/`additional_redirect_urls` to whatever's in the local file, regardless of what else is being changed — confirmed both were still correct production values (not stale localhost/dev placeholders) before pushing. Also learned something new worth recording: `config push`'s output reports every service (`api`, `db.settings`, `auth`, `storage`) as `up_to_date` on a **successful** push, not just on a no-op — the wording can't be used to distinguish "nothing changed" from "change applied successfully." Don't read `up_to_date` as "the push did nothing."


### Fourteenth session (2026-07-28) — Assistant Coach handoff (visibility, live-session control, notes)
- Worked from `ROP-AssistantCoach-Handoff.md` (a fresh spec doc, companion to this file). Before writing anything, checked what the spec assumed against what actually existed — this caught that roughly half of §1 (assistant visibility + read-only plan view) was **already fully built**: `PracticeDetail.jsx` already gated Edit/Plan/Cancel behind `isHeadCoach` while leaving Run available to everyone (exactly the "read-only plan, full run access" ask), and Settings → My Team Assignments already showed every team + role + a `show_on_home` toggle for every coach, not gated to Org mode as the spec assumed. Only two genuinely new pieces got built for §1: a role label on Home's team cards, and a one-tap "Practice Setup View" button that reuses the existing `/preview/:token` page (via `findOrCreatePreviewToken`) rather than building a second equipment-by-location view.
- **§1.3 (live-session control)**: the confirmed strict-single-controller model was already shipped almost exactly as specified (`isController` already gated every action, a non-controller already saw a "Take Control" button) — except mid-run plan editing (`LiveEditBuilder`) was gated on `isController` alone, with no head-coach check, contradicting the one explicit exception the spec called out ("stays gated on role, not on control"). Fixed (`isController && amHeadCoach`). Also rebuilt the take-control affordance: moved from a top banner with a bold primary button into the exact spot where advance/±1min/timer controls normally render, styled deliberately low-emphasis (outline) — the spec's own reasoning was that a bold button in that specific tap zone risks an accidental control-seizure mid-drill, since that's where a thumb lands out of habit. Added a non-blocking toast for whoever just lost control (recommended default in the spec, not asked-for-then-confirmed, but adopted since the doc explicitly invited flagging if unwanted and none came).
- **§2 (notes) had a real near-miss worth remembering**: the spec's own §0.1 said "notes were built once and removed... this is a new system, not a resurrection," referring to the old `NotesTab`/`app_data`-blob notes deleted two sessions ago. That framing was correct about *that* system but incomplete — a **different, currently-live** `notes` table (added 2026-07-14, already wired into the live CommandScreen's quick-note/end-of-practice UI and into both `HistoryViewer` and `SessionHistoryDetail`'s read paths) covers almost the entire staff side of this spec already. This wasn't caught until *after* a full parallel system (`session_notes`/`note_player_tags` tables, `create_session_note`/`submit_session_note_by_token` RPCs, a new composer UI) was designed, migrated to production, and verified working — only surfaced while wiring up `SessionHistoryDetail` and noticing `fetchNotesForPractice` already existed and was already imported there. Root cause: trusted the spec's framing about "no existing notes system" without independently checking for one under a different name, the same class of mistake this file's own Session Log has flagged before (thirteenth session, doc-vs-code drift) — should have grepped for any table already shaped like this before designing a new one, not after.
  
  Corrected same-day, before real usage: a follow-up migration dropped the parallel `session_notes`/`note_player_tags`/both RPCs entirely (zero rows had ever been written to them outside rolled-back test transactions) and extended the **real** `notes` table instead — `author_kind`/`author_label`/`submitted_via_token_id` columns (nullable/defaulted, safe since the table had zero rows), a `note_player_tags` join table keyed on `notes(id)`, an archive UPDATE policy (the table had never had one — "quick-capture-only" per its original 2026-07-14 comment), and one new anonymous RPC (`submit_practice_note_by_token`, matching the existing anon-RPC pattern exactly: token validated server-side via the existing `validate_token()`, never a client-asserted author). The staff-side UI change shrank to match: rather than a new composer/panel, the existing quick-note input and end-of-practice textarea got `@`-mention support added directly. Only `HelperView` (anonymous) got a genuinely new UI surface, since it had no note-taking capability at all before.
  
  A second real bug surfaced by testing, not review: the archive UPDATE policy was added without the underlying `GRANT UPDATE ON notes TO authenticated` — RLS policies are inert without the base grant they sit on top of. Caught by testing the actual archive path end-to-end (`SET LOCAL ROLE authenticated` + real coach JWT claims, inside a rolled-back transaction) rather than assuming the policy alone was sufficient, matching this file's established verification convention. Fixed with a one-line follow-up migration.
  
  **Confirmed before migrating** (per the spec's own instruction not to guess on this): both the 500-character length cap and the 40-notes-per-token rate limit were built in from the start, not deferred.
  
  Every migration this session (four total: the initial parallel system, its correction, the grant fix, and none others needed) was dry-run inside `BEGIN;...;ROLLBACK;` before applying for real, and the final anonymous-write path was verified end-to-end in a rolled-back transaction — a real token created, a real note + player tag submitted through the actual RPC, a stranger's direct table insert confirmed rejected — before calling any of this done.

- **Real-usage bug, same day**: an assistant coach viewing a team's Schedule tab could still see and use `+ Practice`/`+ Series` for a team they don't head-coach. Root cause: `ScheduleScreen.jsx`'s `canScheduleAny` checked `data.teams.some(t => isHeadCoach(t, coachId))` — "is this coach head coach of *any* team at all" — correct for the global cross-team Schedule tab, but never re-scoped to the *specific* team being viewed when reached via a team workspace (`fixedTeamId` set). An assistant on Team A who happened to head-coach Team B saw the buttons on Team A's schedule because the check only ever looked at "any team," never "this one." Fixed by branching on `fixedTeamId`: scoped to that one team's `isHeadCoach` check when set, falling back to the original "any team" check only for the unscoped global view. Same session, also fixed the related UX gap this surfaced: `SchedulePracticeModal`/`SeriesWizard` always defaulted their team picker to `myTeams[0]` (whichever head-coached team sorted first), never the team you were actually standing in — added a `presetTeamId` prop (passed as `fixedTeamId` from `ScheduleScreen`), honored only if the coach actually head-coaches that team (falls back to `myTeams[0]` otherwise, so a stray id can't slip through).


### Thirteenth session (2026-07-27) — doc consolidation + dead code
- Six overlapping docs (this file plus `DATABASE-ARCHITECTURE.md`, `ROP-Goals-Nav-Snapshot.md`, `ROP-Org-Experience-Handoff.md`, `ROP-Public-Library-Spec.md`, `ROP-Sharing-Addendum.md`) had drifted out of sync with each other and with the actual code — most notably, `DATABASE-ARCHITECTURE.md` §7 documented a standalone `OrgHomeScreen.jsx` as shipped, but a later session had already retired it in favor of folding org content into `HomeScreen.jsx` under Coach/Org mode; and `ROP-Public-Library-Spec.md` self-described as "planning only, nothing built" when the Public Library feature it specified had, in fact, since been built and shipped. Rather than trust any one doc, the consolidation cross-checked claims against `git log` and the actual source before merging — caught both of the above, plus a stale "14-day agenda" claim in this file's own feature inventory (the agenda list is 7 days; a *different*, intentionally wider 14-day window exists only for the needs-a-plan nudge — not a contradiction once separated).
- Dead code removed from `src/`, each verified via a whole-repo grep (not just `src/`) before deletion: the orphaned `/team/:teamId/build` route, its `TeamBuildRoute` wrapper, and `BuildTab.jsx` (the tab bar link to it was already dropped in an earlier session per this file's own text, but the route/component were never cleaned up — reachable only by typing the URL directly); and nine unused exported functions from `supabase.js` — `fetchPracticeSeries`/`archivePracticeSeries` (series-scoped cancel/restore now goes through `cancelPractice`'s `scope:'future'` path instead), `reschedulePractice` (no call site anywhere — rescheduling, if built, went a different route), `fetchNoteCountsForPractices` (this file's own 2026-07-15 entry documents deleting its only call site and never removed the export), `endLiveSession` (superseded by a generic `updateLiveSession` call), and ~~four leftovers from the first org-model implementation that a later "Coach/Org mode" session reworked — `promoteDrillToOrgLibrary`, `fetchPendingOrgInvites`, `orgAssignTeamStaff`, `orgAssignPlayer`~~ **correction, 2026-07-28: `fetchPendingOrgInvites` was not actually dead — see the fifteenth-session entry below for the real bug this caused and its fix.** The other three (`promoteDrillToOrgLibrary`/`orgAssignTeamStaff`/`orgAssignPlayer`) were genuinely unreferenced anywhere, including within `supabase.js` itself, and stayed removed. `submitPublicFeedback` was deliberately **not** removed — see the Library section above, it's already-documented intentional dead code, not an oversight. `npm run build` verified clean after every removal.
- **A second, dedicated verification pass** (triggered by a direct question — "only Baseball/Basketball have categories" didn't match what the user knew to be true) re-checked every concrete factual claim in the Feature Inventory and Known Gaps sections against migrations/code directly, rather than trusting the first pass's synthesis. It found and fixed four more real errors, all now corrected in place above: (1) `skill_categories` actually covers 6 sports, not 2 — a 2026-07-18 migration added Football/Soccer/Lacrosse/Volleyball, and the per-coach tag-seeding function was never updated to match, so Baseball/Basketball coaches likely get duplicate-looking tags (personal copy + global) while the 4 newer sports don't; (2) `org_staff.role`'s CHECK constraint only allows `'director'` — `admin` doesn't exist as a value yet, despite being described as already-differentiated; (3) `app_data` was wrongly described as dead/unreferenced — it turned out to be the live backing store for `App.jsx`'s legacy `data`/`update()` state (see the next entry for the full trace and its resolution); (4) a wrong Edge Function name (`org-invite-notify` → `notify-org-invite`). The root cause across all four: the first pass verified some claims against code/git directly but carried others over from the source docs' own framing without redoing that check.
- **Full trace of `App.jsx`'s legacy `data`/`update()` state, and its removal.** Asked to finish what the second verification pass started: what does `app_data` actually still do? Traced `fullData` (`App.jsx`, what every screen actually receives) to `Object.assign({},data,{teams},library,planning)` — since later arguments win, `library`/`planning` overwrite every field they share a name with (`teams`, `locations`, `assets`, `activityLibrary`, `templates`, `practices`), so the *only* survivor from the raw `app_data`-backed `data` object was `.notes`, and nothing read it once `NotesTab` was gone (previous entry). On the write side, a repo-wide grep for actual `update(...)` invocations (not just the prop being passed around) found exactly one call site, inside `ModalLayer.jsx`'s `"editTemplate"` modal branch — and that modal type was never once opened anywhere (`openModal("editTemplate", ...)` has zero call sites; its sibling `"addTemplate"` had no submit handler at all even if reached). Templates are actually edited today through `TemplateWorkspace` (`NewLibraryScreen.jsx`, a real `saveTemplateTree`-backed flow) — the modal branch was a leftover from before that existed. Removed the dead modal branch (variable, submit handler, form JSX, two orphaned `TITLES` entries), which left `update()` with zero call sites anywhere in the app.
  
  With both sides of `app_data` proven dead, removed the whole pathway: `data`/`setData` state and the `update` callback in `App.jsx`; `loadData`/`saveData`/`flushSave`/`setCoachKey` in `supabase.js`; `INIT`, `DEMO_INIT` (an entirely unused demo-coach seed object — not imported anywhere despite a comment claiming otherwise), and `migrateData` (a shape-repair function for the old blob, still carrying a hardcoded `c_jaxon2`→`c_jaxon1` id fix from the pre-rewrite POC era) in `constants.js`; and the now-pointless `update` prop threaded through `HomeScreen`, `ScheduleScreen`, `RostersTab`, `BuilderScreen`, `CommandScreen`/`HistoryViewer`, `PracticeDetail`, `NewLibraryScreen` (which didn't even destructure it), and `ModalLayer`. `fullData` was renamed to plain `data`, now built directly from `teams`/`library`/`planning` with no `app_data` blob underneath it at all.

  One real behavior change, not just deletion: `loaded` (gates the "Loading your data..." spinner) used to flip once `loadData()` resolved. Re-wired to a single effect that runs `Promise.all([refreshTeams(),refreshLibrary(),refreshPlanning()])` on `coachId` change and flips `loaded` after all three resolve — the three individual mount-triggering effects that used to call each one separately were folded into this one. Verified via `npm run build` (clean, slightly smaller bundle) and a dev-server load of the signed-out landing page (no console errors) — the signed-in `loaded` path itself couldn't be click-tested, same standing credentials limitation noted throughout this file's history.

  **This shipped a real production incident.** `Promise.all` has no `.catch()` — a rejection in any one of the three fetches leaves `loaded` stuck at `false` forever, since `.then()` never fires. The old gate never had this failure mode (it was checking something fully decoupled from these three fetches), so this was a new bug introduced by the refactor, not a pre-existing one surfaced by it. It hit in production immediately after deploy (app stuck on the loading screen for the real user). Fixed same-day by switching to `Promise.allSettled`, which always resolves regardless of individual rejections — logs which fetch failed via `console.error` rather than silently hanging. Lesson for next time this pattern comes up: gating UI on `Promise.all` of several independent data fetches is only safe if every one of them is guaranteed to resolve (they're not, in general) — `allSettled` (or a per-fetch `.catch()`) is the default-safe choice.

- **Legacy POC tables dropped** (`app_data`, `coaches`, old `live_sessions`) once `app_data`'s last reference was gone (previous entry). Before dropping anything on the live production database, verified rather than assumed: queried current row counts (5/2/76, matching the already-documented orphaned-POC snapshot), checked `app_data`'s actual keys and confirmed none matched the real coach's actual `auth.users` uuid (all were old POC-era ids like `coach_coach_demo_wwql`), and ran the `DROP TABLE` wrapped in `BEGIN;...;ROLLBACK;` first to confirm no dependency errors (views, FKs) before committing to it for real. Also had to reconcile 2 migrations (`20260724000000`, `20260724010000`) that were already live in the schema but unrecorded in the CLI's migration-history table — the exact drift this file had flagged as pending since the 2026-07-21 session — via `migration repair --status applied` before the drop migration could push cleanly. Applied via `supabase db push`; confirmed post-drop that all three tables are gone and the new migration is correctly recorded with no new drift.

- **Baseball/Basketball skill-tag duplication, fixed.** Root cause confirmed by querying the real coach's actual tags directly rather than reasoning about it in the abstract: every Baseball/Basketball category had an identically-named `scope='coach'` row (from the 2026-07-11 per-coach seeder) sitting next to the `scope='global'` row the 2026-07-18 migration added — 947 duplicate rows across all 17 coaches who'd ever signed in (mostly disposable test accounts), confirmed via direct query before touching anything. A coach's own genuinely custom tags were never at risk — the fix only ever matches on an *exact* `(category_id, name)` collision against an existing global tag, so tags like "Passing on the move," "Layups," "Cutting," and "Transition defense" (none of which are in the seed function's hardcoded list) were confirmed untouched afterward.

  Before deleting anything, checked what actually referenced the duplicate rows: 15 real `drill_tags` rows pointed at a duplicate instead of its global twin (0 `player_focus_areas` rows did). Deleting the duplicates outright would have cascade-deleted those 15 real tag associations (`drill_tags.skill_tag_id` is `ON DELETE CASCADE`), so the migration re-points them to the global tag first — dropping only the rare case where a drill was already tagged with *both* the duplicate and its global twin (which would otherwise violate `drill_tags`' `UNIQUE(activity_library_id, skill_tag_id)`) — then deletes the now-unreferenced duplicates. Dry-run confirmed `drill_tags`' total row count was identical before and after (202), meaning every real tagging survived, just re-pointed.

  `seed_default_skill_tags_for_coach` was also updated (`CREATE OR REPLACE`) so it no longer special-cases Baseball/Basketball — every sport now falls through to the same empty-array no-op. The function and its trigger are kept in place rather than removed, since `ensureDefaultSkillTags` is still called client-side on every sign-in and a future sport could legitimately want true per-coach starter tags again; today it just does nothing for any sport, matching how Football/Soccer/Lacrosse/Volleyball already worked. Applied via `supabase db push`; confirmed post-migration that zero duplicates remain, `drill_tags`' count is unchanged, and the coach's real custom tags are intact.


*This file supersedes and replaces `DATABASE-ARCHITECTURE.md`, `ROP-Goals-Nav-Snapshot.md`, `ROP-Org-Experience-Handoff.md`, `ROP-Public-Library-Spec.md`, and `ROP-Sharing-Addendum.md`, all removed 2026-07-27. Their content is folded in above where still accurate, or captured in the Session Log where superseded. Recover any of them from git history if needed: `git log --all --diff-filter=D -- '<filename>'`.*
